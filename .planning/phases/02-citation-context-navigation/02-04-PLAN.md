---
phase: 02-citation-context-navigation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/citation_context.py
  - frontend/ui.py
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "User can select any citation callout and see its sentence context in the right pane"
    - "Selected callout badge shows a tick mark for the current selection"
    - "Switching callouts updates citation metadata for the new selection"
  artifacts:
    - path: "backend/citation_context.py"
      provides: "Context lookup resolves sentence + neighbors for any callout index"
    - path: "frontend/ui.py"
      provides: "Callout selection state and context fetching per selected callout"
  key_links:
    - from: "frontend/ui.py"
      to: "backend/citation_context.py"
      via: "get_citation_context API call"
      pattern: "get_citation_context"
    - from: "frontend/ui.py"
      to: "session_state citation_selected_index/target"
      via: "select_citation"
      pattern: "citation_selected_index"
---

<objective>
Restore reliable callout context loading and selection state so every citation callout swaps the context pane and highlights the active selection.

Purpose: Close human verification gaps where only the first callout shows context or selection feedback.
Output: Consistent backend context lookup and normalized frontend selection state for all callouts.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-citation-context-navigation/02-citation-context-navigation-VERIFICATION.md
@backend/citation_context.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix citation context lookup for non-first callouts</name>
  <files>backend/citation_context.py</files>
  <action>
Update `get_citation_context` to resolve the selected callout by the full callout list index first, then fall back to target-id matching only when the index is invalid. Do not filter the callout list before index selection; keep the response payload identical (target_id, callout, sentence, previous_sentence, next_sentence). This prevents target-id filtering from hiding valid callout indices.
  </action>
  <verify>python -m compileall backend/citation_context.py</verify>
  <done>Any callout index from the frontend maps to a context response even when a target_id is present.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize selection state and context requests in the UI</name>
  <files>frontend/ui.py</files>
  <action>
Normalize callout target IDs (treat empty/falsey values as None) when rendering callouts, selecting a callout, and building the context request. Use the normalized target ID for both the selection state and the comparison that renders the tick mark. Ensure the context request key uses the normalized target ID so context reloads when switching between callouts.
  </action>
  <verify>python -m compileall frontend/ui.py</verify>
  <done>Clicking any callout updates `citation_selected_index/target`, displays a tick on that callout, and triggers a context fetch keyed to that selection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Consistent callout selection state and context loading for every citation</what-built>
  <how-to-verify>
    1. Run `streamlit run frontend/ui.py` and open a document with multiple callouts.
    2. Click several different callout badges across multiple sentences.
    3. Confirm the right pane updates sentences/metadata for each click and the clicked badge shows a tick mark.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the remaining issue.</resume-signal>
</task>

</tasks>

<verification>
Manual UI verification is required to confirm callout switching and tick mark behavior for multiple callouts.
</verification>

<success_criteria>
- Multiple callouts reliably load context without showing "Context unavailable yet" when data exists.
- The selected callout badge shows a tick mark for any callout in the list.
- Switching callouts replaces context sentence + metadata each time.
</success_criteria>

<output>
After completion, create `.planning/phases/02-citation-context-navigation/02-04-SUMMARY.md`
</output>
