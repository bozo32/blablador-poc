---
phase: 02-citation-context-navigation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/grobid_client.py
  - backend/extraction.py
  - backend/reference_resolver.py
  - backend/schemas.py
  - backend/ingestion_store.py
  - backend/main.py
  - frontend/ingestion_api.py
  - frontend/ui.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "User can see GROBID-consolidated reference metadata alongside resolution results."
    - "Crossref and OpenAlex resolution outputs are compared and mismatches are flagged."
    - "When results disagree, user can select the correct reference and the choice persists."
  artifacts:
    - path: "backend/extraction.py"
      provides: "GROBID consolidated reference metadata in extraction output"
    - path: "backend/reference_resolver.py"
      provides: "Parallel Crossref/OpenAlex resolution with mismatch detection"
    - path: "backend/main.py"
      provides: "Resolution endpoint and user-choice update endpoint"
    - path: "frontend/ui.py"
      provides: "Mismatch resolution prompt with selectable candidates"
  key_links:
    - from: "backend/main.py"
      to: "backend.reference_resolver.resolve_references"
      via: "resolve endpoint"
      pattern: "resolve_references\("
    - from: "backend/main.py"
      to: "backend.ingestion_store.update_ingested_document"
      via: "resolution selection endpoint"
      pattern: "update_ingested_document"
    - from: "frontend/ui.py"
      to: "frontend.ingestion_api.submit_resolution_choice"
      via: "selection action"
      pattern: "submit_resolution_choice\("
---

<objective>
Add consolidated GROBID reference metadata and a Crossref/OpenAlex comparison flow with user resolution when results conflict.

Purpose: Surface mismatched citations early and let users confirm the right reference for downstream citation navigation.
Output: Consolidated extraction data, comparison-aware resolution payloads, and a minimal UI control for resolving mismatches.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-citation-context-navigation/02-02-SUMMARY.md
@.planning/phases/02-citation-context-navigation/02-03-SUMMARY.md
@backend/grobid_client.py
@backend/extraction.py
@backend/reference_resolver.py
@backend/main.py
@frontend/ui.py
@frontend/ingestion_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture GROBID consolidated reference metadata</name>
  <files>backend/grobid_client.py, backend/extraction.py</files>
  <action>
Add GROBID consolidation parameters when calling `processFulltextDocument` so TEI includes consolidated bibliography data.
Extend `parse_bibliography` to extract consolidated fields (title, authors list, year, container/journal, DOI, URL) and store them under a new `grobid` object per reference entry while keeping existing `raw_reference`, `doi`, and `url` fields for backward compatibility.
Avoid changing the overall extraction payload shape outside the bibliography entries.
  </action>
  <verify>
python - <<'PY'
from backend.extraction import parse_tei

tei = """
<TEI xmlns=\"http://www.tei-c.org/ns/1.0\"><text><body></body></text>
<teiHeader><fileDesc><sourceDesc><listBibl>
<biblStruct xml:id=\"b1\"><analytic><title>Sample Title</title>
<author><persName><forename>Ada</forename><surname>Lovelace</surname></persName></author></analytic>
<monogr><title level=\"j\">Journal</title><imprint><date when=\"1843\"/></imprint></monogr>
<idno type=\"DOI\">10.1234/abc</idno><idno type=\"URL\">https://example.org</idno>
</biblStruct></listBibl></sourceDesc></fileDesc></teiHeader></TEI>
"""
refs = parse_tei(tei)["references"]
assert refs and refs[0].get("grobid", {}).get("title") == "Sample Title"
print("ok")
PY
  </verify>
  <done>Each bibliography entry includes a `grobid` object with consolidated metadata fields populated when present.</done>
</task>

<task type="auto">
  <name>Task 2: Compare Crossref/OpenAlex results and persist user selection</name>
  <files>backend/reference_resolver.py, backend/schemas.py, backend/ingestion_store.py, backend/main.py</files>
  <action>
Add OpenAlex resolution support in `reference_resolver.py` and run Crossref/OpenAlex lookups in parallel per reference (ThreadPoolExecutor or similar) using DOI when available and title-based search otherwise.
Introduce a comparison helper that normalizes DOI and title strings, then flags `match`, `mismatch`, `missing`, or `needs_review` when Crossref/OpenAlex disagree with each other or with GROBID consolidated metadata.
Update `ResolvedReference` schema to include `grobid`, `crossref`, `openalex`, `status`, `mismatch_reason`, and `selected_source` fields while keeping existing `doi`, `title`, `year`, `source`, and `confidence` populated from the selected/best candidate.
Add a new POST endpoint (e.g. `/ingest/{doc_id}/resolution/{reference_id}/select`) that stores the user's selected source into the resolution payload and updates the derived `doi/title/year/source` fields.
Use `update_ingested_document` to persist the selection without rewriting unrelated resolution entries.
  </action>
  <verify>
python - <<'PY'
from backend.reference_resolver import compare_candidates

grobid = {"title": "Sample Title", "doi": "10.1234/abc"}
crossref = {"title": "Sample Title", "doi": "10.1234/abc", "source": "crossref"}
openalex = {"title": "Different", "doi": "10.9999/xyz", "source": "openalex"}
status, reason = compare_candidates(grobid, crossref, openalex)
assert status == "mismatch"
print("ok")
PY
  </verify>
  <done>Resolution payloads include GROBID/Crossref/OpenAlex candidates plus a status flag, and user selections can be persisted via the new endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Surface mismatches in the UI and allow selection</name>
  <files>frontend/ingestion_api.py, frontend/ui.py</files>
  <action>
Add an ingestion API helper that posts a resolution selection to the new backend endpoint.
In the citation context pane, detect `resolution.status` values of `mismatch` or `needs_review` and render a compact selector listing GROBID/Crossref/OpenAlex candidates with title + DOI preview.
On selection, call the new helper and refresh the citation context so the selected candidate becomes the active `resolution` entry; keep the UI minimal (single select + apply button + short status text).
  </action>
  <verify>Run `streamlit run frontend/ui.py` and confirm mismatched references show a selector that updates the resolved metadata after applying a choice.</verify>
  <done>Mismatched references show selectable candidates and applying a choice updates the displayed resolution metadata.</done>
</task>

</tasks>

<verification>
- `python - <<'PY'` snippets in Tasks 1-2 pass.
- `streamlit run frontend/ui.py` shows the mismatch selector and selection updates the resolution view for a known mismatched citation.
</verification>

<success_criteria>
- Consolidated GROBID metadata is stored with each bibliography entry and used during resolution comparison.
- Resolution payloads include Crossref/OpenAlex candidates plus a mismatch status and rationale.
- Users can resolve mismatches via the UI and the chosen reference persists in stored resolution data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-citation-context-navigation/02-05-SUMMARY.md`.
</output>
