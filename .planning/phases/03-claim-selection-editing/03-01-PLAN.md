---
phase: 03-claim-selection-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/claim_queue.py
  - frontend/ui.py
autonomous: true

must_haves:
  truths:
    - "Clicking a citation callout adds a new claim to the queue"
    - "Queued claims include candidate segments generated from the citing sentence"
    - "Claim queue state persists across Streamlit reruns"
  artifacts:
    - path: "frontend/claim_queue.py"
      provides: "Queue state helpers and claim segmentation"
    - path: "frontend/ui.py"
      provides: "Session state initialization and enqueue hook"
  key_links:
    - from: "frontend/ui.py"
      to: "frontend/claim_queue.py"
      via: "select_citation -> enqueue_claim"
      pattern: "enqueue_claim"
    - from: "frontend/claim_queue.py"
      to: "BlabladorClient"
      via: "segment_claim_candidates"
      pattern: "segment_claim_candidates"
---

<objective>
Establish claim queue state and segmentation helpers that turn citation callouts into candidate claims.

Purpose: Provide the data backbone for claim review and editing without changing UI layout yet.
Output: Session-state queue storage, segmentation prompt helpers, and enqueue wiring in the Streamlit app.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claim-selection-editing/03-CONTEXT.md
@.planning/phases/03-claim-selection-editing/03-RESEARCH.md
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claim queue state + segmentation helpers</name>
  <files>frontend/claim_queue.py</files>
  <action>
Create a new claim queue helper module that owns session-state storage and candidate claim segmentation.

- Define `init_claim_queue_state()` to set default `st.session_state` keys for queue order, queue items, claim edits, originals, and processed status.
- Implement `segment_claim_candidates(sentence, row_idx, model)` using `BlabladorClient` with a prompt that enforces clause-level segmentation, preserves attribution phrases and parentheticals, and outputs numbered lines; parse lines into clean claim strings and fall back to the full sentence when no segments are returned.
- Implement `enqueue_claim(queue_item)` to add a unique claim entry (keyed by doc_id + citation_index + target_id) to the queue if missing, populate candidate claims via `segment_claim_candidates`, and store originals for hover/reset usage.
- Keep helpers pure to session state; avoid any UI rendering in this task.
  </action>
  <verify>python -m py_compile frontend/claim_queue.py</verify>
  <done>Claim queue state keys exist, and `enqueue_claim` stores candidate claims for a citation sentence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire queue initialization + enqueue hook</name>
  <files>frontend/ui.py</files>
  <action>
Connect the claim queue helper to the Streamlit app without altering the visible layout yet.

- Import `init_claim_queue_state` and `enqueue_claim` from `frontend/claim_queue.py`.
- Call `init_claim_queue_state()` inside `init_session_state()` so queue storage is always present.
- Update `select_citation` (in the citation callout click path) to call `enqueue_claim` with the citation sentence, callout, doc_id, citation_index, target_id, and current LLM model (fall back to `selected_model` or empty string if unset).
- Ensure the enqueue logic runs before context loading so the queue updates immediately when a callout is clicked.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>Clicking a callout enqueues a claim entry and initializes its candidate segments in session state.</done>
</task>

</tasks>

<verification>
- Run `streamlit run frontend/ui.py`, click a citation callout, and confirm a new claim queue entry appears in `st.session_state` when inspecting `st.session_state` (e.g., via `st.json`).
</verification>

<success_criteria>
- Claim queue state and candidate segmentation helpers exist and are wired to citation callout clicks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claim-selection-editing/03-01-SUMMARY.md`
</output>
