---
phase: 04-evidence-attachment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/claim_queue.py
  - frontend/clipboard.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Copy controls place the retrieval instructions payload onto the system clipboard or expose a reliable fallback block."
    - "The UI surfaces explicit success feedback that echoes the copied instructions so reviewers know what was captured."
    - "Retrieval instructions remain accessible even when clipboard APIs are unavailable."
  artifacts:
    - path: "frontend/clipboard.py"
      provides: "Reusable helper that renders a Streamlit component wired to navigator.clipboard with graceful fallback."
    - path: "frontend/claim_queue.py"
      provides: "Retrieval instructions renderer that calls the clipboard helper and shows inline fallback text."
  key_links:
    - from: "frontend/claim_queue.py"
      to: "frontend/clipboard.py"
      via: "render_copy_to_clipboard helper"
      pattern: "from frontend\.clipboard import"
---

<objective>
Make the "Copy instructions" control actually copy retrieval instructions (EVD-01) with visible confirmation and a fallback text surface.

Purpose: Close verifier gap that flagged the stubbed clipboard button so reviewers can trust the copy action and see what was copied.
Output: Clipboard helper module plus updated retrieval instructions UI that emits real copy events and fallback messaging.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-attachment/04-CONTEXT.md
@.planning/phases/04-evidence-attachment/04-VERIFICATION.md
@frontend/claim_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build clipboard helper with fallback messaging</name>
  <files>frontend/clipboard.py</files>
  <action>
- Create `frontend/clipboard.py` containing a `render_copy_to_clipboard(label: str, payload: str, *, key: str, toast: str) -> None` helper that:
  - Uses `streamlit.components.v1.html` to render a `<button>` wired to `navigator.clipboard.writeText(payload)` and emits a custom Streamlit event via `Streamlit.setComponentValue` so Python can detect success/failure.
  - Provides graceful degradation for environments without clipboard access by exposing a read-only `<textarea>` with the payload and instructions telling users to press ⌘/Ctrl+C.
  - Returns booleans or state updates so callers can show success/warning banners, and includes optional `toast` message text to surface via `st.toast`.
- Ensure payloads are safely escaped for embedding in HTML/JS (e.g., `json.dumps`). Strip leading/trailing whitespace but preserve internal newlines.
- Document the helper with a module docstring and type hints so it can be reused later (Phase 8 workspace copy features).
  </action>
  <verify>1. Run `python -m streamlit run frontend/ui.py` and open any claim's retrieval instructions. 2. Click the rendered demo button to confirm plain text copies into your OS clipboard; disable clipboard permissions in the browser dev tools to ensure the fallback textarea appears.</verify>
  <done>Clipboard helper exists, writes payloads to the system clipboard when available, and exposes an accessible fallback block when not.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate helper into retrieval instructions panel</name>
  <files>frontend/claim_queue.py</files>
  <action>
- Update `render_retrieval_instructions` to import and call `render_copy_to_clipboard` when `manual_instructions` exists:
  - Disable the button when no manual instructions are present; otherwise, call the helper with a unique key per reference and toast text such as "Copied retrieval instructions for {callout}".
  - After a successful copy, surface `st.toast`/`st.success` containing a truncated snippet (first ~120 chars) so reviewers see what was captured.
- Replace the placeholder `st.session_state["retrieval_clipboard"]` logic with a lightweight info panel: display the manual instructions inside `st.code(..., language="text")` or `st.text_area` with copyable styling, and include a caption noting the last copied timestamp/state.
- Ensure the rest of the dossier layout (canonical citation, DOI link, metadata sources) remains intact.
  </action>
  <verify>1. `python -m streamlit run frontend/ui.py` → navigate to a claim → expand "Retrieval instructions". 2. Click "Copy instructions" and paste into a text editor to confirm the payload matches the rendered manual instructions and that the UI shows the confirmation banner.</verify>
  <done>Retrieval instructions panel now provides a real copy workflow with confirmation messaging and a visible fallback block, satisfying EVD-01 truth #2.</done>
</task>

</tasks>

<verification>
- Manual run of `python -m streamlit run frontend/ui.py` confirms clipboard success + fallback.
- Visual inspection shows copied snippet and inline instructions block.
</verification>

<success_criteria>
- Copy button writes manual instructions to system clipboard (or exposes fallback) and displays confirmation containing the payload text.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-attachment/04-04-SUMMARY.md`
</output>
