---
phase: 04-evidence-attachment
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/attachment_store.py
  - backend/attachment_pipeline.py
  - backend/schemas.py
  - frontend/attachment_queue.py
  - frontend/ui.py
  - frontend/assets/attachment_panel.css
  - scripts/attachment_smoke.py
  - tests/test_attachment_store.py
  - tests/test_attachment_pipeline.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Attachment lifecycle exposes the required pending → converting → parsing → matched progression in both backend responses and the queue panel."
    - "Queue summary chip/counts reflect converting and matched statuses so reviewers can see progress at a glance."
    - "Timeline entries record converting/matched events so the verifier can confirm transitions end-to-end."
  artifacts:
    - path: "backend/attachment_store.py"
      provides: "Status constants + helpers (`mark_converting`, `mark_matched`) that persist lifecycle events."
    - path: "backend/attachment_pipeline.py"
      provides: "Processing loop that calls the new lifecycle helpers before/after parsing."
    - path: "frontend/attachment_queue.py"
      provides: "Client state that maps backend statuses to queue items, records summary counts, and surfaces converting/matched states."
    - path: "frontend/ui.py"
      provides: "Queue + claim UI that renders the new labels/pills and summary chip."
  key_links:
    - from: "backend/attachment_pipeline.py"
      to: "backend/attachment_store.py"
      via: "mark_converting/mark_matched calls"
      pattern: "mark_(converting|matched)"
    - from: "frontend/attachment_queue.py"
      to: "backend/main.py"
      via: "status polling"
      pattern: "/claims/.+/attachments/status"
---

<objective>
Emit and display the full attachment status lifecycle so reviewers see converting/parsing/matched phases per verifier gap #2.

Purpose: Restore roadmap expectation that queue items move through four explicit stages and that the UI summarizes them.
Output: Backend lifecycle helpers + updated Streamlit queue/summary visuals showing pending → converting → parsing → matched.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-attachment/04-CONTEXT.md
@.planning/phases/04-evidence-attachment/04-VERIFICATION.md
@backend/attachment_store.py
@backend/attachment_pipeline.py
@frontend/attachment_queue.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend lifecycle to convert → parse → match</name>
  <files>backend/attachment_store.py, backend/attachment_pipeline.py, backend/schemas.py, scripts/attachment_smoke.py, tests/test_attachment_store.py, tests/test_attachment_pipeline.py</files>
  <action>
- Introduce `STATUS_CONVERTING` and `STATUS_MATCHED` constants in `backend/attachment_store.py` (keep `STATUS_READY` as an alias for backward compatibility) plus helper functions `mark_converting` and `mark_matched` that log timeline events and set timestamps.
- Update `attachment_pipeline.process_attachment` so it calls `mark_converting` immediately after dequeuing an attachment (before any heavy work) and replaces `mark_ready` with `mark_matched` once artifacts are saved. Ensure retries reset status to `converting` when work restarts.
- Adjust `backend/schemas.AttachmentStatus` docstrings/comments if needed and ensure serialization still includes `status` + timeline entries for the new phases.
- Update `tests/test_attachment_store.py` and `tests/test_attachment_pipeline.py` to assert the new status flow (pending → converting → parsing → matched) and timeline entries. Refresh `scripts/attachment_smoke.py` so it prints/terminates when it hits `matched` instead of `ready`.
  </action>
  <verify>pytest tests/test_attachment_store.py tests/test_attachment_pipeline.py</verify>
  <done>Backend status responses and smoke test output show converting + matched events between pending/parsing and final readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Surface converting/matched states in the Streamlit queue</name>
  <files>frontend/attachment_queue.py, frontend/ui.py, frontend/assets/attachment_panel.css</files>
  <action>
- Update queue state helpers to:
  - Set newly uploaded items to `pending`, flip to `converting` once `_upload_to_backend` succeeds, and map backend `status` values (including `matched`, `error`) without collapsing them back to `ready`.
  - Keep a running summary across `pending`, `converting`, `parsing`, `matched`, and `error`, and include the counts in `summarize_chip_label`.
- Refresh UI rendering:
  - Expand `ATTACHMENT_STATUS_LABELS` + CSS classes to add descriptive text/icons for converting/matched (e.g., spinner vs. check badge) and ensure claim pills & queue items style them distinctly.
  - Show timeline entries for converting/matched events and display the new summary counts at the top of the queue panel.
- Ensure `render_claim_card` uses the updated status labels/pills and that the queue collapse/resume logic considers `converting` items as active work.
  </action>
  <verify>1. `python scripts/attachment_smoke.py --doc demo --claim demo:1a --file fixtures/sample.pdf` prints the four lifecycle states in order. 2. `python -m streamlit run frontend/ui.py` → drop a PDF → observe queue pills and summary chip reflecting pending → converting → parsing → matched.</verify>
  <done>Streamlit queue and summary chip visibly reflect converting/matched statuses with updated styling and counts, closing lifecycle gap.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_attachment_store.py tests/test_attachment_pipeline.py`
- `python scripts/attachment_smoke.py …` shows the four statuses in order and exits only after `matched`
</verification>

<success_criteria>
- Backend emits converting + matched timeline entries and UI shows those statuses (with counts) for every queue item.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-attachment/04-05-SUMMARY.md`
</output>
