---
phase: 05-evidence-matching-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/evidence_matching/__init__.py
  - backend/evidence_matching/types.py
  - backend/evidence_matching/loaders.py
  - backend/evidence_matching/deterministic_matcher.py
  - backend/evidence_matching/pipeline.py
  - backend/evidence_matching/serializers.py
  - backend/settings.py
  - tests/test_evidence_matching_pipeline.py
autonomous: true
must_haves:
  truths:
    - "Attachment-backed sentences are normalized into deterministic windows for a claim."
    - "Evidence pipeline produces ranked EvidenceCandidate objects with combined FAISS/BM25/NLI scoring."
    - "Each candidate carries page/section/bbox metadata so UI can deep-link into PDFs."
  artifacts:
    - path: "backend/evidence_matching/types.py"
      provides: "EvidenceCandidate, CandidateSpan, RankScores data structures"
      contains: "class EvidenceCandidate"
    - path: "backend/evidence_matching/deterministic_matcher.py"
      provides: "BM25-based cite span seeding utilities"
      contains: "def seed_windows"
    - path: "backend/evidence_matching/pipeline.py"
      provides: "Retrieve → rerank → NLI cascade returning EvidenceCandidate list"
      contains: "class EvidencePipeline"
    - path: "tests/test_evidence_matching_pipeline.py"
      provides: "Regression tests for loader + pipeline scoring output"
      contains: "def test_pipeline_orders_candidates"
  key_links:
    - from: "backend/evidence_matching/pipeline.py"
      to: "backend/attachment_store.py"
      via: "load_sentences_for_attachment"
      pattern: "from\\s+backend\\.attachment_store"
    - from: "backend/evidence_matching/pipeline.py"
      to: "backend.hybrid"
      via: "HybridPipeline"
      pattern: "from\\s+backend\\.hybrid"
    - from: "backend/evidence_matching/serializers.py"
      to: "backend/settings.py"
      via: "settings.EVIDENCE_MAX_CANDIDATES"
      pattern: "settings\\.EVIDENCE"
---

<objective>
Build the backend evidence-matching package: convert parsed attachment windows into deterministic cite-span seeds, run the retrieve → rerank cascade, and emit fully annotated EvidenceCandidate objects ready for downstream APIs.

Purpose: Foundation for EVD-04/05 so later plans can focus on orchestration and UI without reimplementing retrieval details.
Output: `backend/evidence_matching` package with loaders, deterministic matcher, pipeline orchestrator, and regression tests describing expected scoring metadata.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-evidence-matching-ranking/05-CONTEXT.md
@.planning/phases/05-evidence-matching-ranking/05-RESEARCH.md
@backend/hybrid.py
@backend/attachment_store.py
@backend/utils.py
</context>

<tasks>

<task type="auto">
  <name>Define evidence candidate data structures and loaders</name>
  <files>backend/evidence_matching/__init__.py, backend/evidence_matching/types.py, backend/evidence_matching/loaders.py, tests/test_evidence_matching_pipeline.py</files>
  <action>
    - Create `backend/evidence_matching` package with `__all__` exports for loaders, matcher, pipeline.
    - Implement `types.py` containing `EvidenceCandidate`, `CandidateSpan`, `RankScores`, and helper builders that enforce label enums, provenance chips, bbox lists, and serialization to dict for FastAPI responses.
    - Build `loaders.py` utilities that read persisted attachment artifacts (`tei.json`, `sentences.ndjson`) to produce normalized window records (text, tokens, tei_ids, page/section, bbox) grouped per claim + attachment and filtered by settings like `EVIDENCE_MAX_WINDOWS`.
    - Wire those loaders to `backend.attachment_store.load_sentences_for_attachment` so deterministic windows reuse the centralized attachment sentence cache rather than re-reading blobs, ensuring attachment metadata stays in sync.
    - Add pytest fixtures validating loader behavior (skips empty sentences, normalizes page placeholders, preserves embeddings if stored) inside `tests/test_evidence_matching_pipeline.py`.
  <verify>pytest tests/test_evidence_matching_pipeline.py -k loader</verify>
  <done>Loader returns deterministic window dicts with location metadata for any attachment that reached `matched` state.</done>
</task>

<task type="auto">
  <name>Implement deterministic cite-span seeding</name>
  <files>backend/evidence_matching/deterministic_matcher.py, backend/evidence_matching/types.py, backend/settings.py, tests/test_evidence_matching_pipeline.py</files>
  <action>
    - Write `deterministic_matcher.py` functions that tokenize claim text + candidate windows via spaCy blank `en`, build `rank_bm25.BM25Okapi`, and return ordered seeds with BM25 scores, heuristic provenance (cited vs heuristic), and badges for ambiguous attachments.
    - Surface matcher tunables (`EVIDENCE_BM25_MIN_SCORE`, `EVIDENCE_SEED_LIMIT`) via `backend/settings.py` with sane defaults and env overrides.
    - Extend unit tests to cover scoring cutoff, provenance tagging, and reproducibility by rerunning the matcher twice on the same corpus.
  <verify>pytest tests/test_evidence_matching_pipeline.py -k matcher</verify>
  <done>Given a claim + attachment windows, the matcher consistently returns the same top-N seed ids with provenance metadata and respects configured score/limit thresholds.</done>
</task>

<task type="auto">
  <name>Compose retrieve → rerank → NLI pipeline</name>
  <files>backend/evidence_matching/pipeline.py, backend/evidence_matching/serializers.py, backend/hybrid.py, backend/utils.py, tests/test_evidence_matching_pipeline.py</files>
  <action>
    - Implement `EvidencePipeline` class that accepts `claim_id`, `claim_text`, and seeded windows, embeds with SentenceTransformers, queries FAISS with τ-filter + cap, reranks via optional SBERT/BM25 fusion and ColBERT (respecting settings + research recommendations), then calls `backend.nli.assess` to assign entail/contrad/neutral labels.
    - Instantiate and delegate retrieval to `backend.hybrid.HybridPipeline` so existing FAISS/BM25 plumbing is reused instead of hand-built index wiring, only adding deterministic seed filtering and rerank scoring on top.
    - Capture per-stage scores inside a `RankScores` object, attach token salience or confidence sparkline payloads when ColBERT returns token_scores, and compute combined rank position for later delta comparisons.
    - Provide serializer helpers to transform pipeline output into JSON-friendly dicts (text snippet, highlight offsets, metadata, badges) trimming snippets to ~600 chars with ellipsis and storing multi-span counts, while enforcing caps such as `settings.EVIDENCE_MAX_CANDIDATES` before returning payloads.
    - Expand pytest coverage to assert ordering by combined score, label distribution counts, and inclusion of page/section metadata plus bbox coordinates derived from loaders.
  <verify>pytest tests/test_evidence_matching_pipeline.py -k pipeline</verify>
  <done>Pipeline returns a list of EvidenceCandidate objects sorted by combined rank, each containing text, location metadata, entail/contrad label, and stage scores ready for API exposure.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_evidence_matching_pipeline.py` to cover loaders, matcher, and pipeline scoring plus serialization.</verification>

<success_criteria>
- Evidence pipeline builds deterministically from attachment artifacts and yields EvidenceCandidate objects with scoring metadata and PDF location info.
- Loader + matcher tests ensure regressions are caught when attachment schemas or tokenizer logic change.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-01-SUMMARY.md`.</output>
