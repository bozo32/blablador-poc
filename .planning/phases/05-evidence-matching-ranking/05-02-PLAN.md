---
phase: 05-evidence-matching-ranking
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/evidence_matching/__init__.py
  - backend/evidence_matching/store.py
  - backend/evidence_matching/service.py
  - backend/attachment_pipeline.py
  - backend/schemas.py
  - backend/main.py
  - backend/settings.py
  - tests/test_evidence_matching_api.py
autonomous: true
must_haves:
  truths:
    - "Claim-focused ranking runs are persisted with run metadata, score deltas, and up to five historical snapshots."
    - "FastAPI exposes evidence endpoints for listing candidates, triggering reruns, and downloading rank history."
    - "Attachment lifecycle changes automatically enqueue evidence reruns so claims always have fresh candidates."
  artifacts:
    - path: "backend/evidence_matching/store.py"
      provides: "Disk-backed persistence for ranking runs + history"
      contains: "class EvidenceRunStore"
    - path: "backend/evidence_matching/service.py"
      provides: "High-level orchestration for auto/manual reruns and filtering"
      contains: "class EvidenceMatchingService"
    - path: "backend/main.py"
      provides: "FastAPI routes under /claims/{claim_id}/evidence*"
      contains: "@app.get(\"/claims/{claim_id}/evidence\")"
    - path: "tests/test_evidence_matching_api.py"
      provides: "API + service tests covering rerun queueing and pagination"
      contains: "def test_list_evidence_candidates"
  key_links:
    - from: "backend/evidence_matching/service.py"
      to: "backend/evidence_matching/pipeline.py"
      via: "EvidencePipeline.run"
      pattern: "from\\s+backend\\.evidence_matching\\.pipeline"
    - from: "backend/attachment_pipeline.py"
      to: "backend.evidence_matching.service"
      via: "trigger_auto_rerun"
      pattern: "EvidenceMatchingService"
    - from: "backend/main.py"
      to: "backend/schemas.py"
      via: "EvidenceCandidateResponse"
      pattern: "schemas\\.Evidence"
---

<objective>
Expose the evidence pipeline through stable backend services: persist ranked runs, keep rerun history with delta metadata, automatically refresh when attachments change, and surface everything through FastAPI endpoints for the frontend.

Purpose: Fulfills EVD-05 by turning raw candidates into consumable APIs with rerun orchestration.
Output: Evidence matching store/service modules + new `/claims/{claim_id}/evidence` endpoints backed by regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-evidence-matching-ranking/05-CONTEXT.md
@.planning/phases/05-evidence-matching-ranking/05-RESEARCH.md
@backend/main.py
@backend/attachment_pipeline.py
@backend/schemas.py
@backend/settings.py
</context>

<tasks>

<task type="auto">
  <name>Persist ranking runs and history</name>
  <files>backend/evidence_matching/__init__.py, backend/evidence_matching/store.py, backend/settings.py, tests/test_evidence_matching_api.py</files>
  <action>
    - Implement `EvidenceRunStore` that writes the latest candidate list plus rank metadata to `settings.EVIDENCE_STORE_DIR/claim_id/run.json`, retaining up to five historical runs per claim and computing delta badges (rank change, demotion reason, diversity note).
    - Add settings knobs for store directory, history depth, and rerun timeout; expose through `.env` via `backend/settings.py` with defaults aligned to research recommendations.
    - Update package exports so other modules can import `EvidenceRunStore` directly.
    - Create pytest fixtures ensuring the store writes/reads JSON atomically, caps history length, and produces delta metadata for downstream rationale sidebar.
  <verify>pytest tests/test_evidence_matching_api.py -k store</verify>
  <done>Store layer can persist and retrieve ranked runs with computed delta metadata and trims history beyond the configured depth.</done>
</task>

<task type="auto">
  <name>Implement evidence matching service + auto reruns</name>
  <files>backend/evidence_matching/service.py, backend/evidence_matching/pipeline.py, backend/attachment_pipeline.py, tests/test_evidence_matching_api.py</files>
  <action>
    - Build `EvidenceMatchingService` that wraps the pipeline + store, exposing methods to `ensure_current_run(claim_id)`, `list_candidates(claim_id, filters, offset, limit)`, `request_rerun(claim_id, note, advanced_settings)` with queueing + status tracking, and `get_history(claim_id)`.
    - Integrate service hooks into `attachment_pipeline.process_attachment` (after successful `mark_matched`) so attachments tied to a claim automatically kick off `ensure_current_run` in a background thread; guard so accept/reject actions remain locked while reruns in progress.
    - Cover service logic in tests using temporary directories + stub pipeline to verify queue serialization, concurrent reruns limit, and auto-trigger when attachments move into `matched` status.
  <verify>pytest tests/test_evidence_matching_api.py -k service</verify>
  <done>Service automatically refreshes evidence after attachments are parsed, enforces rerun queue semantics, and exposes list/history data sourced from the store.</done>
</task>

<task type="auto">
  <name>Add FastAPI evidence endpoints</name>
  <files>backend/schemas.py, backend/main.py, tests/test_evidence_matching_api.py</files>
  <action>
    - Extend `backend/schemas.py` with `EvidenceCandidatePayload`, `EvidenceListResponse`, `EvidenceHistoryResponse`, and `EvidenceRerunRequest` models that mirror the new service output (labels, chips, rank delta, sparkline data, pagination state).
    - Register FastAPI routes:
      * `GET /claims/{claim_id}/evidence` with `label`, `offset`, `limit`, `include_neutral`, `pinned_only` query params.
      * `POST /claims/{claim_id}/evidence/rerun` accepting optional `note` + advanced settings payload; returns rerun job state.
      * `GET /claims/{claim_id}/evidence/history` returning the last N runs plus download links/JSON embed.
    - Ensure endpoints surface rerun lock status and attach run timestamps for the UI progress bar; add tests hitting each route to verify 200s, pagination, rerun queueing, and that reruns trigger new store entries.
  <verify>pytest tests/test_evidence_matching_api.py -k api</verify>
  <done>API routes deliver ranked candidates, allow manual reruns with notes, and expose run history/JSON export, all backed by regression tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_evidence_matching_api.py` to exercise store, service, and FastAPI routes.</verification>

<success_criteria>
- Evidence runs persist on disk with rank history and delta metadata, and reruns can be triggered automatically or manually.
- FastAPI routes expose candidates + history with pagination/filtering, enabling the frontend to consume ranked data.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-02-SUMMARY.md`.</output>
