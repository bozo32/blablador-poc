---
phase: 05-evidence-matching-ranking
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/evidence_api.py
  - frontend/evidence_store.py
  - frontend/claim_queue.py
  - frontend/ui.py
  - tests/test_evidence_store.py
autonomous: true
must_haves:
  truths:
    - "Streamlit session state tracks evidence candidates, filters, pinned cards, and load-more counts per claim."
    - "Selecting a claim or completing attachment parsing automatically fetches ranked evidence and rerun status."
    - "Manual rerun + load more actions enforce concurrency limits (max two concurrent requests) and surface toast feedback."
  artifacts:
    - path: "frontend/evidence_api.py"
      provides: "HTTP client helpers for evidence list, rerun, history"
      contains: "def list_evidence"
    - path: "frontend/evidence_store.py"
      provides: "State machine for candidates, filters, rerun queue"
      contains: "class EvidenceStore"
    - path: "tests/test_evidence_store.py"
      provides: "Unit tests for store transitions and concurrency guard"
      contains: "def test_request_rerun_queue_limit"
    - path: "frontend/ui.py"
      provides: "Hooks that synchronize claim selection + attachments with evidence store"
      contains: "evidence_store.sync_for_claim"
  key_links:
    - from: "frontend/ui.py"
      to: "frontend.evidence_store"
      via: "ensure_claim_state"
      pattern: "from\\s+frontend\\.evidence_store"
    - from: "frontend/evidence_store.py"
      to: "frontend.evidence_api"
      via: "list_evidence"
      pattern: "import\\s+frontend\\.evidence_api"
    - from: "frontend/claim_queue.py"
      to: "frontend.evidence_store"
      via: "record_timeline_event"
      pattern: "EvidenceStore"
---

<objective>
Wire the frontend to the new evidence endpoints: local API client, deterministic session-state store (filters, pins, load more), and hooks that keep the candidate list synced when claim focus or attachment status changes. Provide rerun + load-more actions with concurrency guards per requirements.

Purpose: Bridges backend APIs to the UI layer so later plans can focus on rich card rendering.
Output: `frontend/evidence_api.py`, `frontend/evidence_store.py`, regression tests, and `frontend/ui.py` hooks that fetch/update evidence data.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-evidence-matching-ranking/05-CONTEXT.md
@.planning/phases/05-evidence-matching-ranking/05-RESEARCH.md
@frontend/ui.py
@frontend/attachment_queue.py
@frontend/claim_queue.py
</context>

<tasks>

<task type="auto">
  <name>Create evidence API client helpers</name>
  <files>frontend/evidence_api.py, frontend/ui.py</files>
  <action>
    - Build `frontend/evidence_api.py` with typed helper functions (`list_evidence`, `request_rerun`, `fetch_history`, `export_rank_json`, `jump_to_pdf_span`) that wrap the new FastAPI routes, reuse session `api_url` + auth headers, and catch `requests` errors to surface Streamlit toasts.
    - Include concurrency guards that block more than two simultaneous `list_evidence` requests per claim and annotate responses with remaining counts to drive load-more behavior.
    - Update `frontend/ui.py` imports to pull from the new module and centralize API error messaging.
  <verify>pytest tests/test_evidence_store.py -k api_stub</verify>
  <done>API helpers return parsed dicts with pagination + rerun metadata and enforce the two-request limit described in the context.</done>
</task>

<task type="auto">
  <name>Implement evidence session store</name>
  <files>frontend/evidence_store.py, tests/test_evidence_store.py</files>
  <action>
    - Create `EvidenceStore` class (plus module-level helper functions) managing candidate cache per claim: pinned ids, filter chips, load-more counts, rerun queue states, history payload, and keyboard focus order.
    - Provide methods `ensure_claim_state`, `sync_for_claim`, `apply_filter`, `load_more`, `queue_rerun`, `update_from_payload`, each emitting structured dicts for UI consumption.
    - Encode concurrency rules (max two outstanding fetches; rerun requests serialized) and include instrumentation hooks for toasts/logs.
    - Write unit tests that simulate filter toggles, load-more persistence, rerun queue exhaustion, and auto-refresh when attachments mark stale.
  <verify>pytest tests/test_evidence_store.py -k store</verify>
  <done>Store exposes deterministic state transitions with tests covering filter/pin updates, load-more persistence, and rerun queue enforcement.</done>
</task>

<task type="auto">
  <name>Hook store into claim + attachment flows</name>
  <files>frontend/claim_queue.py, frontend/ui.py, frontend/evidence_store.py, tests/test_evidence_store.py</files>
  <action>
    - Update `frontend/claim_queue.py` timeline + selection helpers to notify the evidence store when claim focus changes or attachments transition to `matched`, so auto-runs fire without manual refresh.
    - Extend `frontend/ui.py` to initialize the store, call `sync_for_claim` when the reviewer selects a claim, surface rerun/load-more buttons with spinner state, and display placeholder skeleton rows until cards land (final visuals deferred to Plan 05-04).
    - Add tests for the store helper functions that mark claims stale and ensure `sync_for_claim` respects load-more persistence when returning reviewers reopen a claim.
  <verify>pytest tests/test_evidence_store.py -k sync</verify>
  <done>Evidence store stays in sync with claim focus + attachment lifecycle, and UI wiring provides working rerun + load-more controls with concurrency feedback.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_evidence_store.py` to validate API helpers, store transitions, and sync hooks.</verification>

<success_criteria>
- Evidence data automatically loads for the active claim, reruns can be triggered with queue awareness, and state persists (filters, load more) per claim.
- Two-request concurrency rule is enforced with tests guarding regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-03-SUMMARY.md`.</output>
