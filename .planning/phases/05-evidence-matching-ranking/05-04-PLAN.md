---
phase: 05-evidence-matching-ranking
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - frontend/components/evidence_card.py
  - frontend/components/rationale_sidebar.py
  - frontend/assets/evidence.css
  - frontend/ui.py
  - tests/test_evidence_components.py
autonomous: true
must_haves:
  truths:
    - "Evidence cards display snippets (~600 chars), badges, confidence sparkline, and inline actions (accept, reject, open PDF, pin, share) with keyboard navigation."
    - "Rationale sidebar stays synced with hovered/pinned cards, exposes rank scores/deltas, and offers JSON export + advanced-mode toggle."
    - "UI renders filter chips, load-more batches of five, global entail/contrad progress bar, and toasts for rerun completion/stale rankings."
  artifacts:
    - path: "frontend/components/evidence_card.py"
      provides: "Reusable Streamlit component rendering cards + inline actions"
      contains: "class EvidenceCardRenderer"
    - path: "frontend/components/rationale_sidebar.py"
      provides: "Sidebar component showing ranking rationale + downloads"
      contains: "def render_rationale_sidebar"
    - path: "frontend/assets/evidence.css"
      provides: "Shared styles for cards, progress bar, keyboard focus"
      contains: ".evidence-card"
    - path: "tests/test_evidence_components.py"
      provides: "Helper tests for snippet truncation, progress aggregation, filter chips"
      contains: "def test_build_progress_summary"
  key_links:
    - from: "frontend/ui.py"
      to: "frontend.components.evidence_card"
      via: "render_evidence_cards"
      pattern: "from\\s+frontend\\.components\\.evidence_card"
    - from: "frontend/ui.py"
      to: "frontend.components.rationale_sidebar"
      via: "render_rationale_sidebar"
      pattern: "from\\s+frontend\\.components\\.rationale_sidebar"
    - from: "frontend/components/evidence_card.py"
      to: "frontend/assets/evidence.css"
      via: "st.markdown"
      pattern: "evidence.css"
---

<objective>
Deliver the full evidence board experience: Streamlit components for the cards and rationale sidebar, progress bar + filter chips, advanced rerun controls, keyboard navigation, and inline actions that align with the Phase 5 decisions.

Purpose: Completes EVD-06 by presenting ranked evidence with all required affordances.
Output: Evidence card and rationale sidebar components, styling assets, UI integration, and helper tests verifying formatting/aggregation logic.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-evidence-matching-ranking/05-CONTEXT.md
@.planning/phases/05-evidence-matching-ranking/05-RESEARCH.md
@frontend/ui.py
@frontend/evidence_store.py
</context>

<tasks>

<task type="auto">
  <name>Build evidence card component with interactions</name>
  <files>frontend/components/evidence_card.py, frontend/assets/evidence.css, tests/test_evidence_components.py</files>
  <action>
    - Create `EvidenceCardRenderer` (Streamlit component or helper) that renders full-width cards: snippet with inline highlight spans, rank + entail/contrad chips, provenance badges, OCR badge, reviewer initials, multi-span badge, inline notes summary, and accept/reject/star/share/Open-in-PDF actions wired to callbacks from the store.
    - Implement keyboard navigation (arrow keys move focus, Enter triggers primary accept) using `st.keyboard` or custom JS embedded via `components.html`; ensure ARIA labels + focus outlines satisfy accessibility.
    - Add CSS in `frontend/assets/evidence.css` for entail vs contradict background tints, hover glow on “Open in PDF,” star pins, share icon, and inline confidence sparkline placeholder.
    - Write helper tests (`tests/test_evidence_components.py`) for snippet truncation logic, highlight merging, and keyboard focus ordering functions to prevent regressions.
  <verify>pytest tests/test_evidence_components.py -k card</verify>
  <done>Component renders cards with required badges/actions, keyboard navigation works, and helper tests cover truncation/highlight utilities.</done>
</task>

<task type="auto">
  <name>Create rationale sidebar and progress UI</name>
  <files>frontend/components/rationale_sidebar.py, frontend/assets/evidence.css, tests/test_evidence_components.py</files>
  <action>
    - Build `render_rationale_sidebar` that syncs with hovered/pinned cards (via EvidenceStore signals) to show rank rationale bullets, diversity/demotion notes, entail/contrad explanation, token-level sparkline, confidence waterfall chart, advanced-mode toggle with toast notification, history download button, and JSON export control.
    - Add helper functions for aggregating progress bar counts (entail vs contradict vs neutral) and generating rank-delta arrow badges; include tests to cover these calculations.
    - Extend `evidence.css` with sidebar + progress bar styles, ensuring responsive layout and mobile fallbacks (sidebar collapses below md breakpoint with toggle chip).
  <verify>pytest tests/test_evidence_components.py -k rationale</verify>
  <done>Sidebar displays synced rationale data with downloads + advanced toggle, and progress summary helpers compute entail/contrad counts for the global bar.</done>
</task>

<task type="auto">
  <name>Integrate evidence board into UI</name>
  <files>frontend/ui.py, frontend/evidence_store.py, frontend/components/evidence_card.py, frontend/components/rationale_sidebar.py, tests/test_evidence_components.py</files>
  <action>
    - Update `frontend/ui.py` evidence panel to use the new components: render claim reminder with unsaved badge, filter chips (entail/contrad/neutral + neutral shortcut), pinned section, neutral grouping accordion, progress bar, load-more button (batches of five), and rerun banner/toasts (stale ranking prompt, rerun logs link).
    - Wire inline actions (accept/reject, pin, share deep link) to EvidenceStore callbacks; disable accept/reject when rerun is running, show tooltip-only queued status, and enforce maximum of two concurrent load-more requests.
    - Surface manual rerun controls in overflow menu with advanced parameter sheet (Modal) and mention optional note entry; ensure rerun completion toast includes log link + JSON export path from backend history payload.
    - Add helper tests verifying progress bar builder + filter chip config (pure functions) while UI integration is manually exercised.
  <verify>pytest tests/test_evidence_components.py -k progress</verify>
  <done>UI renders the full evidence board with cards, sidebar, progress bar, filters, rerun controls, and respecting concurrency/locking behavior.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_evidence_components.py` to validate helper utilities powering cards, sidebar, and progress bar logic.</verification>

<success_criteria>
- Evidence board meets the Phase 5 interaction spec (cards, sidebar, filters, load more, rerun UX) with reusable components and styling assets.
- Helper tests cover formatting/aggregation logic so future UI tweaks don’t break critical behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-04-SUMMARY.md`.</output>
