---
phase: 05-evidence-matching-ranking
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schemas.py
  - backend/main.py
  - backend/attachment_store.py
  - backend/attachment_pipeline.py
  - backend/evidence_matching/service.py
  - tests/test_attachment_pipeline.py
  - tests/test_evidence_matching_api.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Backend accepts claim_text on the very first evidence request or rerun so /claims/{id}/evidence never returns 409 for new claims."
    - "Attachment lifecycle auto-reruns always enqueue claim_text so deterministic windows can be built without user intervention."
    - "Regression tests prove ensure_current_run succeeds for a fresh claim and attachment-triggered reruns execute end-to-end."
  artifacts:
    - path: "backend/schemas.py"
      provides: "AttachmentCreateRequest.claim_text for passing claim text over HTTP"
    - path: "backend/attachment_store.py"
      provides: "Persists claim_text alongside attachment metadata"
    - path: "backend/evidence_matching/service.py"
      provides: "claim_text resolution + auto rerun plumbing"
  key_links:
    - from: "backend/attachment_pipeline.py::process_attachment"
      to: "backend/evidence_matching/service.py::trigger_auto_rerun"
      via: "claim_text argument when auto-enqueuing reruns"
    - from: "backend/main.py::create_claim_attachment"
      to: "backend/attachment_store.create_attachment"
      via: "AttachmentCreateRequest.claim_text payload"
    - from: "backend/main.py::list_claim_evidence"
      to: "EvidenceMatchingService.ensure_current_run"
      via: "claim_text parameter forwarded from request"
---

<objective>
Patch the backend so claim text is preserved from the first attachment upload through every evidence rerun. This removes the 409 conflict raised during initial syncing and allows attachment-driven auto reruns to execute without manual seeding.

Purpose: Close verification gaps 6 & 8 by ensuring claim_text is always available inside the evidence service.
Output: Updated schemas, attachment plumbing, and regression tests that guarantee first-run success.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evidence-matching-ranking/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend attachment schema + storage with claim text</name>
  <files>backend/schemas.py, backend/main.py, backend/attachment_store.py, tests/test_attachment_pipeline.py</files>
  <action>
    - Add an optional <code>claim_text</code> field to <code>AttachmentCreateRequest</code> plus the associated Pydantic response models so claim text can travel over the attachment API.
    - Update <code>create_claim_attachment</code> in <code>backend/main.py</code> to accept the new field and pass it through to <code>attachment_store.create_attachment</code>.
    - Persist <code>claim_text</code> in attachment metadata (ensure it survives public/private views) so later pipeline stages can read it; include the value when returning attachment status to the UI to keep state consistent.
    - Cover the new path with/adjust existing attachment tests (e.g., <code>tests/test_attachment_pipeline.py</code> fixtures) to assert the metadata now contains <code>claim_text</code>.
  </action>
  <verify><code>uv run pytest tests/test_attachment_pipeline.py</code></verify>
  <done>Attachment API accepts claim_text, stores it on disk, and tests confirm the metadata survives round-trips.</done>
</task>

<task type="auto">
  <name>Task 2: Pipe claim text into rerun orchestration + harden ensure_current_run</name>
  <files>backend/attachment_pipeline.py, backend/evidence_matching/service.py, backend/main.py, tests/test_evidence_matching_api.py, tests/test_attachment_pipeline.py</files>
  <action>
    - In <code>attachment_pipeline.process_attachment</code>, load the stored <code>claim_text</code> and pass it when calling <code>evidence_service.trigger_auto_rerun</code>; fall back to existing metadata when absent but prefer the persisted value.
    - Ensure <code>trigger_auto_rerun</code> and <code>request_rerun</code> propagate claim_text, and update <code>ensure_current_run</code>/<code>_resolve_claim_text</code> so they reuse the last stored text before raising. Document why ValueError still occurs when text is missing after all fallbacks.
    - Surface clearer 409 messaging via <code>HTTPException</code> only when no text exists anywhere.
    - Expand API tests to demonstrate a fresh claim succeeds when the UI supplies claim_text, and that attachment-triggered auto reruns execute successfully with the stored text.
  </action>
  <verify><code>uv run pytest tests/test_evidence_matching_api.py</code></verify>
  <done>Evidence endpoints and auto reruns never fail for lack of claim_text, with regression tests confirming both manual and attachment-triggered flows.</done>
</task>

</tasks>

<verification>
- Run <code>uv run pytest tests/test_attachment_pipeline.py tests/test_evidence_matching_api.py</code> to prove both attachment and rerun flows pass.
- Manually inspect <code>tmp/attachments/*/metadata.json</code> if needed to confirm claim_text is persisted alongside each attachment.
</verification>

<success_criteria>
- First evidence sync for a new claim returns 200 when claim_text is provided.
- Attachment lifecycle events enqueue reruns that succeed without additional user action.
- Updated regression tests cover the new paths and prevent regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-05-SUMMARY.md`.
</output>
