---
phase: 08-workspace-fast-path-review-ux
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - frontend/ui.py
  - frontend/state_keys.py
  - frontend/assets/workspace.css
autonomous: true

must_haves:
  truths:
    - "Workspace layout is vertically dense: no large header, settings live behind a gear/drawer"
    - "Workspace uses a consistent 3-pane layout: left (source bin + nav), center (Document/Review), right (citing spans collector)"
  artifacts:
    - path: frontend/assets/workspace.css
      provides: "Dense-mode styles + 3-pane layout CSS helpers"
    - path: frontend/ui.py
      provides: "3-pane shell and settings drawer wiring"
  key_links:
    - from: frontend/ui.py
      to: backend/main.py
      via: "GET /ingest/{doc_id}/body"
      pattern: "/ingest/\{doc_id\}/body"
---

<objective>
Refactor the Streamlit UI into the Phase 8 3-pane workspace shell and add dense mode + settings drawer without changing core backend flows.

Purpose: Establish the new workspace frame (UX-02) before rewiring citations and source ingest.
Output: A 3-pane layout with a gear drawer and a persistent dense-mode toggle.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-workspace-fast-path-review-ux/08-CONTEXT.md

@frontend/ui.py
@frontend/state_keys.py
@frontend/assets/judgment.css
@frontend/assets/evidence.css
</context>

<tasks>

<task type="auto">
  <name>Add workspace CSS + state keys for dense mode and drawer</name>
  <files>frontend/assets/workspace.css, frontend/state_keys.py</files>
  <action>
1) Create `frontend/assets/workspace.css` with:
- A compact baseline for headings/section spacing.
- A `dense` variant (toggle-driven) that reduces vertical padding and font sizes slightly.
- Minimal helper classes for left/center/right pane headers (no huge page title).

2) Update `frontend/state_keys.py` to add canonical keys for:
- `workspace_dense_mode` (bool)
- `workspace_settings_open` (bool)
- `workspace_active_tab` ("Document" | "Review")

Keep naming consistent with existing `state_keys` usage.
  </action>
  <verify>pytest -q</verify>
  <done>CSS file exists and new state keys are referenced (no dead keys); unit tests still pass.</done>
</task>

<task type="auto">
  <name>Refactor Streamlit UI into 3 panes + gear drawer settings</name>
  <files>frontend/ui.py</files>
  <action>
Update `frontend/ui.py` to implement the Phase 8 workspace shell:

- Remove the large `st.title(...)` header.
- Create a 3-column layout (left/center/right).
- Center uses tabs (or a radio) with exactly: `Document` and `Review`.
- Right pane becomes the citing-span collector area (for now, reuse existing chase queue component as a placeholder panel).
- Settings are moved behind a gear control (use Streamlit-native `st.popover` or `st.sidebar`-like drawer, but the trigger must be a gear icon and not a full-width section).
- Add a sticky dense-mode toggle (persisted in session state using the new key).

Do NOT yet remove existing ingestion/evidence flows; re-host them inside the new panes:
- Keep ingestion/navigation controls in left pane.
- Keep document body rendering in center Document tab.
- Keep chasing/evidence/judgment workflows in center Review tab.

Ensure CSS is injected (read `frontend/assets/workspace.css` and embed via `<style>`).
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>App starts, shows 3 panes, settings are accessible via a gear control, and dense mode toggles spacing without breaking existing flows.</done>
</task>

</tasks>

<verification>
- `python -m py_compile frontend/ui.py`
- `pytest -q`
</verification>

<success_criteria>
- The workspace no longer relies on a large page header.
- Settings are tucked away behind a gear/drawer.
- The UI is ready for inline citation and source bin rewiring in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-fast-path-review-ux/08-04-SUMMARY.md`
</output>
