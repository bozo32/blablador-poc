---
phase: 08-workspace-fast-path-review-ux
plan: 08
type: execute
wave: 7
depends_on: [08-07]
files_modified:
  - backend/settings.py
  - backend/nli.py
  - backend/utils.py
  - frontend/ui.py
autonomous: true
user_setup:
  - service: huggingface
    why: "Optional remote inference for faster demos"
    env_vars:
      - name: HF_API_TOKEN
        source: "Hugging Face account -> Access Tokens"

must_haves:
  truths:
    - "A single toggle switches HF-powered steps to remote inference where supported"
    - "When enabled and token present, NLI calls can use HF Inference API instead of local transformers"
  artifacts:
    - path: backend/nli.py
      provides: "Remote inference implementation for NLI when enabled"
    - path: backend/settings.py
      provides: "HF remote inference toggle + token wiring"
    - path: frontend/ui.py
      provides: "UI toggle that propagates to backend"
  key_links:
    - from: backend/nli.py
      to: Hugging Face Inference API
      via: "requests"
      pattern: "api-inference\\.huggingface\\.co"
---

<objective>
Add optional Hugging Face Inference API mode (ML-02) for NLI so demos can run without local heavy inference.

Purpose: Provide a simple performance toggle for POC/demo scenarios.
Output: A backend switch for NLI remote calls + UI toggle; secrets remain local.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/settings.py
@backend/nli.py
@backend/utils.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Add settings and UI toggle for HF remote inference</name>
  <files>backend/settings.py, frontend/ui.py</files>
  <action>
1) Add settings in `backend/settings.py`:
- `HF_REMOTE_INFERENCE: bool` (default false)
- `HF_API_TOKEN: str` (default "")

2) Add a small toggle in the settings drawer in `frontend/ui.py`:
- Label: "Use HF Inference API (demo)"
- When enabled, ensure evidence rerun requests include `advanced_settings={"hf_remote": true}` (and/or profile override) so backend can decide per run.

Do not print tokens; do not add tokens to logs.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>Toggle exists and propagates intent to backend rerun requests without exposing secrets.</done>
</task>

<task type="auto">
  <name>Implement HF Inference API-backed NLI path with safe fallback</name>
  <files>backend/nli.py, backend/utils.py</files>
  <action>
Update `backend/nli.py` so NLI assessment can run via Hugging Face Inference API when enabled.

Constraints:
- Use `requests` (already in environment) to call the HF Inference endpoint.
- Only switch when BOTH are true:
  - the run’s `advanced_settings.hf_remote` (or global `settings.HF_REMOTE_INFERENCE`) is enabled
  - `HF_API_TOKEN` is present
- On any remote error (timeout, 401, 5xx, malformed response), fall back to the existing local path and log a warning without leaking token.

Implementation detail (minimum viable):
- Support the existing NLI model name setting by mapping it to a HF model id string; if unknown, use the current default NLI model string.
- Return labels/scores in the same shape the rest of the pipeline expects.

If embeddings/coref steps are also HF-powered in this codebase, do NOT change them in this plan; ML-02 minimum is NLI.
  </action>
  <verify>pytest -q</verify>
  <done>With HF remote enabled and a valid token, NLI can use remote inference; without token or on failure, local inference still works and tests pass.</done>
</task>

</tasks>

<verification>
- `pytest -q`
- Manual: enable toggle + run a rerun; check logs show remote path used (without printing token)
</verification>

<success_criteria>
- HF remote inference can be enabled for NLI without breaking local mode.
- Secrets remain local (`HF_API_TOKEN` only via env).
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-fast-path-review-ux/08-08-SUMMARY.md`
</output>
