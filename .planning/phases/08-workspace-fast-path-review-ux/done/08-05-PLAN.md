---
phase: 08-workspace-fast-path-review-ux
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - frontend/ui.py
  - frontend/components/chase_queue.py
autonomous: true

must_haves:
  truths:
    - "In-text citation interactions are inline: citations are clickable in the document text"
    - "End-of-block citation button rows are removed"
    - "Clicking an inline citation adds/activates that citing span in the right pane collector"
  artifacts:
    - path: frontend/ui.py
      provides: "Inline citation links and removal of per-paragraph citation button rows"
    - path: frontend/components/chase_queue.py
      provides: "Right pane collector supports activation without reordering"
  key_links:
    - from: frontend/ui.py
      to: query params
      via: "href ?doc=...&cite=...&target=..."
      pattern: "\\?doc=.*&cite="
---

<objective>
Make citation interactions inline by turning citation chips into clickable links and removing the per-paragraph citation button rows.

Purpose: Meet UX-01 and establish the citing-span collector behavior.
Output: Inline citation links that drive right-pane collection and center selection.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-workspace-fast-path-review-ux/08-CONTEXT.md

@frontend/ui.py
@frontend/components/chase_queue.py
</context>

<tasks>

<task type="auto">
  <name>Render inline citation chips as hyperlinks (no button rows)</name>
  <files>frontend/ui.py</files>
  <action>
Update the Document tab rendering so citations are clickable inline:

- Replace the current inline citation rendering that produces `<span class="citation-chip">...` with `<a class="citation-chip citation-chip-link" href="...">...`.
- Reuse existing query param flow (`?doc=...&cite=...&target=...`) so that a click triggers the existing `param_cite` handling and adds the entry to the followed/collector list.
- Keep selected styling (selected chip visually distinct).

Remove end-of-block citation selection UI:
- Delete the entire `if paragraph_citations: ... st.columns ... st.button(...)` block so there are no per-paragraph citation button rows.

Styling updates:
- Adjust the citation CSS (in the existing citation style injector) so the hyperlink feels like an inline underlined citation (underline + subtle hover), while still supporting validated/outcome chip classes.

Do NOT add popups/toasts for clicks.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>Document text shows inline-clickable citations and no longer renders paragraph-level citation button rows.</done>
</task>

<task type="auto">
  <name>Ensure right-pane collector activates without breaking document order</name>
  <files>frontend/components/chase_queue.py</files>
  <action>
Update `frontend/components/chase_queue.py` (and only that component file) so that:
- Activating/opening an item does not reorder the list.
- The list remains in document order (sorted by `citation_index`, then `target_id`).

If the component currently mutates order on open, change it to rely purely on the ordered input list.

This must support the Phase 8 decision: collector is ordered and stable.
  </action>
  <verify>python -m py_compile frontend/components/chase_queue.py</verify>
  <done>Collector behavior is stable: clicks activate items without reordering.</done>
</task>

</tasks>

<verification>
- `python -m py_compile frontend/ui.py`
- Manual smoke: `streamlit run frontend/ui.py` and click citations in Document; verify right pane adds items.
</verification>

<success_criteria>
- Inline citations are the primary interaction surface.
- Right pane serves as the citing-span collector driven by inline citation clicks.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-fast-path-review-ux/08-05-SUMMARY.md`
</output>
