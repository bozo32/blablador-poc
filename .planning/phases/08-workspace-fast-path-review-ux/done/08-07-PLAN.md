---
phase: 08-workspace-fast-path-review-ux
plan: 07
type: execute
wave: 6
depends_on: [08-06]
files_modified:
  - backend/settings.py
  - backend/schemas.py
  - backend/evidence_matching/service.py
  - frontend/ui.py
autonomous: true

must_haves:
  truths:
    - "UI exposes a small set of named execution profiles (Fast/Local, Best/Local)"
    - "Backend uses the selected profile to route retrieval/reranking (not Streamlit-only branching)"
    - "At least one profile re-enables the ColBERT reranker path"
  artifacts:
    - path: backend/settings.py
      provides: "Execution profile definitions and defaults"
    - path: backend/evidence_matching/service.py
      provides: "Profile-aware evidence reruns"
    - path: frontend/ui.py
      provides: "Profile selector that is sent to backend"
  key_links:
    - from: frontend/ui.py
      to: backend/main.py
      via: "Evidence rerun request advanced_settings/profile"
      pattern: "evidence/rerun"
---

<objective>
Add named execution profiles and wire them through the backend evidence rerun path so the backend (not Streamlit) owns retrieval/reranking configuration.

Purpose: Deliver ML-01 with minimal UI surface and deterministic backend routing.
Output: Profile selector + profile-aware evidence reruns (including ColBERT option).
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@backend/settings.py
@backend/evidence_matching/service.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Define execution profiles and expose them in settings + API models</name>
  <files>backend/settings.py, backend/schemas.py</files>
  <action>
1) In `backend/settings.py`, define a small profile registry (hardcoded dict is fine) with names like:
- `Fast/Local`
- `Best/Local`

Each profile must specify overrides relevant to evidence matching/reranking (at minimum):
- `pipeline_mode` (classic/hybrid)
- `reranker` selection that includes a ColBERT-enabled profile

2) In `backend/schemas.py`, ensure the evidence rerun request can carry the selected profile name.
- Use `EvidenceRerunRequest.advanced_settings` (existing) to carry `{"profile": "Fast/Local"}`; do not add a new top-level field unless unavoidable.

Keep defaults backward compatible when no profile is provided.
  </action>
  <verify>pytest -q</verify>
  <done>Profiles exist server-side and requests can carry a profile selection without breaking validation.</done>
</task>

<task type="auto">
  <name>Apply profile selection in backend evidence reruns + add UI selector</name>
  <files>backend/evidence_matching/service.py, frontend/ui.py</files>
  <action>
1) Update `backend/evidence_matching/service.py` so that when a rerun job is executed:
- If `advanced_settings.profile` is present, apply the profile’s overrides for the duration of that run.
- Implementation must not mutate global settings permanently; use a temporary settings object or a shallow override passed down to pipeline construction.

2) Update `frontend/ui.py`:
- Add a compact profile selector in settings drawer.
- Ensure `Request rerun` and auto reruns include `advanced_settings={"profile": selected_profile}`.

Do NOT add new UI tabs. Keep this as a small, named set (no raw JSON editing for profiles).
  </action>
  <verify>pytest -q</verify>
  <done>Selecting a profile changes backend rerun behavior and includes a ColBERT-enabled option.</done>
</task>

</tasks>

<verification>
- `pytest -q`
- Manual smoke: select different profile and request rerun; confirm rerun metadata includes the profile in run history.
</verification>

<success_criteria>
- Profiles are named and selectable.
- Backend uses the selected profile, and one profile explicitly enables ColBERT reranking.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-fast-path-review-ux/08-07-SUMMARY.md`
</output>
