---
phase: 09-claim-graph-consensus-viz
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - backend/schemas.py
  - backend/judgment_store.py
  - backend/main.py
  - tests/test_judgment_store.py
autonomous: true

must_haves:
  truths:
    - "A local 'Current user' scopes saved judgments so multiple reviewers do not overwrite each other"
    - "Exports always include reviewer_uid"
    - "Legacy single-judgment files remain readable (treated as reviewer_uid=default)"
  artifacts:
    - path: backend/judgment_store.py
      provides: "Per-(claim_id, reviewer_uid) judgment persistence + exports"
    - path: backend/main.py
      provides: "Reviewer-aware judgment endpoints"
    - path: tests/test_judgment_store.py
      provides: "Regression coverage for multi-user judgment storage"
  key_links:
    - from: backend/main.py
      to: backend/judgment_store.py
      via: "GET/PUT /claims/{claim_id}/judgment"
      pattern: "reviewer_uid"
---

<objective>
Make claim judgments multi-user by adding `reviewer_uid` to judgment payloads and storing judgments per reviewer, while keeping legacy judgment files readable.

Purpose: Consensus and compare mode require attributable, parallel judgments.
Output: Reviewer-aware judgment APIs + on-disk storage + exports.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-claim-graph-consensus-viz/09-CONTEXT.md

@backend/judgment_store.py
@backend/schemas.py
@backend/main.py

@tests/
</context>

<tasks>

<task type="auto">
  <name>Add reviewer_uid to judgment request/response schemas</name>
  <files>backend/schemas.py</files>
  <action>
1) Extend `JudgmentUpsertRequest` to accept an optional `reviewer_uid` (string). Default to `"default"` when missing.

2) Extend `JudgmentPayload` to include `reviewer_uid` so the API always returns it.

3) Add a small response schema for listing all reviewers for a claim (e.g., `JudgmentByReviewerResponse` with `judgments: list[JudgmentPayload]`).
  </action>
  <verify>python -m py_compile backend/schemas.py</verify>
  <done>Judgment schemas can represent and validate reviewer-scoped judgments.</done>
</task>

<task type="auto">
  <name>Persist judgments per (claim_id, reviewer_uid) with backward compatibility</name>
  <files>backend/judgment_store.py, tests/test_judgment_store.py</files>
  <action>
1) Update `JudgmentStore` storage layout to avoid overwrites:
   - Write one file per (claim_id, reviewer_uid).
   - Include `reviewer_uid` in the stored JSON.

2) Backward compatibility:
   - If the legacy file path (current format) exists for a claim_id, treat it as `reviewer_uid="default"`.
   - Ensure reads for reviewer_uid="default" still return legacy content when no reviewer-specific file exists.

3) Update `list_all`, `list_filtered`, and export helpers to include reviewer_uid in every row.

4) Add pytest coverage in `tests/test_judgment_store.py`:
   - two different reviewer_uids can upsert the same claim_id without overwriting
   - legacy file is still readable
   - exports include reviewer_uid
  </action>
  <verify>pytest -q</verify>
  <done>JudgmentStore reads/writes are reviewer-scoped and legacy files continue to load.</done>
</task>

<task type="auto">
  <name>Expose reviewer-aware judgment endpoints</name>
  <files>backend/main.py</files>
  <action>
1) Update existing endpoints:
   - `GET /claims/{claim_id}/judgment` accepts `reviewer_uid` query param (default "default").
   - `PUT /claims/{claim_id}/judgment` stores the judgment for that reviewer_uid.

2) Add endpoint to fetch all reviewers for a claim, used by disagreement UI:
   - `GET /claims/{claim_id}/judgments` (or similar) returning `JudgmentByReviewerResponse`.

3) Keep `/judgments` and `/judgments/export` working, but ensure returned rows contain reviewer_uid.
  </action>
  <verify>pytest -q</verify>
  <done>API can retrieve a single reviewer’s judgment or all reviewers’ judgments for a claim.</done>
</task>

</tasks>

<verification>
- `pytest -q`
- Quick manual: call `PUT /claims/{claim_id}/judgment?reviewer_uid=Alice` and `...=Bob` and confirm `GET /claims/{claim_id}/judgments` returns 2 entries.
</verification>

<success_criteria>
- Multi-user judgments are stored per reviewer and never overwrite each other.
- Export payloads include reviewer_uid in JSON and CSV.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-02-SUMMARY.md`
</output>
