---
phase: 09-claim-graph-consensus-viz
plan: 05
type: execute
wave: 4
depends_on: [09-03, 09-04]
files_modified:
  - environment.yml
  - frontend/graph_api.py
  - frontend/components/claim_graph_panel.py
  - frontend/ui.py
  - frontend/assets/workspace.css
autonomous: true

must_haves:
  truths:
    - "Claim graph is inspectable interactively (zoom/pan; click node/edge updates a right-side panel)"
    - "Edge inspection shows pro vs con votes with reviewer attribution"
    - "Graph renders provenance layers (external_search dotted) and consensus at-a-glance (badge/label)"
  artifacts:
    - path: frontend/components/claim_graph_panel.py
      provides: "Cytoscape-based graph renderer wrapper for Streamlit"
    - path: frontend/graph_api.py
      provides: "Client helpers for claim graph endpoints"
    - path: frontend/ui.py
      provides: "Graph tab integrating claim graph + inspection panel"
  key_links:
    - from: frontend/ui.py
      to: backend/main.py
      via: "GET /graph/claim-subgraph + PUT /graph/edge/{edge_id}/vote"
      pattern: "claim-subgraph|edge.*vote"
---

<objective>
Implement the Streamlit Graph tab for claim consensus using a Cytoscape-based Streamlit component, including node/edge inspection and edge voting.

Purpose: Make disagreement visible and explorable with smooth pan/zoom and attributed annotations.
Output: Graph tab with interactive renderer + inspection panel wired to backend claim-graph APIs.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-claim-graph-consensus-viz/09-CONTEXT.md

@frontend/ui.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Add Cytoscape Streamlit component dependency</name>
  <files>environment.yml</files>
  <action>
1) Add a Cytoscape-based Streamlit component dependency via pip (preferred: `streamlit-agraph`).
   - This satisfies the Cytoscape.js-in-Streamlit requirement without introducing a custom Node build.

2) Ensure the dependency is added under the existing `pip:` list.
  </action>
  <verify>python -c "import streamlit"</verify>
  <done>`environment.yml` includes the graph component dependency for Cytoscape rendering.</done>
</task>

<task type="auto">
  <name>Create frontend graph_api client and claim_graph_panel renderer</name>
  <files>frontend/graph_api.py, frontend/components/claim_graph_panel.py</files>
  <action>
1) Create `frontend/graph_api.py` with request helpers:
   - `get_claim_subgraph(center_claim_id, hops, edge_cap, min_votes, sources)`
   - `get_edge_votes(edge_id)`
   - `put_edge_vote(edge_id, reviewer_uid, verdict, confidence, comment)`
   - `create_claim_link(source_claim_id, target_claim_id, reviewer_uid)`
   - `delete_claim_link(edge_id, reviewer_uid)`
   - `get_claim_candidates(claim_id, limit)`

2) Create `frontend/components/claim_graph_panel.py`:
   - Map backend graph payload to the component’s node/edge types.
   - Encode consensus counts as an edge label or badge-friendly field (e.g., "3/1").
   - Return selection (node_id or edge_id) back to Streamlit so the UI can render the inspection panel.
  </action>
  <verify>python -m py_compile frontend/graph_api.py</verify>
  <done>Frontend can fetch and render a claim graph and receive user selection events.</done>
</task>

<task type="auto">
  <name>Replace Graph tab with claim graph + inspection panel (MVP)</name>
  <files>frontend/ui.py, frontend/assets/workspace.css</files>
  <action>
1) In `frontend/ui.py`, replace the current Graph tab (citation graphviz) with a claim graph view:
   - Controls: hops (1-3), edge cap per node, min_votes, provenance toggles (auto/manual/external_search).
   - Load behavior: center on current claim when possible; otherwise show instructions.

2) Node inspection (right-side panel):
   - show claim text + provenance (doc_id, citation_index, sentence_id)
   - show candidate edges list (ordered best-match first) with buttons to add edge (materialize)

3) Edge inspection (right-side panel):
   - two columns: supporters (green) and contradictors (red) with reviewer_uid + comment
   - neutrals/uncertain collapsed
   - voting UI for active reviewer (support/contradict/neutral/uncertain + optional comment)

4) Styling:
   - dotted external_search edges
   - thickness scaling by n_total (or vote volume displayed alongside label)
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>Graph tab renders an interactive claim graph and clicking nodes/edges opens an inspection panel with votes and actions.</done>
</task>

</tasks>

<verification>
- `python -m py_compile frontend/ui.py`
- Manual: load any project with claims/edges; click an edge, submit a vote as active reviewer, and confirm aggregates update after reload.
</verification>

<success_criteria>
- Claim graph is navigable (pan/zoom) and supports inspection + voting.
- Provenance styling is visible (external_search dotted) and consensus is visible at a glance.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-05-SUMMARY.md`
</output>
