---
phase: 09-claim-graph-consensus-viz
plan: 07
type: execute
wave: 6
depends_on: [09-06]
files_modified:
  - environment.yml
  - frontend/state_keys.py
  - frontend/ui.py
  - frontend/components/claim_graph_panel.py
  - frontend/components/cytoscape_panel.py
  - frontend/components/cytoscape_component/*
  - data/poc_graph.json
  - README_poc.md
autonomous: false

must_haves:
  truths:
    - "Surfing tab uses Cytoscape.js (not streamlit-agraph) and supports pan/zoom + selection"
    - "Graph-as-architecture POC dataset renders in-app with canonical-only + layer toggles"
    - "Inspector explains canonical vs plural, coordination vs adjudication, and heat"
  artifacts:
    - path: data/poc_graph.json
      provides: "In-repo demo dataset with plural segmentation + disagreement"
    - path: frontend/components/cytoscape_panel.py
      provides: "Streamlit wrapper around Cytoscape.js component returning selection {type,id}"
    - path: README_poc.md
      provides: "8-12 line narrative for the demo"
  key_links:
    - from: frontend/ui.py
      to: data/poc_graph.json
      via: "POC mode graph loader"
      pattern: "poc_graph"
---

<objective>
Replace the current Streamlit Graph renderer with a Cytoscape.js component and ship a small in-repo "graph as architecture" POC in the center pane Surfing tab.

Purpose: Make plurality (segmentation) and disagreement (heat) visible and inspectable, while keeping stable anchors explicit.
Output: Cytoscape-based Surfing view + POC dataset + narrative README.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-claim-graph-consensus-viz/09-07-SPEC.md
@.planning/phases/09-claim-graph-consensus-viz/09-07-RESEARCH.md
@frontend/ui.py
@frontend/components/claim_graph_panel.py
</context>

<tasks>

<task type="auto">
  <name>Rename center pane tabs to Reading / Chasing / Surfing</name>
  <files>frontend/state_keys.py, frontend/ui.py</files>
  <action>
1) Update `frontend/state_keys.py`:
   - Document -> Reading
   - Review -> Chasing
   - Graph -> Surfing

2) Ensure logic keys are unchanged (only labels), so session state + routing remain stable.
  </action>
  <verify>python -m py_compile frontend/state_keys.py frontend/ui.py</verify>
  <done>Center tabs read Reading/Chasing/Surfing and existing flows still work.</done>
</task>

<task type="auto">
  <name>Ship the POC dataset + README narrative</name>
  <files>data/poc_graph.json, README_poc.md</files>
  <action>
1) Create `data/poc_graph.json` per spec (09-07-SPEC.md):
   - 3 Works, 1 CiteAnchor, 2 Users, divergent ClaimAtoms, 2+ ClaimAnchors, EvidenceSpans, Assertions.
   - Include at least one high-heat ClaimAnchor (mixed relation types).

2) Create `README_poc.md` (8-12 lines) explaining the demo narrative:
   - anchors are stable
   - plurality begins at segmentation
   - heat is attention routing, not truth
  </action>
  <verify>python -c "import json; json.load(open('data/poc_graph.json'))"</verify>
  <done>Dataset loads and README tells the intended story concisely.</done>
</task>

<task type="auto">
  <name>Implement Cytoscape.js Streamlit component + Python wrapper</name>
  <files>environment.yml, frontend/components/cytoscape_panel.py, frontend/components/cytoscape_component/*</files>
  <action>
1) Add the minimal dependency/build strategy for a custom Streamlit component:
   - Prefer a **no-build** component (static `index.html` + pinned CDN assets).
   - Keep the Python<->JS contract tiny (elements in, `{type,id}` out) to avoid deep Streamlit wiring.

2) Implement `frontend/components/cytoscape_panel.py` exposing a function like:
   - `render(elements, *, style, layout, height, key) -> selection_dict`
   - Must return selection as `{type: 'node'|'edge', id: '...'}` to match the existing inspector contract.

3) Implement a component frontend that:
    - renders Cytoscape.js
    - installs click listeners and returns the selected node/edge id
    - supports `fit` / focusing on a set of node ids
  </action>
  <verify>python -m py_compile frontend/components/cytoscape_panel.py</verify>
  <done>Cytoscape renders and selection events reach Python reliably.</done>
</task>

<task type="auto">
  <name>Integrate POC mode into Surfing tab with inspector + heat</name>
  <files>frontend/ui.py, frontend/components/claim_graph_panel.py</files>
  <action>
1) Add a Surfing tab sub-toggle:
   - Data source: `POC dataset` vs `Live project`.
   - Default to POC while developing.

2) In POC mode:
   - left panel lists CiteAnchors
   - center panel renders Cytoscape graph focused on selected CiteAnchor subgraph
   - right panel shows Architecture Inspector per selected node type

3) Implement layer toggles + canonical-only view.

4) Implement heat computation and apply visual encoding on ClaimAnchor nodes.

5) Inspector must include the Architecture/Governance/Heat notes in plain language.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>POC mode demonstrates stable anchors, plural segmentation, and disagreement heat in a single focused workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cytoscape Surfing view + Graph-as-Architecture POC dataset</what-built>
  <how-to-verify>
1) Start backend + Streamlit.
2) Open any document, then switch to Surfing.
3) Select `POC dataset` mode.
4) Pick the only CiteAnchor in the left list.
5) Confirm the graph shows:
   - Work + CiteAnchor nodes (stable anchors)
   - ClaimAtoms for u1 and u2 (plural contributions)
   - ClaimAnchors (coordination points)
   - EvidenceSpans and Assertions
6) Toggle `Canonical only` and confirm the view becomes clean but uninformative.
7) Select a high-heat ClaimAnchor and confirm the inspector:
   - shows mixed relation types and the heat explanation
   - explicitly states "not truth" / "coordination" / "no adjudication".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m py_compile frontend/ui.py`
- Manual checkpoint above
</verification>

<success_criteria>
- Surfing tab is Cytoscape-based and selection-driven inspection works.
- POC dataset makes stable anchors vs plural segmentation vs heat visible.
- Canonical-only view demonstrates why plurality is necessary.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-07-SUMMARY.md`
</output>
