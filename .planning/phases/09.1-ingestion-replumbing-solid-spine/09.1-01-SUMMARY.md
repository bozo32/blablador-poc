---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 01
subsystem: infra
tags: [docker, docker-compose, fastapi, streamlit, uvicorn, pip-tools, pip-compile]

# Dependency graph
requires: []
provides:
  - "Docker Compose stack for FastAPI API + Streamlit UI"
  - "Reproducible, hashed Python dependency lockfile for container builds (numpy<2)"
  - "Dockerfiles for app-api (uvicorn) and app-ui (streamlit)"
affects: ["phase-09.1", "local-dev", "deployment", "container-runtime"]

# Tech tracking
tech-stack:
  added: ["pip-tools (pip-compile)"]
  patterns:
    - "pip-tools hashed lockfile for Docker builds (requirements/app.lock.txt)"
    - "docker/ directory contains per-service Dockerfiles"

key-files:
  created:
    - requirements/app.in
    - requirements/app.lock.txt
    - docker/app-api/Dockerfile
    - docker/app-ui/Dockerfile
    - docker-compose.yml
    - .dockerignore
  modified: []

key-decisions:
  - "Use pip-tools (pip-compile --generate-hashes) for deterministic container dependency installs"

patterns-established:
  - "Compose service names double as in-cluster hostnames (app-api, app-ui)"

# Metrics
duration: 23 min
completed: 2026-02-14
---

# Phase 9.1 Plan 01: Containerize API + UI with Compose Summary

**Docker/Compose stack boots FastAPI on :8000 and Streamlit on :8501 with reproducible, hashed pip lock installs (numpy<2).**

## Performance

- **Duration:** 23 min
- **Started:** 2026-02-14T11:46:02Z
- **Completed:** 2026-02-14T12:09:23Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- Added pinned Python dependency inputs + fully hashed lockfile for Docker builds.
- Built `app-api` and `app-ui` container images (Python 3.10) and wired them with Compose.
- Confirmed `http://localhost:8000/docs` returns 200 when running in containers.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add pinned Python requirements for containers** - `3c4a0d2` (chore)
2. **Task 2: Add Dockerfiles for API and UI (shared deps)** - `48ea90c` (feat)
3. **Task 3: Add docker-compose for app-api and app-ui** - `6f2e555` (feat)

Additional fix during verification:

- `503462f` (fix) - add missing `rank-bm25` dependency so `app-api` imports succeed in containers

## Files Created/Modified

- `requirements/app.in` - minimal top-level runtime deps for app-api/app-ui
- `requirements/app.lock.txt` - fully pinned, hashed dependency set (includes `numpy==1.26.4`)
- `docker/app-api/Dockerfile` - builds and runs `uvicorn backend.main:app` on port 8000
- `docker/app-ui/Dockerfile` - builds and runs Streamlit UI on port 8501
- `docker-compose.yml` - Compose wiring, ports, `BACKEND_URL`, and `./data` volume mounts
- `.dockerignore` - excludes `data/`, `.git/`, caches, and planning artifacts from build context

## Decisions Made

None - followed plan as specified.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing `rank-bm25` dependency for container runtime**

- **Found during:** Task 3 verification / overall compose smoke
- **Issue:** `app-api` container crashed on import with `ModuleNotFoundError: No module named 'rank_bm25'`
- **Fix:** Added `rank-bm25` to `requirements/app.in` and re-ran `pip-compile --generate-hashes`
- **Files modified:** `requirements/app.in`, `requirements/app.lock.txt`
- **Verification:** `docker compose up --build` + `curl -sf http://localhost:8000/docs` returns 200
- **Committed in:** `503462f`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Required for correctness (containers must boot); no scope creep.

## Issues Encountered

- `app-api` exited on startup due to missing dependency; fixed by locking `rank-bm25` and rebuilding.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Compose baseline is in place; ready for `09.1-02-PLAN.md` (add GROBID service + in-container config wiring).

---
*Phase: 09.1-ingestion-replumbing-solid-spine*
*Completed: 2026-02-14*
