---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 03
subsystem: infra
tags: [docker-compose, postgres, minio, s3, boto3, psycopg, migrations]

# Dependency graph
requires:
  - phase: 09.1-ingestion-replumbing-solid-spine
    provides: "09.1-02 containerized app-api/app-ui baseline"
provides:
  - "Compose services for Postgres + MinIO (with idempotent works bucket init)"
  - "Env-backed settings + minimal Postgres/S3 wrappers for the ingestion spine"
  - "Idempotent schema creation for works/attempts/jobs/artifacts tables"
affects: [09.1-ingestion-replumbing-solid-spine, ingestion, fallback-worker, model-gateway]

# Tech tracking
tech-stack:
  added: [psycopg, boto3]
  patterns:
    - "Idempotent startup migrations via backend.db.migrate.apply_migrations()"
    - "Thin internal wrappers for Postgres + S3 clients configured via backend.settings"

key-files:
  created:
    - backend/db/__init__.py
    - backend/db/migrate.py
    - backend/db/pg.py
    - backend/object_store/__init__.py
    - backend/object_store/s3.py
  modified:
    - docker-compose.yml
    - backend/settings.py
    - requirements/app.in
    - requirements/app.lock.txt

key-decisions:
  - "Use POSTGRES_DSN as the single Postgres config surface (avoid component sprawl)."
  - "Use boto3 with path-style addressing for MinIO compatibility."

patterns-established:
  - "Compose wires infra via POSTGRES_DSN + S3_* env vars; app code reads via AppSettings."

# Metrics
duration: 12 min
completed: 2026-02-14
---

# Phase 09.1 Plan 03: Postgres + MinIO Spine Summary

**Local Compose now includes Postgres metadata + MinIO artifacts, with app-level DSN/S3 settings, thin wrappers, and idempotent migrations for works/attempts/jobs/artifacts.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-02-14T12:23:30Z
- **Completed:** 2026-02-14T12:35:54Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments
- Added `postgres` and `minio` services to `docker-compose.yml`, plus a one-shot `minio-init` to create the `works` bucket.
- Wired `app-api` to Postgres + MinIO via env vars (`POSTGRES_DSN`, `S3_ENDPOINT_URL`, `S3_*`) while keeping `app-ui` minimal.
- Implemented `backend/db/*` and `backend/object_store/*` wrappers and an idempotent `apply_migrations()` for the ingestion spine tables.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add postgres + minio to docker-compose (idempotent bucket init)** - `6b82ac9` (feat)
2. **Task 2: Add settings + minimal DB + S3 client wrappers** - `109add1` (feat)

**Plan metadata:** (next commit)

## Files Created/Modified
- `docker-compose.yml` - Postgres + MinIO services, env wiring, and `minio-init` bucket creation.
- `backend/settings.py` - Adds `POSTGRES_DSN`, `S3_*`, and `INTERNAL_SERVICE_TOKEN` settings.
- `backend/db/migrate.py` - `apply_migrations()` creates works/attempts/jobs/artifacts tables + indexes idempotently.
- `backend/db/pg.py` - Minimal psycopg connect helper.
- `backend/object_store/s3.py` - Minimal boto3 put/get wrapper for the `works` bucket.
- `requirements/app.in` - Adds `psycopg[binary]` and `boto3`.
- `requirements/app.lock.txt` - Updated lockfile after pip-compile.

## Decisions Made
- Use a single `POSTGRES_DSN` setting (instead of separate host/user/pass/db settings) for simplicity.
- Use `boto3` configured for path-style S3 addressing to ensure MinIO compatibility.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-commit hooks required Black formatting + Flake8 fixes before Task 2 commit; resolved and committed cleanly.

## User Setup Required

None - local dev defaults are provided via Compose env vars.

## Next Phase Readiness

- Ready for `09.1-04-PLAN.md` (worker services can now rely on Postgres + S3 primitives).

---
*Phase: 09.1-ingestion-replumbing-solid-spine*
*Completed: 2026-02-14*
