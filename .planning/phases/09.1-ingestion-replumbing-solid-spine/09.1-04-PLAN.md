---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 04
type: execute
wave: 4
depends_on: ["09.1-03"]
files_modified:
  - backend/ingestion_store.py
  - backend/main.py
  - backend/spine/__init__.py
  - backend/spine/works.py
  - backend/spine/ids.py
  - backend/object_store/s3.py
autonomous: true

must_haves:
  truths:
    - "Uploading a PDF persists the PDF bytes to S3-compatible storage (MinIO) as well as local disk (transition period)."
    - "A Postgres `works` row exists for each ingested document (doc_id == work_id)."
    - "Re-uploading the same PDF is deduplicated by sha256 and does not create duplicate works."
  artifacts:
    - path: backend/spine/works.py
      provides: "create/get work rows in Postgres"
    - path: backend/ingestion_store.py
      provides: "ingest metadata includes S3 pointers (work_id/pdf_object_key)"
  key_links:
    - from: backend/main.py
      to: backend/spine/works.py
      via: "ingest upload endpoint"
      pattern: "create_"
---

<objective>
Introduce the V2 "Work" record and S3-backed PDF storage while keeping the legacy filesystem ingestion working.

Purpose: Establish stable identifiers + object-store artifacts so later extraction jobs can run remotely.
Output: Postgres `works` rows + MinIO/S3 PDF objects + backward-compatible ingestion metadata.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@backend/main.py
@backend/ingestion_store.py
@backend/object_store/s3.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spine helpers for work_id and work rows</name>
  <files>backend/spine/ids.py, backend/spine/works.py, backend/spine/__init__.py</files>
  <action>
Implement minimal spine helpers:
- `backend/spine/ids.py`: helpers for generating/stabilizing IDs; during migration, treat `doc_id == work_id` (same string).
- `backend/spine/works.py`: functions to `upsert_work_from_pdf(work_id, filename, sha256, size_bytes, pdf_object_key)` and `get_work(work_id)`.

Constraints:
- Use the Postgres wrapper from plan 09.1-03.
- Keep schema aligned with V2 contract (`works.work_id` text pk).
- Idempotent upsert by `work_id` and sha256.
  </action>
  <verify>python -c "from backend.spine.works import upsert_work_from_pdf; print('ok')"</verify>
  <done>Spine helper modules exist and import cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-write ingest upload to S3 and Postgres (keep filesystem)</name>
  <files>backend/ingestion_store.py, backend/main.py, backend/object_store/s3.py</files>
  <action>
Update ingestion upload flow so that when `POST /ingest` is called:
- Continue writing the legacy local copy to `data/ingestion/{doc_id}/source.pdf`.
- Additionally write the PDF to S3/MinIO under the V2 key layout:
  - Bucket: `works`
  - Object key: `pdf/{work_id}/{sha256}.pdf`
- Create/upsert a `works` row in Postgres with:
  - `work_id` (use the existing `doc_id`)
  - `filename`, `sha256`, `size_bytes`, `pdf_object_key`

Deduping rule:
- Preserve existing sha256 de-dupe behavior: re-uploading identical bytes returns the existing doc/work id.
- Ensure de-dupe also reuses the same S3 object key (sha256-based).

Metadata compatibility:
- Extend the existing JSON metadata (stored in `metadata.json`) with a `spine` object containing at minimum `{work_id, pdf_object_key}`.
- Do not rename existing keys used by the UI.
  </action>
  <verify>
  docker compose up --build -d && curl -sf -F "file=@fixtures/sample.pdf" http://localhost:8000/ingest | python -c "import sys, json; p=json.load(sys.stdin); assert (p.get('document') or {}).get('id'); print((p.get('document') or {}).get('id'))"
  </verify>
  <done>
  New ingests create `works` rows + S3 objects while legacy filesystem ingestion remains functional.
  </done>
</task>

</tasks>

<verification>
- Uploading the same PDF twice returns the same `id` and does not create duplicate Postgres works.
- The MinIO bucket contains the uploaded PDF under the expected object key prefix.
</verification>

<success_criteria>
- A stable `work_id` and portable PDF storage exist without breaking the existing UI/API.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-04-SUMMARY.md`
</output>
