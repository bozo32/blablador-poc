---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 07
type: execute
wave: 7
depends_on: ["09.1-06"]
files_modified:
  - backend/db/migrate.py
  - backend/ingestion_store.py
  - backend/main.py
  - backend/ingest_pipeline.py
  - backend/spine/works.py
  - backend/spine/attempts.py
  - backend/spine/jobs.py
  - backend/spine/artifacts.py
  - backend/schemas.py
autonomous: true

must_haves:
  truths:
    - "All ingestion spine rows (works/attempts/jobs/artifacts) are scoped by project_id and created_by_user_id."
    - "API requests determine project_id (defaulted for POC) and propagate it through ingest/extract writes."
    - "Existing UI flows continue to work without requiring project selection (defaults apply)."
  artifacts:
    - path: backend/db/migrate.py
      provides: "idempotent schema changes for project/user scoping"
    - path: backend/main.py
      provides: "scope extraction and propagation for ingestion endpoints"
    - path: backend/spine/attempts.py
      provides: "attempt creation is project-scoped"
---

<objective>
Introduce project and user scoping as foundational plumbing for the V2 spine.

Purpose: Enable future multi-user, project-scoped wipes/exports, and isolate ingestion/extraction activity per project.
Output: Postgres spine tables include project_id + created_by_user_id, and API/pipeline propagates them by default.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@.planning/V2-PLANNING.md
@docker-compose.yml
@backend/db/migrate.py
@backend/main.py
@backend/ingest_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scope defaults + request extraction</name>
  <files>backend/main.py, backend/ingest_pipeline.py, backend/ingestion_store.py, backend/schemas.py</files>
  <action>
  - Define default `project_id` and `user_id` for the POC (env overridable).
  - In FastAPI ingest/extract endpoints, resolve `project_id` from request context (header or default) and propagate into spine helpers.
  - Ensure legacy ingestion metadata includes `project_id` (non-breaking, transitional).
  - Keep responses backward compatible for the current Streamlit UI.
  </action>
  <verify>python -c "from backend.settings import settings; print('ok')"</verify>
  <done>Endpoints determine a project_id and pass it through for new spine writes.</done>
</task>

<task type="auto">
  <name>Task 2: Add project_id + created_by_user_id to spine tables and helpers</name>
  <files>backend/db/migrate.py, backend/spine/works.py, backend/spine/attempts.py, backend/spine/jobs.py, backend/spine/artifacts.py</files>
  <action>
  - Extend migrations to add `project_id` and `created_by_user_id` columns to `works`, `attempts`, `jobs`, `artifacts`.
  - Backfill existing rows via DEFAULTs (idempotent).
  - Update spine helper queries/inserts to include `project_id` filters and write `created_by_user_id`.
  - Do not change the settings_hash contract (still derived from work_id/kind/settings_json).
  </action>
  <verify>
  docker compose up --build -d && docker compose exec -T postgres psql -U app -d app -c "\
    SELECT column_name FROM information_schema.columns\
     WHERE table_name='attempts' AND column_name IN ('project_id','created_by_user_id');"
  </verify>
  <done>Spine tables contain scope columns and new writes populate them.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification (scope populated)</name>
  <files>backend/main.py, backend/ingest_pipeline.py</files>
  <action>
  - Run a smoke ingest/extract.
  - Confirm Postgres attempts/jobs/artifacts rows for the work_id have non-empty `project_id` and `created_by_user_id`.
  </action>
  <verify>
  bash scripts/dev/up.sh && bash scripts/dev/smoke_ingest.sh fixtures/sample.pdf && \
    docker compose exec -T postgres psql -U app -d app -c "\
      SELECT project_id, created_by_user_id, count(*)\
        FROM attempts\
       GROUP BY project_id, created_by_user_id\
       ORDER BY count(*) DESC\
       LIMIT 5;"
  </verify>
  <done>New ingest/extract runs produce scoped spine rows and UI behavior is unchanged.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-07-SUMMARY.md`
</output>
