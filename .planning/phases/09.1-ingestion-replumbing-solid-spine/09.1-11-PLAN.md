---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 11
type: execute
wave: 11
depends_on: ["09.1-10"]
autonomous: true
files_modified:
  - backend/db/migrate.py
  - backend/spine/locators.py
  - backend/main.py

must_haves:
  truths:
    - "A stable locator primitive exists in Postgres, scoped by project_id and tied to document_version_id."
    - "API supports creating and retrieving locators without affecting existing UI flows."
    - "Locator payload can represent at least: page+bbox and quote anchors (JSON payload)."
  rollback:
    - "All changes are additive; disabling endpoints does not affect ingestion/extraction."
---

<objective>
Add stable addressing primitives (locators) so future features can reference evidence spans robustly across extraction re-runs and document versions.

This is entailment-ready scaffolding: we store locators now; later plans attach them to claims/evidence/edges and add multi-hop entailment navigation.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/V2-PLANNING.md
@.planning/V2-REPLUMBING-PLAN.md
@backend/db/migrate.py
@backend/main.py
@backend/spine/documents.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add locator table</name>
  <files>backend/db/migrate.py</files>
  <action>
  Add an idempotent `locators` table:
  - `locator_id` (pk)
  - `project_id` (scope)
  - `created_by_user_id`
  - `document_version_id` (FK to `document_versions`)
  - `type` (text)
  - `payload_json` (jsonb)
  - `created_at`
  </action>
  <verify>
  docker compose up --build -d && docker compose exec -T app-api python -c "from backend.db import apply_migrations; apply_migrations(); print('migrated')" && \
  docker compose exec -T postgres psql -U app -d app -c "\\dt locators"
  </verify>
  <done>Table exists in Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Implement locator helpers</name>
  <files>backend/spine/locators.py</files>
  <action>
  Implement:
  - `create_locator(project_id, created_by_user_id, document_version_id, type, payload_json)`
  - `get_locator(locator_id, project_id)`
  - `list_locators_for_document_version(project_id, document_version_id, limit)`
  </action>
  <verify>python -c "from backend.spine.locators import create_locator; print('ok')"</verify>
  <done>Helpers import cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add additive API endpoints</name>
  <files>backend/main.py</files>
  <action>
  Add project-scoped endpoints (use `X-Project-Id`, defaulted):
  - `POST /spine/locators`
  - `GET /spine/locators/{locator_id}`
  - `GET /spine/document-versions/{document_version_id}/locators`
  </action>
  <verify>
  python - <<'PY'
import json, subprocess

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

# Create a fresh ingest to guarantee document_version exists.
resp = sh('curl -4 -sf -F "file=@fixtures/sample.pdf" "http://127.0.0.1:8000/ingest?auto_process=false"')
assert doc_id

payload = {
  'document_version_id': doc_id,
  'type': 'pdf_page_bbox',
  'payload': {'page': 1, 'bbox': [0,0,10,10], 'quote': 'stub'}
}
loc = sh('curl -4 -sf -X POST http://127.0.0.1:8000/spine/locators -H "Content-Type: application/json" -d ' + json.dumps(payload))
locator_id = json.loads(loc)['locator_id']
assert locator_id

sh(f'curl -4 -sf http://127.0.0.1:8000/spine/locators/{locator_id} >/dev/null')
lst = sh(f'curl -4 -sf http://127.0.0.1:8000/spine/document-versions/{doc_id}/locators')
assert len((json.loads(lst).get('locators') or [])) >= 1
print('ok', locator_id)
PY
  </verify>
  <done>Endpoints create/read/list locators.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-11-SUMMARY.md`
</output>
