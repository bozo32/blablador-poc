---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 12
type: execute
wave: 12
depends_on: ["09.1-11"]
autonomous: true
files_modified:
  - backend/main.py
  - backend/ingest_pipeline.py
  - backend/spine/pdf_source.py
  - backend/object_store/s3.py

must_haves:
  truths:
    - "Primary extraction reads PDF bytes from S3 (MinIO) using spine pointers, not from data/ingestion/**/source.pdf."
    - "Legacy filesystem source.pdf remains optional fallback behind a flag."
  rollback:
    - "SPINE_PDF_SOURCE=legacy forces reading local source.pdf"
---

<objective>
Eliminate runtime dependence on local `data/ingestion/**/source.pdf` for extraction.

This is required for remote workers and for eventually removing filesystem ingestion as a runtime dependency.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/main.py
@backend/ingest_pipeline.py
@backend/object_store/s3.py
@backend/spine/ingest_view.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spine PDF source helper</name>
  <files>backend/spine/pdf_source.py</files>
  <action>
  Implement a helper that resolves a PDF for a given work/document id:
  - prefer S3 object key from spine (works/pdf_object_key or document_versions)
  - download bytes from S3 and write to a temp file path for grobid_client
  - return the temp path
  </action>
  <verify>python -c "from backend.spine.pdf_source import get_pdf_temp_path; print('ok')"</verify>
  <done>Helper imports and can write a temp PDF from S3 bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire extraction paths to use spine PDF source</name>
  <files>backend/main.py, backend/ingest_pipeline.py</files>
  <action>
  Replace `get_document_source_path(...)` usage in extraction with the new spine PDF source helper.
  Keep legacy local file fallback behind `SPINE_PDF_SOURCE=legacy`.
  </action>
  <verify>
  bash scripts/dev/up.sh && \
    python - <<'PY'
import json, subprocess
from pathlib import Path

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

resp = sh('curl -4 -sf -F "file=@fixtures/sample.pdf" "http://127.0.0.1:8000/ingest?auto_process=false"')
doc_id = (json.loads(resp).get('document') or {}).get('id')
assert doc_id

# Ensure this run does not depend on legacy local source.pdf.
legacy_source = Path('data/ingestion') / str(doc_id) / 'source.pdf'
if legacy_source.exists():
    legacy_source.unlink()

sh(f'curl -4 -sf -X POST http://127.0.0.1:8000/ingest/{doc_id}/extract >/dev/null')
print('ok', doc_id)
PY
  </verify>
  <done>Extraction succeeds even if local source.pdf is missing (spine S3 source).</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-12-SUMMARY.md`
</output>
