---
phase: 09.2-ingestion-automation-robustness-fallback-extraction
plan: 05
objective: "Add a separate OCR-capable worker (container) and implement hybrid per-page OCR policy with adaptive DPI."
depends_on:
  - plan: 04
provides:
  - "OCR-enabled fallback for scanned PDFs"
  - "Per-page provenance and quality stats"
---

# 09.2-05: Hybrid OCR Worker Service

## Work

- Add a new worker service/container dedicated to fallback + OCR.
- Policy (locked):
  - Prefer text layer; OCR only for low-coverage pages
  - DPI escalation: 150 -> 200 -> 300 on low confidence

## Verification

- Use a scanned-ish fixture.
- Confirm:
  - fallback completes asynchronously
  - quality flags: `fallback.ocr_used=true`, counts for OCR pages
  - output artifacts are stored the same way as non-OCR fallback
