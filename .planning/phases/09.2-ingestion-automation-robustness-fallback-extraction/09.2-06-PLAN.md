---
phase: 09.2-ingestion-automation-robustness-fallback-extraction
plan: 06
objective: "Orchestrate fallback attempts as jobs: auto-run on primary failure; manual force fallback; polling + cancellation."
depends_on:
  - plan: 05
provides:
  - "Idempotent job submission"
  - "Progress reporting and cancellation"
---

# 09.2-06: Orchestration (Jobs + Polling)

## Work

- Auto-run fallback when primary attempt fails (do not block UI thread).
- Add manual `POST /ingest/{id}/fallback-extract`.
- Add job polling endpoints and surface progress.
- Add cancel endpoint.

## Verification

- For the choker:
  - `POST /ingest/{id}/extract` fails/partials
  - fallback starts automatically
  - polling shows progress; completion persists artifacts
- For cancellation:
  - cancel transitions to a terminal state and leaves partial artifacts marked as such
