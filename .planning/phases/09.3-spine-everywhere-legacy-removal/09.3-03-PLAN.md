---
phase: 09.3-spine-everywhere-legacy-removal
plan: 03
objective: "Persist evidence runs + selections in the spine (history + artifacts) and remove dependence on data/evidence_* directories." 
---

# 09.3-03: Evidence Runs + Selections to Spine

## Work

- Store evidence run inputs/outputs in Postgres; large payloads as object-store artifacts.
- Store evidence selections in Postgres (or artifacts with pointers).

## Verification

- Trigger an evidence run; confirm history is visible.
- Select evidence; confirm persisted.
- Delete local `data/evidence_runs/**` and `data/evidence_selections/**` and repeat.
