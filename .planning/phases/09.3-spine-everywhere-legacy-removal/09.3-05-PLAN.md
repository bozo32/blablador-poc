---
phase: 09.3-spine-everywhere-legacy-removal
plan: 05
objective: "Decide and implement a graph-store strategy that does not require local sqlite files as durable state." 
---

# 09.3-05: Graph Stores Strategy

## Decision

Pick one:

1) Migrate graph stores to Postgres (durable, multi-host safe).
2) Treat graph stores as rebuildable caches derived from spine artifacts (durable source stays in Postgres/artifacts).

## Verification

- Start from empty `./data`.
- Ingest 1-2 PDFs, run extraction/resolution.
- Confirm graph-dependent UI features work (citation context / claim navigation) without requiring local `.db` files.
