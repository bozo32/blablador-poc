---
phase: 09.3-spine-everywhere-legacy-removal
plan: 06
objective: "Delete legacy filesystem stores and compatibility flags; spine is the only runtime." 
---

# 09.3-06: Delete Legacy Stores + Compat Flags

## Work

- Remove legacy filesystem stores and their call sites:
  - ingestion store remnants
  - attachment store remnants
  - evidence/judgment local directory stores
- Remove compatibility env vars/flags. Keep only what is necessary for configuring Postgres/S3 endpoints.

## Verification

- Fresh checkout + `docker compose up --build`.
- With empty `./data` directory:
  - ingest -> extract -> fallback (on failure) -> resolve -> attach -> evidence -> judge -> export
- Confirm `/dev/wipe` resets state reliably.
