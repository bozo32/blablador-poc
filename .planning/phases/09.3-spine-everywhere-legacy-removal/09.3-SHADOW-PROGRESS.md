# 09.3 Shadow Progress (Crash-Resumable)

This file is a running checkpoint log for Phase 09.3 work.

Goal (from `.planning/phases/09.3-spine-everywhere-legacy-removal/09.3-CONTEXT.md`):
the system runs without requiring local durable state under `./data/**`.

Conventions
- “Observed” means confirmed by reading code / migrations in this repo.
- “Verified” means executed in this session and recorded here.
- When resuming after a crash: start at **Next Actions**.

## Current Branch State (as of this checkpoint)

- Branch: `feature/09-1-ingestion-replumbing-solid-spine`
- Working tree: staged changes for 09.3 graph/spans Postgres persistence; a few unrelated unstaged edits exist (tooling + README typo).

## Active Work Log

- 2026-02-20: Resume after interruption.
  - Observed: `backend/main.py` is mid-refactor toward spine-only; legacy imports removed but several `_spine_reads_enabled()` / `_legacy_ingestion_enabled()` call sites remain and must be deleted.
  - Plan: fix obvious doc/tooling noise first; then remove remaining legacy branches from `backend/main.py` in small patches; then remove `backend/ingestion_store.py` + `backend/project_io.py` and legacy settings.

- 2026-02-20: Cleanup + spine-only main.
  - Fixed accidental typo in `README_poc.md`.
  - Reverted `.opencode/*` dependency bumps (unrelated).
  - `backend/main.py`: removed remaining legacy/dual branches and removed reliance on `INGESTION_DIR`/`ATTACHMENT_DIR`/`EVIDENCE_STORE_DIR` paths; `/dev/wipe` now only removes local ColBERT caches.
  - Deleted legacy filesystem modules/tests: `backend/ingestion_store.py`, `backend/project_io.py`, `backend/ingest_pipeline.py`, `tests/test_ingest_pipeline_fallback.py`.
  - Removed legacy AppSettings durable path fields from `backend/settings.py` and updated callers/tests.
  - Verified: `python -m compileall backend tests scripts` passes.
  - Verified: runtime smoke still OK after spine-only edits (`/docs`, `/dev/wipe`, `/spans/upsert`).
  - Verified: `docker compose exec -T app-api python -m compileall backend` passes.
  - Note: `pytest` is not installed in this environment (`python -m pytest` fails).

- 2026-02-20: Test env note.
  - Verified: tests pass under Conda env `blablador310` after installing missing deps there (`psycopg[binary]`, `boto3`) and making NLI imports lazy to avoid broken optional ML deps.
  - `conda run -n blablador310 python -m pytest -q` => 92 passed, 4 skipped.

- 2026-02-20: E2E corpus + 09.3 persistence smoke.
  - Verified: `bash scripts/dev/smoke_corpus.sh fixtures/corpus-09.2.txt` passes.
  - Verified: deterministic choker scenario (GROBID down): `bash scripts/dev/with_grobid_down.sh -- bash scripts/dev/smoke_corpus.sh fixtures/corpus-09.2.txt` passes and produces fallback extraction payload.
  - Verified: attachments are readable via API after normalizing `uploaded_at` for spine-backed records (`GET /attachments/{id}` no longer 500s).
  - Verified: evidence selection + judgment persist across `docker compose restart app-api`.

## Plan-by-Plan Status

### 09.3-01: Attachments to Spine

- Observed: `attachments`, `attachment_events`, `attachment_artifacts` exist in `backend/db/migrate.py`.
- Observed: attachment persistence uses Postgres + object store (see `backend/attachment_store.py`, `backend/attachment_pipeline.py`).
- Observed: attachment “donor reuse” no longer assumes local artifact files; it reuses object-store artifact keys (`backend/main.py`).
- Verified: not re-run in this session.

Commands to verify (docker compose recommended):
- `docker compose up -d`
- Upload a cited PDF via UI or API; confirm it parses; then delete local `data/attachments/**` and repeat.

### 09.3-02: Judgments + Exports to Spine

- Observed: `judgments` table exists in `backend/db/migrate.py`.
- Observed: judgments are Postgres-backed (see `backend/judgment_store.py`).
- Verified: not re-run in this session.

Commands to verify:
- Record a judgment, restart `app-api`, confirm persistence.
- Export via UI/API and confirm non-empty.

### 09.3-03: Evidence Runs + Selections to Spine

- Observed: `evidence_runs` + `evidence_selections` exist in `backend/db/migrate.py`.
- Observed: selections are Postgres-backed (see `backend/evidence_selection_store.py`).
- Verified: not re-run in this session.

Commands to verify:
- Trigger an evidence run; confirm run history and selection persistence; delete local `data/evidence_*` and repeat.

### 09.3-04: Project Metadata to Spine

- Observed: `project_meta` exists in `backend/db/migrate.py`.
- Observed: API uses spine meta when spine reads are enabled (`GET /project`, `PUT /project` in `backend/main.py`).
- Known legacy tail: `backend/project_io.py` still implements file-backed `data/project.json` and is used when spine reads are disabled.
- Verified: not re-run in this session.

Commands to verify:
- `GET /project`, update reviewers, restart, confirm persistence.
- Confirm `data/project.json` is not required in spine mode.

### 09.3-05: Graph Stores Strategy (No Required Local SQLite)

- In progress in this working tree.

Observed changes (staged):
- `backend/db/migrate.py`: adds Postgres tables for `graph_*` and `span_graph_*`.
- `backend/graph_store.py`: now Postgres-backed (nodes/edges/votes) and project-scoped.
- `backend/span_graph_store.py`: now Postgres-backed span graph store.
- Tests updated to assume Postgres-backed graph/spans.

Known loose ends:
- `backend/settings.py` still defines `CLAIM_DB_PATH` + `GRAPH_DB_PATH` even though stores are Postgres-backed.
- `POST /dev/wipe` truncates spine ingest tables but does not explicitly truncate all 09.3 tables (attachments/evidence/judgments/project meta/graph/spans).

Verification target (from `09.3-05-PLAN.md`):
- Start from empty `./data` and confirm graph-dependent UI features work and workboard state survives restart.

### 09.3-06: Delete Legacy Stores + Compat Flags

- Not started.

Legacy tails inventory (expected work in 09.3-06):
- `backend/project_io.py`: file-backed `data/project.json` + zip export/import; still wired in `backend/main.py` (`/project/export`, `/project/import`, and non-spine fallback path).
- `backend/ingestion_store.py`: legacy local ingestion directory scanning and file I/O.
- `backend/settings.py`: legacy local durable-path settings (`INGESTION_DIR`, `ATTACHMENT_DIR`, `EVIDENCE_STORE_DIR`, `CLAIM_DB_PATH`, `GRAPH_DB_PATH`).
- `backend/evidence_selection_store.py`: legacy `root_dir` property kept for older tests.
- `backend/main.py`: residual uses of legacy paths and legacy export/import endpoints.

## Verified This Session

- `python -m compileall backend tests` (host) passes.
- Compose smoke (container): `apply_migrations()` + `GraphStore().wipe()` + `SpanGraphStore().wipe()` succeeds.
- Runtime smoke (host -> compose):
  - `GET http://localhost:8000/docs` returns `200`.
  - `POST /dev/wipe` returns `ok: true` and truncates 09.3 tables (attachments/evidence/judgments/project meta/graph/spans).
  - `POST /spans/upsert` (with `ingest_id`) returns a `span:` id.

Not yet verified:
- Compose runtime smoke.
- Postgres migrations applied against a real DB.
- End-to-end ingest -> extract -> attach -> evidence -> judge -> export without local durable stores.

## Next Actions (Resume Here)

1) Start 09.3-06 removal work (delete/disable legacy filesystem stores + compat flags).
2) Verify from empty `./data`:
   - ingest -> extract -> resolve -> attach -> evidence -> judge -> export
   - restart `app-api` and confirm state persists (no local durable stores)
