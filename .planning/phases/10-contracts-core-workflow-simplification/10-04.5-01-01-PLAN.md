---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/schemas.py
  - backend/attachment_store.py
  - backend/attachment_pipeline.py
autonomous: true

must_haves:
  truths:
    - "A source PDF can be uploaded without any UI-local filesystem path"
    - "Uploaded source PDFs persist in object store and can be processed by the attachment pipeline"
    - "Exact-duplicate source uploads (same bytes) are detected and do not create new global sources by default"
  artifacts:
    - path: "backend/main.py"
      provides: "multipart attachment upload endpoint"
    - path: "backend/attachment_store.py"
      provides: "store attachment created from bytes/object key (no local_path required)"
    - path: "backend/attachment_pipeline.py"
      provides: "pipeline can process attachments created without local_path"
    - path: "backend/schemas.py"
      provides: "schema for attachment upload response"
  key_links:
    - from: "backend/main.py"
      to: "backend/attachment_store.py"
      via: "create attachment from uploaded bytes"
      pattern: "create_.*attachment.*(bytes|upload)"
    - from: "backend/attachment_pipeline.py"
      to: "attachment.pdf_object_key"
      via: "read bytes from object store"
      pattern: "pdf_object_key"
---

<objective>
Add a spine-only attachment upload path (multipart bytes) so the web app never depends on UI-local file paths.

Purpose: Make Intake deployable as a real multi-user web app.
Output: A new backend endpoint to upload source PDFs into the attachment system using object store + Postgres only.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-CONTEXT.md
@backend/main.py
@backend/attachment_store.py
@backend/attachment_pipeline.py
@backend/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multipart source upload endpoint (no local_path)</name>
  <files>backend/main.py, backend/schemas.py</files>
  <action>
Create a new endpoint for uploading a source PDF directly to the backend:

- Add `POST /attachments/upload` that accepts multipart form `file` (PDF) plus optional JSON fields in form params:
  - `reference_hint` (json string), `claim_text` (string), `doc_id` (optional), `citation_index` (optional), `target_id` (optional)
- Persist the PDF bytes to object store and create a *global* attachment row with `pdf_object_key` set.
- Return the public attachment status payload (same shape as existing `/attachments` responses).
- Add a request/response schema if needed; keep compatibility with existing `AttachmentResponse`.
- Compute and persist an exact `content_sha256` for dedupe (project-scoped): if an existing *global, unarchived* source attachment has the same hash, return that attachment instead of creating a new row.

Do NOT:
- require `local_path`
- write to `data/**` as the source of truth
  </action>
  <verify>
curl -sf -F "file=@corpus/workflow/hicks-chatgpt-is-bullshit.pdf;type=application/pdf" http://localhost:8000/attachments/upload | python -c "import json,sys; p=json.load(sys.stdin); print(bool((p.get('attachment') or {}).get('id')))"

# Dedupe check: second upload returns same attachment id
ID1=$(curl -sf -F "file=@corpus/workflow/hicks-chatgpt-is-bullshit.pdf;type=application/pdf" http://localhost:8000/attachments/upload | python -c "import json,sys; print((json.load(sys.stdin).get('attachment') or {}).get('id') or '')")
ID2=$(curl -sf -F "file=@corpus/workflow/hicks-chatgpt-is-bullshit.pdf;type=application/pdf" http://localhost:8000/attachments/upload | python -c "import json,sys; print((json.load(sys.stdin).get('attachment') or {}).get('id') or '')")
test "${ID1}" = "${ID2}"
  </verify>
  <done>
`POST /attachments/upload` exists, returns an attachment id, and repeated uploads of identical bytes return the same global attachment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Allow attachment pipeline to process byte-uploaded attachments</name>
  <files>backend/attachment_store.py, backend/attachment_pipeline.py</files>
  <action>
Update storage/pipeline so attachments created via `pdf_object_key` (without `local_path`) process correctly:

- Extend attachment creation in `backend/attachment_store.py` to support a row where `local_path` may be blank/null but `pdf_object_key` is present.
- Ensure the pipeline reads the PDF bytes from object store using `pdf_object_key` as the source.
- Ensure status transitions remain the same (pending -> converting -> parsing -> matched/error).
- Keep existing local_path behavior intact for local/dev flows.
  </action>
  <verify>
# Upload and let the pipeline run; then poll status
ID=$(curl -sf -F "file=@corpus/workflow/hicks-chatgpt-is-bullshit.pdf;type=application/pdf" http://localhost:8000/attachments/upload | python -c "import json,sys; print((json.load(sys.stdin).get('attachment') or {}).get('id') or '')")
python - <<'PY'
import time,requests,sys
aid=sys.argv[1]
for _ in range(120):
  r=requests.get(f"http://localhost:8000/attachments/{aid}",timeout=10)
  r.raise_for_status()
  st=(r.json().get('attachment') or {}).get('status')
  if st in ('matched','error'):
    print(st)
    raise SystemExit(0 if st=='matched' else 2)
  time.sleep(1)
raise SystemExit(3)
PY "$ID"
  </verify>
  <done>
An attachment uploaded via bytes progresses through the pipeline and reaches `matched` (or a clear `error`) without relying on backend access to a UI-local path.
  </done>
</task>

</tasks>

<verification>
- `python -m compileall -q backend`
- `pytest -q` (if present; otherwise skip)
</verification>

<success_criteria>
- Source upload works with multipart bytes and persists to object store
- Exact duplicates are deduped project-scoped
- Attachment pipeline processes byte-uploaded attachments
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-01-SUMMARY.md`
</output>
