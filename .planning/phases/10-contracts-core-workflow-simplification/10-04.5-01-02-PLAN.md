---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-02
type: execute
wave: 2
depends_on:
  - 10-04.5-01-01
files_modified:
  - frontend/ui.py
  - frontend/attachment_queue.py
  - frontend/ingestion_api.py
  - frontend/components/chase_queue.py
  - scripts/dev/verify_10_04_5_01_intake.sh
autonomous: false

must_haves:
  truths:
    - "User can drop multiple PDFs once and see each classified as citing/source/?"
    - "Unknown (?) intake items are held until user chooses citing vs source"
    - "Source Inbox shows project-shared, unassigned, matched sources with status icons and archive"
    - "Assignments use clone semantics; global source remains reusable"
  artifacts:
    - path: "frontend/ui.py"
      provides: "Unified Intake UI at top of left pane"
    - path: "frontend/attachment_queue.py"
      provides: "Uploads sources via backend bytes endpoint"
    - path: "scripts/dev/verify_10_04_5_01_intake.sh"
      provides: "Stage A intake verifier"
  key_links:
    - from: "frontend/attachment_queue.py"
      to: "/attachments/upload"
      via: "multipart upload"
      pattern: "/attachments/upload"
    - from: "frontend/ui.py"
      to: "frontend/ingestion_api.py"
      via: "citing upload and auto extract/resolve"
      pattern: "upload_pdf\(" 
    - from: "frontend/ui.py"
      to: "/attachments"
      via: "Source Inbox list"
      pattern: "/attachments\?archived=false.*X-Project-Id"
    - from: "frontend/ui.py"
      to: "/attachments/{id}"
      via: "archive/unarchive"
      pattern: "PATCH.*/attachments/.*archived.*X-Project-Id"
    - from: "frontend/components/chase_queue.py"
      to: "/attachments/{id}/clone"
      via: "Requested works assignment"
      pattern: "/attachments/.*/clone.*X-Project-Id"
    - from: "frontend/components/chase_queue.py"
      to: "/attachments/{id}/promote-ingest"
      via: "ensure source_ingest_id exists"
      pattern: "/attachments/.*/promote-ingest.*X-Project-Id"
---

<objective>
Unify the left-pane Intake UX (citing + source + unknown) and make source uploads web-deployable by using the backend bytes upload endpoint.

Purpose: Make Intake understandable and diagnosable; remove hidden upload paths.
Output: A single “Drop PDFs” entrypoint, Intake Inbox, and project-shared Source Inbox.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-CONTEXT.md
@frontend/ui.py
@frontend/attachment_queue.py
@frontend/ingestion_api.py
@frontend/components/chase_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace split uploaders with Unified Intake (Inbox + Source Inbox)</name>
  <files>frontend/ui.py</files>
  <action>
Implement left pane structure and unified drop behavior per context decisions:

- Add a top-of-left-pane “Drop PDFs” control that accepts multiple PDFs.
- After drop, show an “Intake Inbox” list with one row per file:
  - status icon: processing (animated), matched/complete (green), error (red X), unknown (?)
  - clicking the icon shows: current stage, last event time, error text
  - unknown rows show a clickable `?` with a menu: choose Citing vs Source, plus optional “Attach now” when a citing doc/target context exists.
  - allow per-file override before submission.
- Replace/remove (or hide behind Advanced) the existing "Upload documents" expander and the hidden "Advanced: cited source uploads" uploader.
- Reorganize left pane into collapsible sections in this order:
 - Reorganize left pane into collapsible sections in this order:
   1) Intake (expanded)
   2) Documents (expanded)
   3) Sources (collapsed unless errors/unknown)
   4) Project/Settings
   5) Advanced (collapsed; legacy CSV+TEI import lives here)
- Implement “resizable section heights” as a pragmatic Streamlit-compatible control:
  - add a per-section height slider (remembered per user/reviewer) that controls list max-height / items shown.
  - keep the VSCode-like collapsible feel; do not block on true drag handles.

Keep:
- Superscript/subscript stacked counts next to document rows, default meaning cites/cited-by.

Implement Source Inbox wiring (do not leave as implied):
- Backing data: `GET /attachments?archived=false` (send `X-Project-Id`) then filter to unassigned + matched (global sources):
  - `claim_id` empty
  - `doc_id` empty
  - `target_id` empty
  - `citation_index` is null
  - `status == matched`
- Render rows with icon+color per state; include a collapsible Errors section (red X).
- Add archive/unarchive actions using `PATCH /attachments/{id}` (send `X-Project-Id`) with `{archived: true|false}`.
- After promotion to ingest (`source_ingest_id` present), switch display label to citation-style label; otherwise show filename.
  </action>
  <verify>
Manual:
1) Start app; left pane shows Intake at top.
2) Drop 2 PDFs; see 2 Inbox rows and per-file classification controls.
3) Unknown row can be resolved via `?` menu.
  </verify>
  <done>
Single Intake entrypoint exists; both citing and source flows are discoverable; hidden cited-source uploader no longer blocks the workflow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route source uploads through /attachments/upload and add verifier script</name>
  <files>frontend/attachment_queue.py, frontend/ingestion_api.py, scripts/dev/verify_10_04_5_01_intake.sh</files>
  <action>
Wire source uploads to the backend bytes endpoint and add a fast verifier:

- Update `frontend/attachment_queue.py` enqueue/upload logic so Source PDFs are uploaded via `POST /attachments/upload` (multipart), not via persisted `local_path`.
- Send `X-Project-Id` on all `/attachments*` calls (upload/list/patch/clone/promote) so Source Inbox is project-scoped.
- Preserve local-only “Open PDF” convenience as optional; do not require it for attachment processing.
- Add `scripts/dev/verify_10_04_5_01_intake.sh` that:
 - Add `scripts/dev/verify_10_04_5_01_intake.sh` that:
   1) uploads one citing PDF via `/ingest`
   2) triggers extract+resolve and waits for completion
   3) uploads one source PDF via `/attachments/upload` and waits until status is matched/error
   4) asserts duplicate upload returns same global source id
  5) calls `/attachments/{id}/clone` twice with different `doc_id` values and asserts:
      - both clones are created with different ids
      - the original global attachment remains unassigned (no doc_id/claim_id/target_id)
  6) asserts project isolation:
     - uploading the same bytes under two different `X-Project-Id` values yields different global source ids
     - listing sources under each project id does not show the other's attachment

Keep script output very clear (PASS/FAIL per step).
  </action>
  <verify>
bash scripts/dev/verify_10_04_5_01_intake.sh
  </verify>
  <done>
Source uploads no longer depend on UI-local filesystem paths; verifier script passes on a clean restart.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure assignment flow uses clone semantics (Requested works)</name>
  <files>frontend/components/chase_queue.py</files>
  <action>
Make the Requested Works assignment flow explicitly use clone semantics:

- When assigning from Source Inbox candidates, call `POST /attachments/{id}/clone` (not PATCHing the global attachment).
- If the chosen global source lacks `source_ingest_id`, call `POST /attachments/{id}/promote-ingest` before cloning.
- Send `X-Project-Id` on all these calls.
- After clone succeeds, resume the workflow run.

This must ensure the global source remains reusable and that doc-level ref->ingest linking can persist.
  </action>
  <verify>
Manual:
1) Pick a citespan with Requested works.
2) Assign the same global source to two different requested targets; confirm the global source remains in Source Inbox.
  </verify>
  <done>
Requested Works assignment uses clones; global source remains reusable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Unified Intake UI + spine-only source upload</what-built>
  <how-to-verify>
1) Run: `bash scripts/dev/verify_10_04_5_01_intake.sh`
2) In UI: drop a citing PDF and a source PDF together; confirm they route correctly.
3) Confirm Source Inbox shows the matched source, and you can archive/unarchive it.
4) Assign that source to two different claims (Requested works) and confirm the original global source remains visible.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m compileall -q frontend backend`
</verification>

<success_criteria>
- Unified Intake left pane exists and is usable
- Source uploads go through bytes endpoint
- Verifier script passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-02-SUMMARY.md`
</output>
