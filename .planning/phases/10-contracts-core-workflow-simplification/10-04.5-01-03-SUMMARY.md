---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-03
subsystem: api
tags: [fastapi, postgres, attachments, multitenancy]

# Dependency graph
requires:
  - phase: 10-contracts-core-workflow-simplification
    provides: 10-04.5-01-01 bytes-only /attachments/upload with sha256 dedupe
provides:
  - X-Project-Id enforcement and 404-on-mismatch guards for /attachments endpoints
  - project-scoped attachment_store helpers for list/get/update operations
  - non-mutating clone invariant to keep sources global/unassigned
affects: [10-05 demo, source inbox, multi-project safety]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Project scoping via Header(None, alias=\"X-Project-Id\") + store-level project filters"
    - "Prefer 404 on cross-project mismatch to avoid existence leakage"

key-files:
  created: [.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-03-SUMMARY.md]
  modified: [backend/main.py, backend/attachment_store.py]

key-decisions:
  - "Project-scoped attachment reads use SQL WHERE attachment_id AND project_id to return None on mismatch"
  - "Clone only from unassigned sources; enforce invariants and return 409 on violations"

patterns-established:
  - "Attachment endpoint handlers resolve project_id once and pass it to project-scoped store helpers"

# Metrics
duration: 9 min
completed: 2026-02-26
---

# Phase 10-04.5 Plan 01-03: Attachments Project Scoping Summary

**All /attachments list/read/mutate/clone/promote operations are now project-scoped by X-Project-Id with 404-on-mismatch, and cloning is guarded as non-mutating for the source attachment.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-02-26T17:18:49Z
- **Completed:** 2026-02-26T17:27:59Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Enforced `X-Project-Id` scoping across `/attachments` list/read/mutate/clone/promote (prefer 404 on cross-project mismatch)
- Added project-scoped attachment_store helpers so list/get/update operations can be filtered server-side
- Locked down clone semantics so cloning can never mutate the source attachment placement fields

## Task Commits

Each task was committed atomically:

1. **Task 1: Enforce X-Project-Id scoping for attachments list/read/mutate** - `c68b77a` (feat)
2. **Task 2: Confirm clone semantics do not mutate the global source** - `51e1470` (fix)
3. **Task 3: Flag prune candidates for later refactor (do not delete unless replaced)** - [pending] (docs)

**Plan metadata:** [pending] (docs: complete plan)

## Files Created/Modified

- `backend/main.py` - Require/read `X-Project-Id` for attachments endpoints; enforce 404-on-mismatch
- `backend/attachment_store.py` - Add project-scoped list/get/update helpers and clone invariants
- `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-03-SUMMARY.md` - Execution summary + prune candidates

## Decisions Made

- Prefer store-level scoping (`WHERE attachment_id=%s AND project_id=%s`) so cross-project reads return None and endpoints can safely 404.
- Treat cloning as an operation on global/unassigned sources; reject cloning from placed attachments and validate invariants transactionally.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Prune candidates

1. `backend/main.py` - route `POST /attachments` (`create_global_attachment`)
   - why: legacy local-path upload path; does not enforce X-Project-Id and likely superseded by `/attachments/upload`
   - replaced_by: `POST /attachments/upload`
   - safe_to_delete_after: 10-04.5 refactor pass once callers are confirmed migrated
   - confidence: medium

2. `backend/main.py` - route `POST /claims/{claim_id}/attachments` (`create_claim_attachment`)
   - why: legacy local-path upload path; does not accept X-Project-Id; likely unused in unified Intake flow
   - replaced_by: `POST /attachments/upload` + `POST /attachments/{id}/clone` (placed row)
   - safe_to_delete_after: 10-04.5 refactor pass once UI/clients no longer call it
   - confidence: low

3. `backend/attachment_store.py` - functions `set_archived`, `set_placement`
   - why: unscoped helpers; `/attachments/*` handlers now use `*_for_project` variants
   - replaced_by: `set_archived_for_project`, `set_placement_for_project`
   - safe_to_delete_after: after confirming no remaining call sites require unscoped updates
   - confidence: medium

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for the remaining 10-04.5 Source Inbox/Intake hardening work; attachments endpoints now enforce server-side partitioning by project.

---
*Phase: 10-contracts-core-workflow-simplification*
*Completed: 2026-02-26*
