---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-04
type: execute
wave: 2
depends_on:
  - 10-04.5-01-01
files_modified:
  - backend/main.py
  - backend/spine/ingest_view.py
  - backend/spine/documents.py
autonomous: true

must_haves:
  truths:
    - "Ingest upload uses multipart bytes and persists PDFs to object store (no UI-local path dependency)"
    - "Ingest list/read are project-scoped server-side (cross-project reads are denied)"
  artifacts:
    - path: "backend/main.py"
      provides: "/ingest endpoints enforce X-Project-Id scoping"
  key_links:
    - from: "backend/main.py"
      to: "X-Project-Id"
      via: "ingest scoping"
      pattern: "Header\(None, alias=\"X-Project-Id\"\)"
---

<objective>
Harden the citing-document ingest surface for web deployability: bytes-only uploads and server-side project isolation for list/read.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-CONTEXT.md
@backend/main.py
@backend/spine/ingest_view.py
@backend/spine/documents.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce X-Project-Id on /ingest list/read (404 on mismatch)</name>
  <files>backend/main.py, backend/spine/ingest_view.py, backend/spine/documents.py</files>
  <action>
Make ingest endpoints project-scoped server-side:

- Require `X-Project-Id` header (400 if missing) by default; allow dev-only fallback via `ALLOW_DEFAULT_PROJECT_ID_FOR_DEV=true`.
- Ensure `GET /ingest` lists only documents in the given project.
- Ensure `GET /ingest/{doc_id}` returns 404 if the document is not in the given project.
- Prefer 404 on mismatch (do not leak existence across projects).
  </action>
  <verify>
P1="proj-a"; P2="proj-b"
PDF="${CITING_PDF_PATH:-corpus/workflow/rudko-chatgpt-is-incredible-at-being-average.pdf}"

DOC1=$(curl -sf -H "X-Project-Id: ${P1}" -F "file=@${PDF};type=application/pdf" http://localhost:8000/ingest | python -c "import json,sys; print(((json.load(sys.stdin).get('document') or {}).get('id')) or '')")
test -n "${DOC1}"

# Cross-project read denied
curl -sf -H "X-Project-Id: ${P2}" "http://localhost:8000/ingest/${DOC1}" >/dev/null && exit 1 || true
  </verify>
  <done>
Ingest list/read are project-scoped and deny cross-project access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm ingest is bytes-only and object-store-backed</name>
  <files>backend/main.py</files>
  <action>
Confirm and harden the ingest upload path:

- Upload uses multipart PDF bytes and persists into object store (S3/MinIO) and Postgres.
- No backend code path requires reading a UI-local filesystem path for ingest.
- If any legacy `local_path`-style ingest path exists, gate it behind an explicit dev-only flag or remove it.
  </action>
  <verify>
# Smoke: ingest upload works with multipart bytes
P1="proj-a"
PDF="${CITING_PDF_PATH:-corpus/workflow/rudko-chatgpt-is-incredible-at-being-average.pdf}"
curl -sf -H "X-Project-Id: ${P1}" -F "file=@${PDF};type=application/pdf" http://localhost:8000/ingest | python -c "import json,sys; d=json.load(sys.stdin).get('document') or {}; print('ok' if d.get('id') else 'bad')" | grep -q ok
  </verify>
  <done>
Ingest upload is bytes-only and compatible with web deployment.
  </done>
</task>

</tasks>

<verification>
- `python -m compileall -q backend`
</verification>

<success_criteria>
- /ingest list/read are project-scoped
- Ingest upload does not depend on UI-local paths
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-04-SUMMARY.md`
</output>
