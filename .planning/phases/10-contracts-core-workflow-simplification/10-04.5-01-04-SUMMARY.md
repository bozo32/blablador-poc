---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-04
subsystem: api
tags: [fastapi, ingest, project-scoping, postgres, s3, minio]

# Dependency graph
requires:
  - phase: 10-04.5-01-01
    provides: ingest spine baseline + object store persistence
  - phase: 10-04.5-01-03
    provides: attachment project scoping + safer clone semantics
provides:
  - X-Project-Id-scoped /ingest list/read with 404 on cross-project mismatch
  - Multipart-bytes ingest upload (no UI-local path dependency)
  - Legacy local_path attachment-create endpoints gated for dev-only use
affects: [10-04.5, intake, web-deploy]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Header-scoped multi-project reads using X-Project-Id with dev-only default fallback
    - Deny-by-default gating for legacy UI-local file path inputs

key-files:
  created:
    - .planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-04-SUMMARY.md
  modified:
    - backend/main.py

key-decisions:
  - "Missing X-Project-Id returns 400 unless ALLOW_DEFAULT_PROJECT_ID_FOR_DEV=true"
  - "Legacy local_path attachment creates are disabled by default (ALLOW_LOCAL_PATH_UPLOAD_FOR_DEV escape hatch)"

patterns-established:
  - "Project scoping: require X-Project-Id header and deny cross-project reads with 404"
  - "Dev-only fallbacks must be explicit env flags"

# Metrics
duration: 20 min
completed: 2026-02-26
---

# Phase 10-04.5 Plan 01-04: Ingest Surface Hardening Summary

**/ingest now enforces server-side project scoping via X-Project-Id and keeps uploads bytes-only + object-store-backed for web deploys.**

## Performance

- **Duration:** 20 min
- **Started:** 2026-02-26T17:33:09Z
- **Completed:** 2026-02-26T17:53:18Z
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments
- Required `X-Project-Id` on `/ingest` list/read (dev-only default allowed via `ALLOW_DEFAULT_PROJECT_ID_FOR_DEV=true`).
- Kept `/ingest` upload as multipart-bytes -> S3/MinIO + Postgres (no UI-local path dependency).
- Prevented accidental web usage of legacy `local_path` attachment-create routes by gating them behind `ALLOW_LOCAL_PATH_UPLOAD_FOR_DEV=true`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Enforce X-Project-Id on /ingest list/read (404 on mismatch)** - `5595dfb` (fix)
2. **Task 2: Confirm ingest is bytes-only and object-store-backed** - `efd23ce` (fix)
3. **Task 3: Flag prune candidates for later refactor (do not delete unless replaced)** - (this commit)

## Files Created/Modified
- `backend/main.py` - Enforce X-Project-Id scoping for `/ingest` + gate legacy local_path attachment creates.
- `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-04-SUMMARY.md` - Execution record + prune candidates.

## Decisions Made
- Require explicit `X-Project-Id` by default on ingest endpoints; missing header returns 400 unless dev fallback is enabled.
- Treat any client-provided `local_path` API surface as dev-only (deny by default; bytes upload endpoints are the web-safe contract).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Gated legacy local_path attachment create routes (filesystem read surface)**
- **Found during:** Task 2 (bytes-only ingest hardening)
- **Issue:** `/claims/{claim_id}/attachments` and `/attachments` accepted `local_path`, enabling a deployed client to request server-local file reads.
- **Fix:** Added `ALLOW_LOCAL_PATH_UPLOAD_FOR_DEV` gate; default behavior rejects these routes with guidance to use bytes upload.
- **Files modified:** `backend/main.py`
- **Verification:** `/ingest` multipart upload still succeeds; overall `python -m compileall -q backend` passes.
- **Committed in:** `efd23ce`

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Security/deploy hardening aligned with plan intent; no scope expansion into new features.

## Issues Encountered
None.

## Prune candidates

1. path: `backend/main.py`
   symbol: `create_claim_attachment` (`POST /claims/{claim_id}/attachments`)
   why: Legacy request model includes `local_path` (UI-local filesystem dependency); now dev-gated.
   replaced_by: bytes upload via `POST /attachments/upload` (plus follow-up to associate to claim)
   safe_to_delete_after: follow-up plan verifying web UI never calls local_path endpoints
   confidence: medium

2. path: `backend/main.py`
   symbol: `create_global_attachment` (`POST /attachments`)
   why: Legacy request model includes `local_path`; now dev-gated.
   replaced_by: `POST /attachments/upload`
   safe_to_delete_after: follow-up plan verifying no local_path clients remain + tests updated
   confidence: high

3. path: `backend/schemas.py`
   symbol: `AttachmentCreateRequest.local_path`, `AttachmentGlobalCreateRequest.local_path`
   why: Only needed for legacy local_path endpoints; should disappear once those routes are removed.
   replaced_by: form/multipart upload fields on `POST /attachments/upload`
   safe_to_delete_after: prune candidates 1-2 are deleted and API clients are updated
   confidence: medium

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ready for the next 10-04.5 intake hardening step with `/ingest` now safe for multi-project + web upload clients.

---
*Phase: 10-contracts-core-workflow-simplification*
*Completed: 2026-02-26*
