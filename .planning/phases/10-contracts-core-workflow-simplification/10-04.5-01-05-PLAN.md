---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-05
type: execute
wave: 5
depends_on:
  - 10-04.5-01-02
  - 10-04.5-01-03
  - 10-04.5-01-04
files_modified:
  - frontend/attachment_queue.py
  - frontend/ui.py
  - frontend/components/chase_queue.py
autonomous: false

must_haves:
  truths:
    - "Source Inbox is project-shared and project-scoped (no cross-project leakage)"
    - "Source uploads go through /attachments/upload (multipart bytes), not UI-local paths"
    - "Assignments use clone semantics; global source remains reusable"
  artifacts:
    - path: "frontend/attachment_queue.py"
      provides: "multipart source upload + project header propagation"
    - path: "frontend/ui.py"
      provides: "Source Inbox list + archive/unarchive"
    - path: "frontend/components/chase_queue.py"
      provides: "Requested works assignment via clone/promote"
  key_links:
    - from: "frontend/attachment_queue.py"
      to: "/attachments/upload"
      via: "multipart upload"
      pattern: "/attachments/upload"
    - from: "frontend/attachment_queue.py"
      to: "X-Project-Id"
      via: "header propagation"
      pattern: "X-Project-Id"
    - from: "frontend/ui.py"
      to: "/attachments"
      via: "Source Inbox list"
      pattern: "/attachments\?archived=false"
    - from: "frontend/components/chase_queue.py"
      to: "/attachments/{id}/clone"
      via: "Requested works assignment"
      pattern: "/attachments/.*/clone"
    - from: "frontend/components/chase_queue.py"
      to: "/attachments/{id}/promote-ingest"
      via: "ensure source_ingest_id exists"
      pattern: "/attachments/.*/promote-ingest"
---

<objective>
Make source handling fully web-deployable: Source Inbox is project-scoped, uploads use bytes endpoint, and assignment uses clones.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-CONTEXT.md
@frontend/attachment_queue.py
@frontend/ui.py
@frontend/components/chase_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Source uploads through /attachments/upload with X-Project-Id</name>
  <files>frontend/attachment_queue.py</files>
  <action>
Update Source upload plumbing:

- Upload source PDFs via `POST /attachments/upload` (multipart bytes).
- Ensure every request to `/attachments*` includes `X-Project-Id`:
  - upload
  - list
  - read status
  - archive/unarchive
- Preserve any local-only PDF caching as optional; backend processing must not depend on UI-local `local_path`.
- Add a quick call-site audit: search for all `/attachments` requests across `frontend/ui.py`, `frontend/attachment_queue.py`, and `frontend/components/chase_queue.py` and ensure headers include `X-Project-Id`.
  </action>
  <verify>
Static audit:
- `rg -n "(/attachments|attachments/upload)" frontend/ui.py frontend/attachment_queue.py frontend/components/chase_queue.py`
- For each request call site found, confirm it passes a `headers={"X-Project-Id": ...}` (or calls a shared helper that injects it).

Manual smoke: upload a source and confirm backend receives it without `local_path`.
  </verify>
  <done>
Source uploads use bytes endpoint and consistently send project header.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Source Inbox list + archive/unarchive (project-shared)</name>
  <files>frontend/ui.py</files>
  <action>
Implement project-shared Source Inbox:

- Backing data: `GET /attachments?archived=false` with `X-Project-Id` and filter to unassigned+matched global sources.
- Render status iconography per context (matched green, processing animated, unknown ?, error red X).
- Errors appear in a collapsible Errors section.
- Diagnosability: each source row must have a clickable status/icon area that opens details showing:
  - backend status
  - last event timestamp (best-effort)
  - last error text
- Archive/unarchive actions call `PATCH /attachments/{id}` with `X-Project-Id`.
- Display label switches from filename to citation label after promote-ingest; disambiguate collisions with short hash.
  </action>
  <verify>
Manual: Source Inbox shows matched sources and archive/unarchive works.
  </verify>
  <done>
Source Inbox is visible, project-shared, project-scoped, and each item has a details view (status/last event/error).
  </done>
</task>

<task type="auto">
  <name>Task 3: Requested works assignment uses clone + promote-ingest</name>
  <files>frontend/components/chase_queue.py</files>
  <action>
Ensure assignment uses clone semantics:

- Assign from Source candidates calls `POST /attachments/{id}/clone` (never PATCHes the global source).
- If `source_ingest_id` missing, call `POST /attachments/{id}/promote-ingest` first.
- Send `X-Project-Id` on these calls.
  </action>
  <verify>
Manual: assign the same global source twice; original remains unassigned and reusable.
  </verify>
  <done>
Assignment uses clones and leaves global source reusable.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flag prune candidates for later refactor (do not delete unless replaced)</name>
  <files>.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-05-SUMMARY.md</files>
  <action>
While implementing, look for redundant Source upload code paths (especially anything that builds/sends `local_path`) and duplicated attachment request helpers.

Rules:
- Only delete code that is explicitly replaced and verified within this plan.
- Everything else becomes a prune candidate to be confirmed in the dedicated refactor pass after 10-04.5.

In the SUMMARY, add a section `## Prune candidates` (may be empty). For each candidate, include:
- path
- symbol (function/class/widget)
- why it's likely dead/redundant
- replaced_by (/attachments/upload + Source Inbox + clone), if known
- safe_to_delete_after (which plan/verification)
- confidence: high | medium | low
  </action>
  <verify>
rg -n "^## Prune candidates" .planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-05-SUMMARY.md
  </verify>
  <done>
Prune candidates are captured without expanding scope.
  </done>
</task>

</tasks>

<verification>
- `python -m compileall -q frontend backend`
</verification>

<success_criteria>
- Source Inbox is project-scoped and usable
- Source uploads are bytes-only
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-05-SUMMARY.md`
</output>
