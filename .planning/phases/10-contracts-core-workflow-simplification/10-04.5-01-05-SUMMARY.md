---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-05
subsystem: ui
tags: [streamlit, attachments, source-inbox, multitenancy, clone]

# Dependency graph
requires:
  - phase: 10-04.5-01-01
    provides: bytes-only /attachments/upload + sha256 dedupe
  - phase: 10-04.5-01-03
    provides: /attachments endpoints project-scoped + non-mutating clone
provides:
  - Frontend source uploads via /attachments/upload (multipart bytes)
  - X-Project-Id propagation for /attachments* requests in UI helpers
  - Project-shared Source Inbox UI (backend-backed list + archive/unarchive)
  - Requested works assignment via promote-ingest + clone (no global mutation)
affects: [10-04.5 intake, source inbox, multi-project safety]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Always send X-Project-Id for /attachments* requests"
    - "Assign global sources by cloning (never PATCH a global/unassigned source)"

key-files:
  created:
    - .planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-05-SUMMARY.md
  modified:
    - frontend/attachment_queue.py
    - frontend/evidence_api.py
    - frontend/ui.py
    - frontend/components/chase_queue.py

key-decisions:
  - "Source Inbox is backend-backed and project-scoped; Streamlit queue is a view/cache"
  - "Requested works assignment uses promote-ingest (best-effort) then clone"

# Metrics
duration: manual (tooling degraded)
completed: 2026-02-27
---

# Phase 10-04.5 Plan 01-05: Source Inbox + Clone Assignment Summary

**Made source handling web-deployable end-to-end: uploads use bytes-only `/attachments/upload`, the Source Inbox is project-scoped and backend-backed, and assignments happen via clone semantics so the global source remains reusable.**

## Accomplishments

- Routed Source uploads through `POST /attachments/upload` (multipart bytes) and ensured `/attachments*` requests include `X-Project-Id`.
- Added `X-Project-Id` propagation for attachment span excerpt/jump calls so evidence review keeps working with server-side scoping.
- Implemented a project-shared Source Inbox UI that lists backend attachments and supports archive/unarchive.
- Updated Requested works assignment to use `POST /attachments/{id}/promote-ingest` (best-effort) then `POST /attachments/{id}/clone` (never mutating the global source).

## Task Commits

1. **Task 1: Route Source uploads through /attachments/upload with X-Project-Id** - `331ca6f` (feat)
2. **Task 2: Implement Source Inbox list + archive/unarchive (project-shared)** - `59d2122` (feat)
3. **Task 3: Requested works assignment uses clone + promote-ingest** - `80013f0` (feat)
4. **Task 4: Flag prune candidates for later refactor (do not delete unless replaced)** - `ce78cea` (docs)

**Plan metadata:** [pending] (docs: complete plan)

## Prune candidates

1. `frontend/attachment_queue.py` - legacy placement helper `place_attachment()` uses `PATCH /attachments/{id}`
   - why: mutates the attachment row; global sources should be assigned via clone
   - replaced_by: `/attachments/{id}/clone` call sites (requested works + Source Inbox assignment)
   - safe_to_delete_after: `bash scripts/dev/verify_10_04_5_01_intake.sh` passes and no callers remain
   - confidence: medium

2. `frontend/ui.py` - older Source Bin surfaces (pre-Intake) now largely redundant
   - why: Intake + Source Inbox are the intended entrypoints
   - replaced_by: `render_intake_panel()` + `render_sources_panel()`
   - safe_to_delete_after: 10-04.5 refactor pass
   - confidence: low

## Deviations / Notes

- Implemented this plan without spawning subagents due to persistent tool aborts (see commit messages for atomic task boundaries).

---
*Phase: 10-contracts-core-workflow-simplification*
*Completed: 2026-02-27*
