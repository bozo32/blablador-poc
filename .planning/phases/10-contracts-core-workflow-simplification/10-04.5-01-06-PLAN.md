---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-06
type: execute
wave: 5
depends_on:
  - 10-04.5-01-05
files_modified:
  - scripts/dev/verify_10_04_5_01_intake.sh
autonomous: false

must_haves:
  truths:
    - "After a restart, previously uploaded citing+source items still list/read within their project"
    - "Cross-project access is denied (read/patch/list isolation)"
    - "Source uploads work without any UI-local filesystem paths"
  artifacts:
    - path: "scripts/dev/verify_10_04_5_01_intake.sh"
      provides: "automated intake + isolation + restart checks"
  key_links:
    - from: "scripts/dev/verify_10_04_5_01_intake.sh"
      to: "/attachments/upload"
      via: "multipart upload"
      pattern: "/attachments/upload"
---

<objective>
Add a single verifier script and a human verification gate proving Intake is spine-only, project-scoped, and restart-safe.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement verify_10_04_5_01_intake.sh (upload, dedupe, clone, isolation, restart)</name>
  <files>scripts/dev/verify_10_04_5_01_intake.sh</files>
  <action>
Create `scripts/dev/verify_10_04_5_01_intake.sh` with clear PASS/FAIL per step.

Requirements:
- Parameterize PDFs via env vars:
  - `CITING_PDF_PATH` (default: `corpus/workflow/rudko-chatgpt-is-incredible-at-being-average.pdf`)
  - `SOURCE_PDF_PATH` (default: `corpus/workflow/hicks-chatgpt-is-bullshit.pdf`)
- Use two project ids (e.g. `proj-a`, `proj-b`) and always send `X-Project-Id`.

Checks:
1) Upload citing PDF via `/ingest` and wait for extract+resolve.
   - Explicitly verify via:
     - `GET /ingest` (list includes the citing doc under the project)
     - `GET /ingest/{doc_id}` (read succeeds under the project)
   - Assert citing upload is bytes-only (no UI-local path contract): the upload uses multipart file bytes and returns a document with an object-store-backed pdf reference (no local_path dependency).
2) Upload source PDF via `/attachments/upload` and wait for matched/error.
3) Assert dedupe within a project returns the same global source id.
4) Assert clone creates distinct ids and does not mutate source.
5) Assert cross-project isolation (list/read/patch denies and lists don’t leak).
   - For citing docs: attempt `GET /ingest/{doc_id}` under the wrong project id and assert it fails (404/empty as appropriate).
6) Restart check:
  - Primary (automated): if `docker compose` is available, perform `docker compose restart` (or restart the API service) and then re-run list/read checks.
  - Fallback (two-phase): if `docker compose` is not available, script writes a small state file (ids + project ids) and exits with a clear instruction to:
    1) restart the API process manually
    2) rerun `bash scripts/dev/verify_10_04_5_01_intake.sh --phase post`
    The `--phase post` run must deterministically PASS/FAIL.
  </action>
  <verify>
bash scripts/dev/verify_10_04_5_01_intake.sh
  </verify>
  <done>
Script passes and proves isolation + restart persistence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Verifier script + end-to-end intake proof</what-built>
  <how-to-verify>
1) Run: `bash scripts/dev/verify_10_04_5_01_intake.sh`
2) In UI: upload one source once, assign it twice; confirm global source remains reusable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m compileall -q .`
</verification>

<success_criteria>
- Verifier exists and is readable
- Verifier covers restart persistence and project isolation
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-06-SUMMARY.md`
</output>
