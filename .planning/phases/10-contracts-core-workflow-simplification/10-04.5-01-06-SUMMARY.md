---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-06
subsystem: verification
tags: [verification, bash, intake, multitenancy, restart]

# Dependency graph
requires:
  - phase: 10-04.5-01-05
    provides: Source Inbox + clone semantics wired in UI
provides:
  - Verifier script for intake + isolation + restart persistence
  - Consolidated prune-candidates backlog for the 10-04.5 refactor pass

affects: [10-04.5 acceptance, demo readiness]

# Metrics
duration: manual (tooling degraded)
completed: pending checkpoint
---

# Phase 10-04.5 Plan 01-06: Verifier + Gate Summary

## What I built

- Added `scripts/dev/verify_10_04_5_01_intake.sh` to exercise:
  - bytes-only citing ingest + bytes-only source uploads
  - project isolation (cross-project 404s)
  - dedupe (same bytes in-project)
  - clone non-mutation invariants
  - restart persistence (docker compose path or two-phase manual fallback)
- Consolidated all prune candidates into:
  - `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-PRUNE-CANDIDATES.md`

## Task Commits

1. **Task 1: Implement verify_10_04_5_01_intake.sh** - `017e54f` (test)
2. **Task 2: Consolidate prune candidates backlog** - `0206398` (docs)

## Notes

- Verifier was hardened after an initial JSONDecodeError on non-API responses.
- It now validates `GET /openapi.json` contains `/ingest` and prints response excerpts on non-JSON.
- Fix commit: `1b1e90e`
- Follow-up fix (Python heredoc quoting): `b13994a`

## Checkpoint (blocking)

Run the verifier and do a quick UI smoke:

1) `bash scripts/dev/verify_10_04_5_01_intake.sh`
2) In UI: upload one source once, assign it twice; confirm the global source remains reusable.
3) Review: `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-PRUNE-CANDIDATES.md`

Resume signal: type `approved` or describe issues.
