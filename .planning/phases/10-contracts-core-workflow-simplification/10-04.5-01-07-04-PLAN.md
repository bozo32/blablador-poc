---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-07-04
type: execute
wave: 9
depends_on:
  - 10-04.5-01-07-02
files_modified:
  - backend/main.py
  - backend/spine/opinion_events.py
autonomous: true

must_haves:
  truths:
    - "Project export includes opinion events so a user can restore reading artifacts later"
    - "Project import restores opinion events deterministically (append-only replay)"
    - "Export/import is project-scoped via X-Project-Id (no accidental default-project coupling)"
  artifacts:
    - path: "backend/main.py"
      provides: "/project/export + /project/import include opinion_events.ndjson"
    - path: "backend/spine/opinion_events.py"
      provides: "NDJSON export/import helpers for opinion events"
  key_links:
    - from: "backend/main.py"
      to: "backend/spine/opinion_events.py"
      via: "zipfile export/import wiring"
      pattern: "opinion"
---

<objective>
Extend project export/import to include reviewer opinion artifacts (opinion events) so reading artifacts are portable.

Purpose: User-controlled restore semantics for reading artifacts.
Output: opinion_events.ndjson included in project zip; import replays it.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-07-CONTEXT.md
@backend/main.py
@backend/spine/project_meta.py
@backend/spine/evidence_decisions.py
@backend/spine/opinion_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NDJSON import helper for opinion events</name>
  <files>backend/spine/opinion_events.py</files>
  <action>
Extend `backend/spine/opinion_events.py` with an import function:

- `import_events_ndjson(project_id, user_id, blob: bytes, overwrite: bool = False) -> dict`

Rules:
- Preserve event ids if present in NDJSON; otherwise generate.
- If `overwrite=false`, skip events already present (by (project_id, event_id) or by idempotency key if used).
- Ensure import is safe and deterministic for an empty project restore.
  </action>
  <verify>
python -m compileall -q backend
  </verify>
  <done>
Opinion events NDJSON can be replayed into the database without duplicates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire opinion_events.ndjson into /project/export and /project/import</name>
  <files>backend/main.py</files>
  <action>
Update `GET /project/export` and `POST /project/import` to include opinion events.

Changes:
- Require `X-Project-Id` header for export/import (use `_require_project_id_for_upload`).
- In export zip, add `opinion_events.ndjson` containing all opinion events readable by the exporting principal.
- In import, if `opinion_events.ndjson` exists, replay it using `import_events_ndjson(...)`.

Keep existing evidence decisions export/import unchanged.
  </action>
  <verify>
python -m compileall -q backend
  </verify>
  <done>
Project export/import includes opinion events and is project-scoped via X-Project-Id.
  </done>
</task>

</tasks>

<verification>
- Export a project, import into a fresh/empty project id, and confirm follow statuses appear for the reviewer.
</verification>

<success_criteria>
- Zip export contains `opinion_events.ndjson`.
- Import replays opinion events without duplication.
- Export/import are explicitly project-scoped via `X-Project-Id`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-contracts-core-workflow-simplification/10-04.5-01-07-04-SUMMARY.md`.
</output>
