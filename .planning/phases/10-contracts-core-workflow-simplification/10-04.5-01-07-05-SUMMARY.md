---
phase: 10-contracts-core-workflow-simplification
plan: 10-04.5-01-07-05
subsystem: ui
tags: [streamlit, opinion-layer, follow-rail, server-backed-state]

# Dependency graph
requires:
  - phase: 10-04.5-01-07-03
    provides: Opinion event storage schema (follow/ignore/complete events)
  - phase: 10-04.5-01-07-04
    provides: Backend API endpoints for follow projection (list_follows_by_doc)
provides:
  - Reading rail backed by server opinion follow projection (durable, reviewer-scoped)
  - Frontend span resolution helper for cite-window operations
  - Consistent X-Project-Id header pattern across opinion API helpers
affects: [future phases requiring follow persistence across sessions]

# Tech tracking
tech-stack:
  added: []
  patterns: [server-backed opinion layer for UI state, append-only follow with ignore status]

key-files:
  created: []
  modified:
    - frontend/opinion_api.py - Added _opinion_headers helper, resolve_span_id function
    - frontend/ui.py - Wired reading rail to server-backed opinion layer

key-decisions:
  - Used graceful degradation pattern: server-first with session_state fallback
  - Drop uses append-only ignore status (not delete) per plan requirement

patterns-established:
  - "Server-backed UI state: read from server projection, write to opinion events"
  - "Transient cache: session_state used only for fast re-renders within same Streamlit run"

# Metrics
duration: ~5min
completed: 2026-02-28
---

# Phase 10: Plan 10-04.5-01-07-05 Summary

**Reading rail now backed by server opinion layer with append-only follow/ignore - enables durable, reviewer-scoped chase queue across page refreshes**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-02-28
- **Tasks:** 3/3 (completed)
- **Files modified:** 2 (frontend/opinion_api.py, frontend/ui.py)

## Accomplishments

1. Added consistent X-Project-Id header pattern to opinion_api.py via `_opinion_headers()` helper
2. Added `resolve_span_id()` helper to resolve cite-window span_id from (doc_id, citation_index, target_id)
3. Wired ui.py reading rail to use server-backed opinion layer:
   - Follow actions write to server via `append_follow()`
   - Drop actions write ignore status to server (append-only, not delete)
   - Rail loading reads from server via `list_follows_by_doc()`
   - Graceful degradation: falls back to session_state when server unavailable

## Task Commits

All work completed in single atomic commit:

- **Commit:** `1c6c245` (feat)
  - Task 1: Add _opinion_headers() helper for consistent X-Project-Id pattern
  - Task 2: Add resolve_span_id() helper for span resolution
  - Task 3: Wire ui.py reading rail to server-backed opinion layer

## Files Created/Modified

- `frontend/opinion_api.py` - Added `_opinion_headers()` helper and `resolve_span_id()` function
- `frontend/ui.py` - Updated `_follow_citation()`, `_queue_drop()`, and rail loading to use server-backed opinion layer

## Decisions Made

- Used graceful degradation: server-first, fall back to session_state if API unavailable
- Drop uses append-only "ignore" status per plan requirement (not delete)
- Session state retained as transient cache for fast re-renders within same Streamlit run

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - all tasks completed as specified.

## Next Phase Readiness

- Reading rail is now durable (persists across page refreshes)
- Reviewer-scoped (rail changes when switching Current user)
- Drop does not delete; uses append-only ignore status
- Ready for testing with backend opinion API endpoints

---
*Phase: 10-contracts-core-workflow-simplification*
*Completed: 2026-02-28*
