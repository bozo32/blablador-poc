---
status: testing
phase: 10-04.5-01-07
source: 10-04.5-01-07-01-SUMMARY.md, 10-04.5-01-07-02-SUMMARY.md, 10-04.5-01-07-03-SUMMARY.md, 10-04.5-01-07-04-SUMMARY.md, 10-04.5-01-07-05-SUMMARY.md
started: 2026-02-28
updated: 2026-02-28
---

## Current Test

[testing blocked by UI issues]

## Tests

### 1. Follow citations persist across page refresh
expected: |
  1. Open a document with citations
  2. Click to follow 2 citations (add them to chase queue)
  3. Refresh the page (or restart Streamlit)
  4. The chase queue should still show those 2 followed citations
result: issue
reported: "when clicking on citation chips in the reading pane, the most recently selected citespan replaces the one before it at the top of the right pane. This is wrong. Citespans should be presented in dropdown areas as is done for the 'documents' 'sources' 'project/settings/' menu areas on the left. Also: dropdown areas on right should have less padding (0.1 line is enough)."
severity: blocker

### 2. Switch reviewer - rail changes
expected: |
  1. With followed citations for Reviewer A, switch Current user to Reviewer B
  2. The chase queue should be empty (or show Reviewer B's followed citations)
  3. Follow some citations as Reviewer B
  4. Switch back to Reviewer A - should see Reviewer A's followed citations again
result: pending

### 3. Drop citation uses append-only ignore
expected: |
  1. Have followed citations in the chase queue
  2. Click the X/drop button to remove a citation
  3. The citation disappears from the queue
  4. Click to follow that same citation again - it should reappear (not blocked)
result: pending

### 4. Project export includes opinion events
expected: |
  1. Have some followed citations in a project
  2. Use the project export function (or API endpoint)
  3. The exported zip should contain opinion_events.ndjson with follow events
result: pending

### 5. Project import restores opinion events
expected: |
  1. Export a project with followed citations
  2. Import the export into a fresh/empty project
  3. The imported project should show the same followed citations
result: pending

## Summary

total: 5
passed: 0
issues: 1
pending: 5
skipped: 0

## Gaps

- truth: "Follow citations persist across page refresh"
  status: failed
  reason: "User reported: when clicking on citation chips in the reading pane, the most recently selected citespan replaces the one before it at the top of the right pane. This is wrong. Citespans should be presented in dropdown areas as is done for the 'documents' 'sources' 'project/settings/' menu areas on the left. Also: dropdown areas on right should have less padding (0.1 line is enough)."
  severity: blocker
  test: 1

- truth: "Assign source to citation works without getting stuck"
  status: failed
  reason: "User reported: when clicking 'assign' to assign a source to a citation, the program got stuck in a loop"
  severity: blocker
  test: (not in test plan - existing bug)
