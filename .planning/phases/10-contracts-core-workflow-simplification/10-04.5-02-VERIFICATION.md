---
phase: 10-04.5-02
verified: 2026-03-02T08:14:17Z
status: gaps_found
score: 1/6 must-haves verified
gaps:
  - truth: "uid+project scope is mandatory end-to-end across frontend/backend"
    status: failed
    reason: "Multiple backend and frontend paths still default to project/user/reviewer values instead of strict scope headers."
    artifacts:
      - path: "backend/main.py"
        issue: "Fallback defaults remain on scoped paths (e.g. lines ~5230, ~5296, ~5379, ~5536, ~5620-5621)."
      - path: "frontend/evidence_api.py"
        issue: "Uses session fallback project_id='default' and does not send strict scope identity on core evidence calls."
      - path: "frontend/judgment_api.py"
        issue: "Judgment calls are unscoped (no X-Project-Id / X-User-Id)."
    missing:
      - "Remove remaining request-time DEFAULT_PROJECT_ID/DEFAULT_USER_ID fallbacks from scoped handlers"
      - "Require and propagate X-Project-Id + X-User-Id for evidence/judgment flows"
  - truth: "Write-path strictification is complete for pilot + expanded routes"
    status: partial
    reason: "Pilot routes are strictified, but additional write paths still bypass strict scope contract."
    artifacts:
      - path: "backend/main.py"
        issue: "Pilot write routes (/ledger mutations, /attachments mutations, /opinions/events) are strict, but /claims/{claim_id}/evidence/decisions/events still assigns DEFAULT_USER_ID and allows project fallback."
      - path: "backend/main.py"
        issue: "Other mutation-like routes (e.g. /claims/confirm, evidence selection/judgment writes) remain unscoped."
    missing:
      - "Strictify remaining write endpoints under activity/evidence/judgment surfaces"
      - "Enforce fail-closed identity semantics consistently (no default actor assignment)"
  - truth: "Project membership list/create/select and active project persistence are wired end-to-end"
    status: failed
    reason: "Backend membership APIs exist, but frontend does not use them for picker/list/create/select or active-project restore."
    artifacts:
      - path: "backend/main.py"
        issue: "Endpoints exist (/projects, /projects/select, /projects/active)."
      - path: "frontend/ui.py"
        issue: "Scope selector is free-text User ID/Project ID; no project list/create/select integration."
      - path: "frontend"
        issue: "No client wiring found for /projects* endpoints."
    missing:
      - "Add frontend client + UI flow for list/create/select"
      - "Hydrate applied scope from backend active project truth on load"
  - truth: "Opinion visibility ACL semantics (private/selectable/public) are fully enforced"
    status: partial
    reason: "Visibility normalization and basic ACL filtering exist, but selectable/group semantics are not fully modeled/enforced."
    artifacts:
      - path: "backend/spine/opinion_events.py"
        issue: "ACL uses owner/public/selectable checks only; group_id/mode are stored but not enforced in filters."
      - path: "backend/main.py"
        issue: "Read routes pass viewer context and membership, but no group/mode evaluation path is present."
    missing:
      - "Implement and enforce explicit selectable/group membership semantics"
      - "Document and enforce mode/group policy at read/write boundaries"
---

# Phase 10-04.5-02 Verification Report

**Phase Goal:** Make `uid + project_id` mandatory before activity reads/writes, remove silent `default/local` fallback behavior, and complete scope UX + visibility policy stitching through PR-10.
**Verified:** 2026-03-02T08:14:17Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | uid+project scope is mandatory end-to-end across frontend/backend | ✗ FAILED | Backend still has fallback paths (`backend/main.py` ~5230/5296/5379/5536/5620-5621). Frontend evidence/judgment clients do not enforce/send full strict scope identity. |
| 2 | Write-path strictification complete for pilot + expanded routes | ✗ FAILED (partial) | Pilot is strict (`/ledger/*`, `/attachments/*` mutations, `/opinions/events`), but evidence/judgment write surfaces remain non-strict or defaulted. |
| 3 | Project membership/list/create/select + active project persistence works end-to-end | ✗ FAILED | Backend `/projects*` endpoints + membership spine helper exist; frontend has no `/projects*` wiring and uses manual text scope entry. |
| 4 | Frontend scope selector apply/switch gating is implemented | ✓ VERIFIED | `frontend/ui.py` has Apply/Switch block and gated panes (`render_scope_selector_block`, `has_applied_scope` checks around ~5671 and ~5851). |
| 5 | Opinion visibility ACL (private/selectable/public) fully enforced | ✗ FAILED (partial) | Visibility normalization + ACL clause exist, but selectable/group/mode semantics are not fully enforced (`backend/spine/opinion_events.py`). |
| 6 | No blocking deferred items remain for PR-10 completion | ✗ FAILED | Multiple blocking gaps remain (scope fallback removal, frontend project membership wiring, strictification completion). |

**Score:** 1/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `backend/main.py` | Canonical strict scope enforcement without fallback for scoped activity paths | ⚠️ ORPHANED/PARTIAL | Strict helper exists and is wired in many routes, but several scoped/evidence paths still fallback/default. |
| `backend/spine/project_membership.py` | Membership + active project persistence source of truth | ✓ VERIFIED | list/create/select/active primitives implemented and used by `/projects*` endpoints. |
| `frontend/ui.py` | Scope selector with apply/switch gating + project list/create/select | ⚠️ ORPHANED/PARTIAL | Apply/Switch + gating exists; project picker/list/create/select not wired. |
| `frontend/evidence_api.py` | Strict scoped calls for evidence paths | ✗ STUB/PARTIAL | Uses `project_id` fallback to `default`; no strict identity header contract for core evidence calls. |
| `frontend/judgment_api.py` | Strict scoped calls for judgment paths | ✗ STUB/PARTIAL | No project/user scope headers on judgment calls. |
| `backend/spine/opinion_events.py` | Full private/selectable/public ACL semantics | ⚠️ PARTIAL | Basic ACL/filtering implemented; group/mode semantics not enforced. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `frontend/ui.py` | `frontend/scope_lock.py` | `scope_lock.apply_draft_scope()` and `has_applied_scope()` | WIRED | Apply/Switch updates applied scope; workspace panes gated. |
| `frontend/ui.py` | backend `/projects*` APIs | API client calls | NOT_WIRED | No frontend `/projects`, `/projects/select`, `/projects/active` usage found. |
| `frontend/evidence_api.py` | backend claims evidence/decisions routes | request headers/params | PARTIAL | Uses reviewer params; does not enforce strict scope identity headers and defaults project. |
| `backend/main.py` opinion routes | `backend/spine/opinion_events.py` ACL filters | `viewer_uid/viewer_is_project_member` threading | PARTIAL | Viewer context passed; ACL does not enforce group/mode semantics. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| User ask #1 | Verification request | uid+project scope wiring completeness frontend/backend | ✗ BLOCKED | Defaults/fallbacks remain in backend + frontend evidence/judgment clients unscoped. |
| User ask #2 | Verification request | write-path strictification pilot + expanded routes | ? PARTIAL | Pilot strictification done; expanded coverage incomplete for evidence/judgment writes. |
| User ask #3 | Verification request | membership/list/create/select + active persistence | ✗ BLOCKED | Backend complete; frontend integration missing. |
| User ask #4 | Verification request | scope selector apply/switch gating frontend | ✓ SATISFIED | Implemented and wired in `frontend/ui.py` + `frontend/scope_lock.py`. |
| User ask #5 | Verification request | opinion ACL enforcement private/selectable/public | ? PARTIAL | Visibility normalization + basic ACL present; selectable/group semantics incomplete. |
| User ask #6 | Verification request | identify remaining blockers/deferred items | ✓ SATISFIED | Blockers enumerated in gaps section. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `backend/main.py` | ~5230, ~5296 | `x_project_id ... or DEFAULT_PROJECT_ID` fallback | 🛑 Blocker | Scoped reads still silently default project. |
| `backend/main.py` | ~5620-5621 | fallback project + `user_id = DEFAULT_USER_ID` in evidence decision write route | 🛑 Blocker | Write actor identity can default instead of fail-closed. |
| `frontend/evidence_api.py` | 14-19 | `project_id` fallback to `"default"` | 🛑 Blocker | Client can issue requests under implicit default scope. |
| `frontend/judgment_api.py` | 103-139 | unscoped judgment requests | ⚠️ Warning | Judgment flows bypass strict scope contract. |

### Human Verification Required

### 1. Scope persistence across fresh browser sessions

**Test:** Open fresh session, verify active user/project hydrates from backend membership active project, not local defaults.
**Expected:** Previously selected active project for user is restored; no default/local silent fallback.
**Why human:** End-to-end UI/session behavior is not fully inferable from static code alone.

### 2. Opinion ACL semantics for selectable/group behavior

**Test:** With multiple members in same project and differing group context, verify `private/selectable/public` visibility matrix.
**Expected:** private owner-only, selectable limited by intended group/member rules, public visible per policy.
**Why human:** Intended business semantics for group/mode are not fully encoded in a machine-verifiable contract here.

### Gaps Summary

This rollout is **partial**, not complete through PR-10. Core guardrails exist (canonical scope guard, pilot strictification, scope apply/switch UI gating, backend project-membership APIs), but full goal achievement is blocked by: (1) remaining backend default/fallback paths, (2) unscoped/partially scoped evidence+judgment frontend/backend paths, (3) missing frontend wiring for `/projects` list/create/select/active persistence, and (4) incomplete selectable/group ACL semantics in opinion visibility enforcement.

---

_Verified: 2026-03-02T08:14:17Z_
_Verifier: Claude (gsd-verifier)_

---

## Re-Verification (post PR-11 blocker closure)

**Verified:** 2026-03-02T08:58:00Z
**Status:** mostly_verified
**Score:** 5/6 truths verified (1 deferred)

### Updated Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | uid+project scope is mandatory end-to-end across frontend/backend | ✓ VERIFIED | Strict headers now required for remaining evidence/judgment and citation context/graph fallback routes; frontend evidence/judgment clients fail-fast and send strict identity headers. |
| 2 | Write-path strictification complete for pilot + expanded routes | ✓ VERIFIED | Evidence decision writes and judgment writes now require explicit scope identity and fail closed on missing/mismatched identities. |
| 3 | Project membership/list/create/select + active project persistence works end-to-end | ✓ VERIFIED | Scope selector now lists/creates/selects projects via `/projects*` APIs and applies selected active project in scope flow. |
| 4 | Frontend scope selector apply/switch gating is implemented | ✓ VERIFIED | Existing apply/switch lock remains and now integrates membership project list/create/select/active. |
| 5 | Opinion visibility ACL (private/selectable/public) fully enforced | ⚠️ PARTIAL (safe fail-closed) | Selectable now fails closed without group ACL context and requires explicit group ACL inputs to allow; full shared group resolver integration still deferred. |
| 6 | No blocking deferred items remain for PR-10 completion | ✓ VERIFIED (with non-blocking defer) | Remaining defer is non-blocking hardening: integrating canonical group ACL source for positive selectable sharing semantics. |

### Re-Verification Test Gate

- `pytest tests/test_scope_strict_remaining_routes_api.py tests/test_evidence_decisions_api.py tests/test_scope_write_pilot_api.py` ✅
- `pytest tests/test_frontend_scope_client_guards.py tests/test_evidence_store.py tests/test_judgment_frontend_store.py` ✅
- `pytest tests/test_project_api_scope_headers.py tests/test_frontend_scope_selector_lock.py tests/test_project_membership_api.py tests/test_project_scope_api.py` ✅
- `pytest tests/test_opinion_visibility_acl_api.py` ✅
