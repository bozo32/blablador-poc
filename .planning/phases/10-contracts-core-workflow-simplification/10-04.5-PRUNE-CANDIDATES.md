# 10-04.5 Prune Candidates Backlog

This file consolidates prune candidates captured during 10-04.5 execution.

Checklist for each candidate:
- [ ] Confirm candidate really has no callers (`rg`)
- [ ] Remove candidate
- [ ] Run `bash scripts/dev/verify_10_04_5_01_intake.sh`
- [ ] Manual UI smoke: Intake + Source Inbox + assignment

## 10-04.5-01-01

- path: `backend/main.py`
  symbol: `POST /attachments` + `POST /claims/{claim_id}/attachments`
  why: legacy `local_path`-based attachment creation; web UI should prefer bytes-only uploads
  replaced_by: `POST /attachments/upload`
  safe_to_delete_after: 10-04.5 refactor pass once UI intake drop uses bytes-only uploads end-to-end
  confidence: medium

- path: `backend/attachment_store.py`
  symbol: `create_attachment(local_path=...)`
  why: couples attachment creation to filesystem paths; should become optional/legacy once all clients use byte uploads
  replaced_by: `create_attachment_from_bytes(...)`
  safe_to_delete_after: 10-04.5 refactor pass + verification that no callers depend on local_path
  confidence: medium


## 10-04.5-01-02

1. `frontend/ui.py` - legacy "Upload documents" expander upload handler
   - why: unified Intake is now the primary entrypoint
   - replaced_by: `render_intake_panel()` dropzone
   - safe_to_delete_after: 10-04.5-01-06 verifier script passes + UI smoke
   - confidence: high

2. `frontend/ui.py` - hidden "Advanced: cited source uploads" uploader
   - why: unified Intake should be the only upload entrypoint
   - replaced_by: Intake -> Source routing + Source Inbox
   - safe_to_delete_after: 10-04.5-01-05 completes Source Inbox UI + byte upload path
   - confidence: medium


## 10-04.5-01-03

1. `backend/main.py` - route `POST /attachments` (`create_global_attachment`)
   - why: legacy local-path upload path; does not enforce X-Project-Id and likely superseded by `/attachments/upload`
   - replaced_by: `POST /attachments/upload`
   - safe_to_delete_after: 10-04.5 refactor pass once callers are confirmed migrated
   - confidence: medium

2. `backend/main.py` - route `POST /claims/{claim_id}/attachments` (`create_claim_attachment`)
   - why: legacy local-path upload path; does not accept X-Project-Id; likely unused in unified Intake flow
   - replaced_by: `POST /attachments/upload` + `POST /attachments/{id}/clone` (placed row)
   - safe_to_delete_after: 10-04.5 refactor pass once UI/clients no longer call it
   - confidence: low

3. `backend/attachment_store.py` - functions `set_archived`, `set_placement`
   - why: unscoped helpers; `/attachments/*` handlers now use `*_for_project` variants
   - replaced_by: `set_archived_for_project`, `set_placement_for_project`
   - safe_to_delete_after: after confirming no remaining call sites require unscoped updates
   - confidence: medium


## 10-04.5-01-04

1. path: `backend/main.py`
   symbol: `create_claim_attachment` (`POST /claims/{claim_id}/attachments`)
   why: Legacy request model includes `local_path` (UI-local filesystem dependency); now dev-gated.
   replaced_by: bytes upload via `POST /attachments/upload` (plus follow-up to associate to claim)
   safe_to_delete_after: follow-up plan verifying web UI never calls local_path endpoints
   confidence: medium

2. path: `backend/main.py`
   symbol: `create_global_attachment` (`POST /attachments`)
   why: Legacy request model includes `local_path`; now dev-gated.
   replaced_by: `POST /attachments/upload`
   safe_to_delete_after: follow-up plan verifying no local_path clients remain + tests updated
   confidence: high

3. path: `backend/schemas.py`
   symbol: `AttachmentCreateRequest.local_path`, `AttachmentGlobalCreateRequest.local_path`
   why: Only needed for legacy local_path endpoints; should disappear once those routes are removed.
   replaced_by: form/multipart upload fields on `POST /attachments/upload`
   safe_to_delete_after: prune candidates 1-2 are deleted and API clients are updated
   confidence: medium


## 10-04.5-01-05

1. `frontend/attachment_queue.py` - legacy placement helper `place_attachment()` uses `PATCH /attachments/{id}`
   - why: mutates the attachment row; global sources should be assigned via clone
   - replaced_by: `/attachments/{id}/clone` call sites (requested works + Source Inbox assignment)
   - safe_to_delete_after: `bash scripts/dev/verify_10_04_5_01_intake.sh` passes and no callers remain
   - confidence: medium

2. `frontend/ui.py` - older Source Bin surfaces (pre-Intake) now largely redundant
   - why: Intake + Source Inbox are the intended entrypoints
   - replaced_by: `render_intake_panel()` + `render_sources_panel()`
   - safe_to_delete_after: 10-04.5 refactor pass
   - confidence: low
