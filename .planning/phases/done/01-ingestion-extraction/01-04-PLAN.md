---
phase: 01-ingestion-extraction
plan: 04
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - frontend/ingestion_api.py
  - frontend/ui.py
autonomous: true

must_haves:
  truths:
    - "User can upload a PDF from the UI and see it listed"
    - "User can view extracted metadata, citations, and resolution status"
    - "User can trigger extraction and resolution from the UI"
  artifacts:
    - path: "frontend/ingestion_api.py"
      provides: "HTTP helpers for ingestion endpoints"
    - path: "frontend/ui.py"
      provides: "UI workflow for ingestion + extraction display"
  key_links:
    - from: "frontend/ui.py"
      to: "/ingest"
      via: "upload_pdf helper"
      pattern: "upload_pdf"
    - from: "frontend/ui.py"
      to: "/ingest/{doc_id}/extract"
      via: "trigger_extraction helper"
      pattern: "extract"
    - from: "frontend/ui.py"
      to: "/ingest/{doc_id}/resolve"
      via: "trigger_resolution helper"
      pattern: "resolve"
---

<objective>
Expose PDF ingestion and extraction results in the Streamlit UI.

Purpose: Let users upload PDFs, run extraction, and inspect citations in-app.
Output: Streamlit ingestion UI wired to backend ingestion/extraction endpoints.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/ui.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ingestion API helpers for the UI</name>
  <files>frontend/ingestion_api.py</files>
  <action>
    Create `frontend/ingestion_api.py` with request helpers:
    - `upload_pdf(api_url, file)` returns the created ingestion record.
    - `list_documents(api_url)` returns ingestion list.
    - `get_document(api_url, doc_id)` returns document detail.
    - `trigger_extraction(api_url, doc_id)` calls the extraction endpoint.
    - `trigger_resolution(api_url, doc_id)` calls the resolution endpoint.
    Use `requests` with clear error handling and surface HTTP errors as
    `RuntimeError` with response text.
  </action>
  <verify>python -c "from frontend.ingestion_api import upload_pdf; print(upload_pdf)"</verify>
  <done>Reusable ingestion API helpers are available for Streamlit UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add PDF ingestion UI workflow</name>
  <files>frontend/ui.py</files>
  <action>
    Extend `frontend/ui.py` to include a PDF ingestion section:
    - Add a sidebar block for PDF upload (single or multi-file) that calls
      `upload_pdf` and refreshes a `ingested_docs` cache in session state.
    - Add a selectbox or list to pick the active document.
    - Provide buttons to `Run Extraction` and `Resolve References` that call
      the ingestion API helpers and refresh the document detail.
    - Render metadata, citations, and bibliography entries in the main panel
      using expanders or tables. Include resolution fields (DOI/confidence)
      when available.

    Keep existing segmentation UI intact; place ingestion UI ahead of the
    segmentation flow so users can start with PDFs.
  </action>
  <verify>streamlit run frontend/ui.py</verify>
  <done>UI supports upload, extraction, and resolution viewing for PDFs.</done>
</task>

</tasks>

<verification>
- Upload a PDF in the UI and confirm it appears in the document list.
- Click extraction and resolution actions; verify metadata and DOI fields render.
</verification>

<success_criteria>
- Streamlit UI provides an end-to-end ingestion and extraction view for Phase 1.
- Users can trigger extraction/resolution without leaving the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-extraction/01-04-SUMMARY.md`
</output>
