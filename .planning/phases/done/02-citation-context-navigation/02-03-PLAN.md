---
phase: 02-citation-context-navigation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/citation_graph.py
  - frontend/ingestion_api.py
  - frontend/ui.py
autonomous: true
gap_closure: true
user_setup:
  - service: openalex
    why: "External citation graph expansion"
    env_vars:
      - name: OPENALEX_API_KEY
        source: "OpenAlex dashboard -> API keys"
must_haves:
  truths:
    - "User can open citation context for a selected callout"
    - "User can follow a citation to load cited context metadata in the validation pane"
    - "User can browse a citation tree of referenced and cited-by works when available"
  artifacts:
    - path: "backend/main.py"
      provides: "Citation graph endpoint wires OpenAlex expansion"
    - path: "backend/citation_graph.py"
      provides: "Depth-aware citation graph expansion with cited-by nodes"
    - path: "frontend/ingestion_api.py"
      provides: "Citation graph API helper supporting DOI/OpenAlex identifiers"
    - path: "frontend/ui.py"
      provides: "Citation graph UI passes DOI and renders cited-by nodes"
  key_links:
    - from: "backend/main.py"
      to: "backend/citation_graph.py"
      via: "build_citation_graph"
      pattern: "build_citation_graph"
    - from: "frontend/ui.py"
      to: "/ingest/{doc_id}/citation-graph"
      via: "get_citation_graph with doi"
      pattern: "citation-graph"
---

<objective>
Close citation graph gaps so OpenAlex expansion powers depth-aware cited-by trees.

Purpose: Restore NAV-02/NAV-03 by wiring OpenAlex graphs and cited-by nodes into the API/UI.
Output: Updated backend graph endpoint + frontend request wiring for DOI-based expansion.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-citation-context-navigation/02-citation-context-navigation-VERIFICATION.md
@.planning/phases/02-citation-context-navigation/02-01-SUMMARY.md
@.planning/phases/02-citation-context-navigation/02-02-SUMMARY.md
@backend/main.py
@backend/citation_graph.py
@frontend/ingestion_api.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OpenAlex expansion into citation graph endpoint</name>
  <files>backend/main.py</files>
  <action>
Update `/ingest/{doc_id}/citation-graph` to prefer OpenAlex expansion when a DOI or OpenAlex work ID is available. Use the resolved DOI from the document resolution data (or the `doi` query param if provided) to call `citation_graph.build_citation_graph(depth, max_nodes)`; if no DOI/OpenAlex ID is available, fall back to `build_local_citation_graph` as today. Keep existing error handling (translate OpenAlex failures to 502) and preserve the response shape.
  </action>
  <verify>curl "http://localhost:8000/ingest/{doc_id}/citation-graph?target_id={id}&depth=2&max_nodes=10&doi=10.0000/example" returns nodes with relation values including cited_by when OpenAlex data exists.</verify>
  <done>Graph endpoint selects OpenAlex expansion when DOI/OpenAlex ID is present and still returns local fallback when missing.</done>
</task>

<task type="auto">
  <name>Task 2: Pass resolved DOI into citation graph requests</name>
  <files>frontend/ingestion_api.py, frontend/ui.py</files>
  <action>
Extend `get_citation_graph` to accept an optional `doi` parameter and include it in query params when present. In the citation graph UI, pass the resolved DOI (or OpenAlex ID if available in resolution metadata) into `get_citation_graph` so the backend can build OpenAlex graphs; keep the depth and max_nodes controls as-is. Do not change the graphviz rendering logic; it already supports cited-by edges via relation coloring.
  </action>
  <verify>Run `streamlit run frontend/ui.py`, load a citation graph with a resolved DOI, and confirm the request URL includes `doi=` and the graph shows cited-by edges when OpenAlex returns them.</verify>
  <done>UI requests include DOI/OpenAlex identifiers and cited-by nodes render when available.</done>
</task>

</tasks>

<verification>
- `curl http://localhost:8000/ingest/{doc_id}/citation-graph?target_id={id}&depth=2&max_nodes=10&doi=10.0000/example` returns nodes/edges including cited-by relations when OpenAlex data exists.
- Streamlit citation graph renders referenced and cited-by nodes when a resolved DOI is present.
</verification>

<success_criteria>
- Citation graph endpoint returns cited-by nodes (or stub nodes) when OpenAlex data exists.
- Depth setting changes the OpenAlex graph expansion depth in the response.
- UI passes DOI/OpenAlex identifiers so cited-by nodes appear in the graph when available.
</success_criteria>

<output>
After completion, create `.planning/phases/02-citation-context-navigation/02-03-SUMMARY.md`.
</output>
