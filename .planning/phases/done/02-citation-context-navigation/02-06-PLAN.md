---
phase: 02-citation-context-navigation
plan: 06
type: execute
wave: 2
depends_on: ["02-05"]
files_modified:
  - backend/schemas.py
  - frontend/ui.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Mismatch resolution UI shows the citing bibliography author/year/title alongside candidate selections"
    - "Mismatch resolution UI flags when consolidated bibliography metadata is missing"
  artifacts:
    - path: "backend/schemas.py"
      provides: "Citation context reference schema includes consolidated grobid metadata"
      contains: "class CitationContextReference"
    - path: "frontend/ui.py"
      provides: "Mismatch resolution UI renders citing bibliography summary and consolidation status"
      contains: "Resolution needs review"
  key_links:
    - from: "backend/main.py"
      to: "backend/schemas.py"
      via: "response_model filtering for citation context"
      pattern: "CitationContextResponse"
    - from: "frontend/ui.py"
      to: "citation context response"
      via: "reference/resolution grobid metadata"
      pattern: "citation_follow_open"
---

<objective>
Expose consolidated bibliography metadata in the mismatch resolution UI so reviewers see citing-entry context and can spot missing consolidation data.

Purpose: Provide clear context for mismatch decisions and highlight references that lack consolidated metadata.
Output: Citation context schema passes grobid data and the Streamlit mismatch UI renders the entry + warning.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-citation-context-navigation/02-05-SUMMARY.md
@backend/schemas.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose consolidated bibliography metadata in citation context schema</name>
  <files>backend/schemas.py</files>
  <action>
Add a `grobid` field to `CitationContextReference` so citation context responses can include consolidated bibliography metadata (title/authors/year). Keep the field optional and avoid changing endpoint logic; rely on existing reference entries already carrying `grobid` data.
  </action>
  <verify>python -m compileall backend</verify>
  <done>Citation context responses can include `reference.grobid` with author/year/title when available.</done>
</task>

<task type="auto">
  <name>Task 2: Show citing bibliography summary and missing consolidation flag in mismatch UI</name>
  <files>frontend/ui.py</files>
  <action>
In the mismatch/needs_review block, render a citing bibliography summary derived from `reference.grobid` (fallback to `resolution.grobid` and `reference.raw_reference`). Format as “Author1; Author2 (Year) — Title”. If the grobid metadata is missing or empty (no title/authors/year), surface a visible warning or caption stating consolidated metadata is missing. Keep the candidate select and apply behavior unchanged.
  </action>
  <verify>python -m compileall frontend</verify>
  <done>Mismatch UI shows the citing bibliography entry next to candidate selection and flags missing consolidation data.</done>
</task>

</tasks>

<verification>
- `python -m compileall backend frontend`
</verification>

<success_criteria>
- Mismatch resolution UI displays citing bibliography author/year/title alongside candidate options.
- Missing consolidated metadata is clearly flagged for references without grobid details.
- Citation context payload continues to include resolution candidates and selection workflow unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-citation-context-navigation/02-06-SUMMARY.md`
</output>
