---
phase: 03-claim-selection-editing
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/claim_queue.py
  - frontend/ui.py
autonomous: true

must_haves:
  truths:
    - "Right pane shows a claim queue ordered oldest-first"
    - "Each queued claim displays the citing sentence and candidate claims"
    - "User can select a candidate claim for a queued item"
    - "Confirm parsing marks a claim as processed with a green status indicator"
  artifacts:
    - path: "frontend/claim_queue.py"
      provides: "Queue rendering helpers with status handling and claim selection state"
    - path: "frontend/ui.py"
      provides: "Claim queue section in the right pane"
  key_links:
    - from: "frontend/ui.py"
      to: "frontend/claim_queue.py"
      via: "render_claim_queue"
      pattern: "render_claim_queue"
    - from: "frontend/claim_queue.py"
      to: "st.session_state"
      via: "status update on confirm"
      pattern: "claim_status"
    - from: "frontend/claim_queue.py"
      to: "st.session_state"
      via: "selected claim tracking"
      pattern: "claim_selection"
---

<objective>
Render the claim queue in the right pane with status indicators and parsing confirmation actions.

Purpose: Make candidate claims visible and actionable in the claim review workflow.
Output: Streamlit UI for claim queue panels, including pending/processed states.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claim-selection-editing/03-CONTEXT.md
@.planning/phases/03-claim-selection-editing/03-RESEARCH.md
@frontend/ui.py
@frontend/claim_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build claim queue rendering with status controls</name>
  <files>frontend/claim_queue.py</files>
  <action>
Add Streamlit rendering helpers for the claim queue using `st.expander` panels.

- Implement `render_claim_queue()` that iterates through `st.session_state.claim_queue` oldest-first and renders one expander per claim entry.
- Compose expander headers as "{Author} ({Year})" or fallback to the callout text, prefixing a status dot (gray pending, green processed) using HTML/CSS in `st.markdown`.
- Inside each expander, show the full citing sentence and a read-only list of candidate claims (use the current edited text if present, otherwise originals).
- Add a per-claim selection control (radio/selectbox) that stores the chosen candidate index in `st.session_state.claim_selection[queue_id]` (or similarly named map) using stable widget keys.
- Add a "Confirm parsing" button that sets the claim status to processed and updates the default expanded state to collapsed on subsequent reruns.
- Avoid nested expanders and avoid heavy computation inside the expander body.
  </action>
  <verify>python -m py_compile frontend/claim_queue.py</verify>
  <done>Queued items render as expandable panels with selectable candidate claims and confirm buttons that mark them processed.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the claim queue in the right pane</name>
  <files>frontend/ui.py</files>
  <action>
Insert the claim queue section below the citation graph in the right pane.

- Import and call `render_claim_queue()` after the citation graph block inside `draw_ingestion_panel`.
- Ensure the queue section is visually anchored at the bottom of the right pane (use `st.divider()` and a heading such as "Claim Queue").
- When citation context loads, update any queued item with reference metadata (author/year) so queue headers are meaningful.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>The right pane shows the claim queue with status indicators and confirm actions for each queued claim.</done>
</task>

</tasks>

<verification>
- Run `streamlit run frontend/ui.py`, click multiple callouts, and confirm the queue panels appear under the citation graph with pending/processed status indicators.
- Choose a candidate claim in a queue panel and confirm the selected claim is stored in `st.session_state` for that queue item.
- Click "Confirm parsing" and verify the status dot turns green and the panel collapses by default on rerun.
</verification>

<success_criteria>
- Users can see queued claims, select a candidate claim, inspect candidate segments, and mark them processed from the right pane.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claim-selection-editing/03-02-SUMMARY.md`
</output>
