---
phase: 04-evidence-attachment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/attachment_queue.py
  - frontend/ui.py
  - frontend/claim_queue.py
  - frontend/assets/attachment_panel.css
autonomous: false

must_haves:
  truths:
    - "Claims show an accessible drag/drop surface with keyboard fallback for attaching cited PDFs"
    - "Dropped files enter a persistent queue panel with statuses (pending, converting, parsing, matched)"
    - "Attachment queue supports multi-file drops, auto-matches files to claims, and exposes manual reassign when ambiguous"
    - "A keyboard-accessible modal exists to attach/detach when drag-and-drop is unavailable"
  artifacts:
    - path: "frontend/attachment_queue.py"
      provides: "Queue state management, drop handlers, auto-match heuristics, accessibility fallbacks"
    - path: "frontend/ui.py"
      provides: "Drag/drop affordance on claim cards, queue panel mounting, modal trigger"
    - path: "frontend/assets/attachment_panel.css"
      provides: "Styles for queue panel, drop states, status indicators"
  key_links:
    - from: "frontend/ui.py"
      to: "frontend/attachment_queue.py"
      via: "drop handlers + queue rendering"
      pattern: "attachment_queue"
    - from: "frontend/attachment_queue.py"
      to: "frontend/claim_queue.py"
      via: "auto-match attachments to claims"
      pattern: "auto_match_claim"
    - from: "frontend/ui.py"
      to: "frontend/assets/attachment_panel.css"
      via: "st.markdown CSS injection"
      pattern: "attachment-panel"
---

<objective>
Build the attachment queue UX: drag/drop surfaces on claims, persistent queue panel with status tracking, auto-matching, and accessibility fallbacks per CONTEXT decisions.

Purpose: Fulfill EVD-02 UX requirements so reviewers can drop cited PDFs, see queued processing, and manage attachments even without drag-and-drop.
Output: Attachment queue state/helpers, Streamlit UI wiring for drop surfaces and modal fallback, plus visual styling for queue panel + statuses.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evidence-attachment/04-CONTEXT.md
@.planning/phases/04-evidence-attachment/04-RESEARCH.md
@frontend/ui.py
@frontend/claim_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attachment queue state + drop handlers</name>
  <files>frontend/attachment_queue.py, frontend/claim_queue.py</files>
  <action>
Create queue management utilities and auto-matching logic per phase context.

- New module `frontend/attachment_queue.py` manages `st.session_state.attachment_queue`, statuses (`pending`, `converting`, `parsing`, `matched`, `error`), and metadata (doc_id, reference_id, filenames, local temp paths, claim targets, history logs).
- Implement `init_attachment_queue_state()`, `handle_drop(claim_id, files)`, `enqueue_files(files, source)` for multi-file drops, `auto_match_queue_items()` that matches files to claims by reference metadata (author/year/DOI) via `claim_queue` data, and history logging limited to five events per claim.
- Provide `attach_to_claim(claim_id, queue_item_id)` and `detach_attachment(claim_id)` helpers that update queue state, honor shared attachment overrides, and record timeline events.
- Include accessibility-friendly keyboard fallback: `open_attachment_modal(claim_id)` helper to attach via file uploader when drag/drop isn't possible.
  </action>
  <verify>python -m py_compile frontend/attachment_queue.py frontend/claim_queue.py</verify>
  <done>Attachment queue helpers manage drop events, statuses, auto-matching, and modal fallback entry points integrated with claim queue data.</done>
</task>

<task type="auto">
  <name>Task 2: Render drop surfaces + queue panel UI</name>
  <files>frontend/ui.py, frontend/assets/attachment_panel.css</files>
  <action>
Wire drag/drop UI, queue panel, and accessibility fallback controls in Streamlit.

- Add CSS file for queue panel layout, status dots, dimming non-target claims, and persistent summary chip per CONTEXT instructions; inject via `st.markdown` when panel mounts.
- For each claim card, wrap the body in a drop zone: highlight on drag-over, dim other claims, and show skeleton state while parsing. Add keyboard focus + "Attach PDF" hotkey hooking into `open_attachment_modal`.
- Implement queue panel component (collapsible side/bottom pane) showing all queued files with statuses, inline progress indicators, and summary chip with error counts. Include inline conversion status icons and auto-collapse behavior after batch completion with persistent chip to reopen.
- Add manual reassignment UI for ambiguous matches (pop-up list to resolve), per CONTEXT requirement.
- Provide per-claim attach/detach actions referencing the queue state and ensuring multi-file drops first land in the queue before auto-match.
  </action>
  <verify>
1. Run `streamlit run frontend/ui.py`.
2. Drop multiple fake files (use Streamlit uploader) onto claims; confirm queue panel shows stacked files, statuses, summary chip, and manual reassignment flow.
3. Trigger keyboard fallback (modal) and attach/detach to ensure accessibility path works.
  </verify>
  <done>Claims expose drag/drop + modal fallback, queue panel persists across reruns with statuses, and summary chip reopens the panel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Attachment queue UX: drag/drop surfaces, queue panel, auto-matching indicators, accessibility fallback modal.</what-built>
  <how-to-verify>
    1. Run `streamlit run frontend/ui.py`.
    2. Drop 2-3 sample PDFs (or dummy files) onto different claims. Confirm other claims dim, target claim highlights, and queue panel lists files with pending → matched statuses.
    3. Collapse queue panel and verify summary chip shows counts + reopens on click.
    4. Use keyboard shortcut or "Attach PDF" modal to add a file without drag/drop; ensure it lands in the queue and can be assigned.
    5. Trigger manual reassignment for an ambiguous match and confirm the resolving pop-up appears.
    6. Detach an attachment and confirm the file leaves the queue and claim timeline logs the event.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming UX, or describe issues to address.</resume-signal>
</task>

</tasks>

<verification>
- Manual Streamlit testing per checkpoint instructions ensures drop surfaces, queue panel, and modal fallback behave as required.
</verification>

<success_criteria>
- Attachment queue fully functional with drag/drop + modal fallback, auto-matching status, and persistent panel/chip states ready for backend wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-attachment/04-02-SUMMARY.md`
</output>
