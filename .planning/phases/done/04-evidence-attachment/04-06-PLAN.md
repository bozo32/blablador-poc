---
phase: 04-evidence-attachment
plan: 06
type: execute
wave: 2
depends_on: ["04-04", "04-05"]
files_modified:
  - frontend/attachment_queue.py
  - frontend/claim_queue.py
  - frontend/ui.py
  - frontend/assets/attachment_panel.css
  - tests/test_claim_queue.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "New queue items automatically run matching heuristics; high-confidence matches attach themselves with timeline entries and toast feedback."
    - "Ambiguous attachments are flagged in the queue timeline and stay highlighted until a reviewer manually reassigns them."
    - "Queue panel exposes controls to reassign attachments (and logs the resolution), satisfying the manual-disambiguation requirement."
  artifacts:
    - path: "frontend/attachment_queue.py"
      provides: "Auto-match orchestration, ambiguous state tracking, and attach/detach helpers that update timelines."
    - path: "frontend/claim_queue.py"
      provides: "Matching heuristics + timeline helpers reused by the queue."
    - path: "frontend/ui.py"
      provides: "Queue UI that surfaces ambiguity alerts, dropdowns for reassignment, and buttons to confirm attachments."
  key_links:
    - from: "frontend/attachment_queue.py"
      to: "frontend/claim_queue.py"
      via: "auto_match_claim / record_timeline_event"
      pattern: "claim_queue\.(auto_match_claim|record_timeline_event)"
    - from: "frontend/ui.py"
      to: "frontend/attachment_queue.py"
      via: "attach_to_claim / auto_match_queue_items"
      pattern: "attachment_queue\.(attach_to_claim|auto_match_queue_items)"
---

<objective>
Restore auto-match + manual reassignment flow so multi-file drops land on the right claims and ambiguous files are resolvable (EVD-02 gap #3).

Purpose: Ensure attachments do not sit idle when heuristics could attach them, and provide reviewers UI tools to fix ambiguous matches.
Output: Queue logic + UI controls that auto-assign high-confidence files, flag low-confidence ones, and let reviewers reassign with timeline logging.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-evidence-attachment/04-CONTEXT.md
@.planning/phases/04-evidence-attachment/04-VERIFICATION.md
@frontend/claim_queue.py
@frontend/attachment_queue.py
@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement queue auto-match + ambiguity tracking</name>
  <files>frontend/attachment_queue.py, frontend/claim_queue.py</files>
  <action>
- Replace the `auto_match_queue_items` stub with logic that:
  - Iterates over queue items lacking `claim_id` (or explicitly marked ambiguous) and calls `claim_queue.auto_match_claim` to receive scored candidates.
  - Auto-attaches items whose top score ≥ configurable threshold (default 0.65) by calling `attach_to_claim(..., via="auto" , score=score)` and logging a queue + claim timeline event.
  - For low-confidence matches, store the top 3 candidates (id/score/metadata) as `item["ambiguous_matches"]`, mark `item["status"] = "pending"`, and record a history entry `ambiguous` so the UI can highlight them.
- Call `auto_match_queue_items` whenever new items are enqueued (including modal/global drops) and after backend sync so resumable items also get matched.
- Add helpers to mark ambiguity resolved when an item is manually reassigned, clearing the `ambiguous_matches` list and logging a history event.
  </action>
  <verify>1. Run `pytest tests/test_claim_queue.py::test_auto_match_claim` (add/extend fixture if needed) to ensure scoring helpers behave deterministically. 2. In a Streamlit session, drop a PDF without choosing a claim and confirm it auto-attaches (or becomes flagged ambiguous) based on filename metadata.</verify>
  <done>Queue items run heuristics automatically, attach themselves when confident, and record “ambiguous” events when confidence is low.</done>
</task>

<task type="auto">
  <name>Task 2: Add manual reassignment controls + alerts in the queue panel</name>
  <files>frontend/ui.py, frontend/attachment_queue.py, frontend/assets/attachment_panel.css</files>
  <action>
- Enhance `render_queue_item` to display:
  - A selectbox listing all available claims (label = callout + truncated claim text) pre-selecting the current attachment target.
  - Inline badges/tooltips when `ambiguous_matches` exist (e.g., “Attention: ambiguous match — select a claim below”), plus a button to accept the top candidate.
  - An “Assign to claim” button that calls a new `attachment_queue.assign_from_ui(queue_item_id, claim_id, *, via="manual", reason="reassigned")` helper, updates backend uploads if needed, and logs both queue + claim timelines.
- Highlight ambiguous queue cards (CSS class) and surface a summary warning at top of the queue panel listing how many items need attention.
- After manual assignment buttons run, trigger `attachment_queue.refresh_summary_counts()` and optionally rerun `auto_match_queue_items` to pick up remaining files.
  </action>
  <verify>1. `python -m streamlit run frontend/ui.py` → drop multiple PDFs; for at least one ambiguous case, confirm the queue card shows the warning + selectbox and that clicking “Assign” updates the claim + timeline. 2. Confirm the queue header shows a banner (e.g., “2 attachments need assignment”) until all ambiguous items are resolved.</verify>
  <done>Reviewers can resolve ambiguous attachments entirely from the queue panel, with visual alerts clearing as soon as reassignment occurs.</done>
</task>

</tasks>

<verification>
- Manual Streamlit run demonstrating auto-match + manual reassignment flow.
- Any updated unit tests for claim/queue helpers pass (`pytest tests/test_claim_queue.py` if expanded).
</verification>

<success_criteria>
- Auto-match heuristics run whenever files enter the queue, confident matches attach automatically, and ambiguous cases are highlighted until manually resolved via the queue UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-attachment/04-06-SUMMARY.md`
</output>
