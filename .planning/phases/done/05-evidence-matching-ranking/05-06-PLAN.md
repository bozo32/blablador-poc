---
phase: 05-evidence-matching-ranking
plan: 06
type: execute
wave: 2
depends_on: ["05-05"]
files_modified:
  - frontend/claim_queue.py
  - frontend/evidence_store.py
  - frontend/ui.py
  - frontend/attachment_queue.py
  - tests/test_evidence_store.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Selecting or rerunning a claim always sends claim_text so the initial evidence fetch succeeds without 409 errors."
    - "Attachment uploads include claim_text, enabling automatic reruns to execute immediately after parsing."
    - "The UI surfaces a clear prompt with a seeded rerun action when claim_text is missing so reviewers know how to proceed."
  artifacts:
    - path: "frontend/evidence_store.py"
      provides: "Metadata-backed claim_text propagation for sync + rerun calls"
    - path: "frontend/ui.py"
      provides: "Claim header + rerun controls wired to send claim_text and show remediation banner"
    - path: "frontend/attachment_queue.py"
      provides: "Attachment POST body containing claim_text for backend auto reruns"
    - path: "tests/test_evidence_store.py"
      provides: "Coverage for claim_text fallback + rerun queue metadata"
  key_links:
    - from: "frontend/ui.render_evidence_panel"
      to: "frontend.evidence_store.EvidenceStore.sync_for_claim"
      via: "claim_text parameter sourced from claim_queue record"
    - from: "frontend/attachment_queue.py::_upload_to_backend"
      to: "backend /claims/{id}/attachments"
      via: "JSON payload that now includes claim_text"
    - from: "claim_queue.record_timeline_event"
      to: "evidence_store.sync_for_claim"
      via: "metadata updates that keep claim_text cached for background refreshes"
---

<objective>
Ensure the frontend always ships claim_text alongside evidence requests, reruns, and attachment uploads, while providing a visible recovery path when claim_text is missing.

Purpose: Close verification gaps by removing silent failures in the UI and seeding the backend with the data it requires.
Output: Updated claim registry, store, UI wiring, attachment uploads, and regression tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evidence-matching-ranking/05-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist claim_text in store metadata and reuse it for sync/rerun calls</name>
  <files>frontend/claim_queue.py, frontend/evidence_store.py, tests/test_evidence_store.py</files>
  <action>
    - When claim records are registered or fetched, push <code>claim</code> text (plus callout + flags) into <code>EvidenceStore.ensure_claim_state(..., metadata=...)</code> so every claim has cached text/metadata.
    - Update <code>EvidenceStore.sync_for_claim</code>, <code>load_more</code>, <code>apply_filter</code>, <code>queue_rerun</code>, and helper entry points to automatically fall back to the stored metadata claim_text when a caller doesn't supply one; allow overrides for edited text.
    - Ensure background refresh paths (e.g., <code>claim_queue.record_timeline_event</code>) forward claim_text when triggering <code>sync_for_claim</code> so auto refreshes succeed.
    - Extend <code>tests/test_evidence_store.py</code> to cover metadata seeding, fallback behavior, and rerun requests that now attach claim_text.
  </action>
  <verify><code>uv run pytest tests/test_evidence_store.py</code></verify>
  <done>EvidenceStore always knows a claim’s text, and automated tests prove sync/rerun calls include it even when not explicitly provided.</done>
</task>

<task type="auto">
  <name>Task 2: Send claim_text from UI + attachment uploads and surface remediation prompt</name>
  <files>frontend/ui.py, frontend/attachment_queue.py</files>
  <action>
    - Update <code>render_evidence_panel</code> (and any other evidence entry points) to pass the active claim’s text into every <code>sync_for_claim</code>, <code>queue_rerun</code>, and <code>load_more</code> invocation; keep the store metadata in sync when claims change.
    - Augment rerun/refresh controls with a visible warning banner when the last evidence fetch failed due to missing claim_text (e.g., HTTP 409). Provide a button that resubmits the stored claim text (and optional edited text) so reviewers can fix the issue inline.
    - When attachments are uploaded (<code>_upload_to_backend</code>), include the claim’s text in the POST payload so backend auto reruns have the data they need immediately.
    - Add inline docstrings or comments describing how the new remediation flow works so future plan phases respect it.
  </action>
  <verify>
Run <code>streamlit run app.py</code> (or existing frontend entry point) and select a claim lacking runs; verify the warning + "Send claim text" button appears, then exercise attachment upload for the same claim to confirm claim_text travels over the network.
  </verify>
  <done>UI interactions never silently fail for missing claim text, and attachment uploads now seed backend auto reruns without additional input.</done>
</task>

</tasks>

<verification>
- Execute <code>uv run pytest tests/test_evidence_store.py</code> to cover the new metadata plumbing.
- Smoke test the Streamlit UI: select a claim with no evidence runs, observe the claim-text prompt, click the seeded rerun button, and confirm the evidence list loads without 409 errors. Drop an attachment onto the same claim to ensure the backend logs show <code>claim_text</code> in the payload.
</verification>

<success_criteria>
- The evidence panel immediately issues a successful fetch for new claims because claim_text is always provided.
- Attachment uploads pass claim_text to the backend, enabling automatic reruns right after parsing.
- Reviewers see a clear action item (button/input) when claim_text is missing instead of a silent failure.
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-matching-ranking/05-06-SUMMARY.md`.
</output>
