---
phase: 09-claim-graph-consensus-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/project_io.py
  - backend/schemas.py
  - backend/main.py
autonomous: true

must_haves:
  truths:
    - "Project metadata persists a per-project reviewer list and active reviewer"
    - "Reviewer names are normalized (trim + collapsed whitespace, case-insensitive uniqueness)"
    - "Active reviewer survives restart via data/project.json"
  artifacts:
    - path: backend/project_io.py
      provides: "Read/write project metadata including reviewer fields"
    - path: backend/schemas.py
      provides: "ProjectMeta schema with reviewer fields"
    - path: backend/main.py
      provides: "GET/PUT /project exposes reviewer fields"
  key_links:
    - from: backend/main.py
      to: backend/project_io.py
      via: "GET/PUT /project"
      pattern: "read_project_meta|write_project_meta"
---

<objective>
Extend project metadata to support local reviewer identities (no auth) and persist an active reviewer selection.

Purpose: Multi-user consensus features require a stable, per-project reviewer list and a durable "current user".
Output: Reviewer-aware ProjectMeta schema + updated /project endpoints.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-claim-graph-consensus-viz/09-CONTEXT.md

@backend/project_io.py
@backend/schemas.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Add reviewer fields to ProjectMeta with normalization rules</name>
  <files>backend/schemas.py</files>
  <action>
1) Update `ProjectMeta` to include these optional fields with safe defaults:
   - `reviewers: list[str]` (default empty)
   - `active_reviewer_uid: str | None`
   - `compare_reviewer_a: str | None`, `compare_reviewer_b: str | None`
   - `graph_settings: dict` (default empty; used to persist hops/edge_cap/min_votes/provenance toggles)

2) Add validators so reviewer values are normalized:
   - trim
   - collapse internal whitespace to single spaces
   - treat uniqueness case-insensitively when validating `reviewers`

3) Ensure `ProjectMeta` remains backwards compatible with existing `data/project.json` that only has `name/version/created_at/updated_at`.
  </action>
  <verify>python -m py_compile backend/schemas.py</verify>
  <done>`schemas.ProjectMeta` accepts legacy project.json and emits reviewer fields when present.</done>
</task>

<task type="auto">
  <name>Persist reviewer fields in project.json and allow partial updates</name>
  <files>backend/project_io.py, backend/main.py</files>
  <action>
1) Update `backend/project_io.py`:
   - `read_project_meta()` preserves existing keys and returns a dict that can include reviewer fields.
   - Add a write/merge helper so callers can update reviewer fields without clobbering unknown future keys.
   - Enforce the reviewer normalization + uniqueness rules before writing.

2) Update `PUT /project` in `backend/main.py` so the client can update:
   - `name`
   - `reviewers` and `active_reviewer_uid`
   - `compare_reviewer_a/b` and `graph_settings`

Keep existing behavior working: sending only `{name}` should still succeed.
  </action>
  <verify>pytest -q</verify>
  <done>`GET /project` returns reviewer fields; `PUT /project` persists them and they survive restart.</done>
</task>

</tasks>

<verification>
- `pytest -q`
- Quick manual: start backend, `curl http://localhost:8000/project`, then `curl -X PUT http://localhost:8000/project -H 'Content-Type: application/json' -d '{"name":"demo","reviewers":["Alice","Bob"],"active_reviewer_uid":"Alice"}'`.
</verification>

<success_criteria>
- Project meta can store multiple reviewer names and an active reviewer.
- Reviewer name normalization prevents duplicates like `"Alice"` vs `" alice  "`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-01-SUMMARY.md`
</output>
