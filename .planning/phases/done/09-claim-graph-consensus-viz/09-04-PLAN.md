---
phase: 09-claim-graph-consensus-viz
plan: 04
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - frontend/project_api.py
  - frontend/judgment_api.py
  - frontend/judgment_store.py
  - frontend/ui.py
autonomous: true

must_haves:
  truths:
    - "A 'Current user' dropdown scopes all judgment saves/loads"
    - "User can add a new reviewer name and it becomes active immediately"
    - "Other reviewers' judgments are inspectable without switching the active reviewer"
  artifacts:
    - path: frontend/ui.py
      provides: "Reviewer picker UI + reviewer-scoped judgment UI"
    - path: frontend/project_api.py
      provides: "Project meta API client including reviewer fields"
    - path: frontend/judgment_api.py
      provides: "Judgment API client that passes reviewer_uid"
  key_links:
    - from: frontend/ui.py
      to: backend/main.py
      via: "reviewer_uid query param on judgment endpoints"
      pattern: "reviewer_uid"
---

<objective>
Add a local reviewer identity selector in the Streamlit workspace and wire judgments to save/load per active reviewer.

Purpose: Enable multi-user local review and set the foundation for compare mode.
Output: Reviewer picker + reviewer-scoped judgment interactions.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-claim-graph-consensus-viz/09-CONTEXT.md

@frontend/ui.py
@frontend/project_api.py
@frontend/judgment_api.py
@frontend/judgment_store.py
</context>

<tasks>

<task type="auto">
  <name>Update project_api to read/write reviewer fields</name>
  <files>frontend/project_api.py</files>
  <action>
1) Update `put_meta(...)` to accept a dict payload (not just name) so the UI can update reviewer fields:
   - reviewers
   - active_reviewer_uid
   - compare_reviewer_a/b
   - graph_settings

2) Keep backwards compatibility: existing callers that only pass a name should still work.
  </action>
  <verify>python -m py_compile frontend/project_api.py</verify>
  <done>Frontend can persist reviewer settings via `/project` without breaking name-only updates.</done>
</task>

<task type="auto">
  <name>Wire judgment_api + judgment_store to include reviewer_uid</name>
  <files>frontend/judgment_api.py, frontend/judgment_store.py</files>
  <action>
1) Update `frontend/judgment_api.py`:
   - `get_judgment(claim_id, reviewer_uid=...)`
   - `put_judgment(claim_id, payload, reviewer_uid=...)`
   - add `get_all_judgments(claim_id)` calling the new backend endpoint.

2) Update `frontend/judgment_store.py` caching so the cache key includes reviewer_uid (so switching reviewers doesn't show stale judgments).
  </action>
  <verify>python -m py_compile frontend/judgment_api.py</verify>
  <done>Judgment store can switch reviewers and fetch the correct judgment for each.</done>
</task>

<task type="auto">
  <name>Add Current user dropdown and Other reviewers section in UI</name>
  <files>frontend/ui.py</files>
  <action>
1) In the Project panel (left pane), add a compact "Current user" control:
   - select existing reviewers
   - add a new reviewer name inline
   - switch immediately and persist active reviewer via `PUT /project`

2) In the judgment panel:
   - ensure all save/load calls pass `active_reviewer_uid`
   - add an "Other reviewers" section that lists other reviewer_uids and their verdict/note preview for the same claim

3) Handle empty state:
   - if no reviewer exists, prompt to create one and do not allow save until active reviewer is set.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>User can create/switch reviewers; judgments save/load are scoped to the active reviewer and other reviewers' judgments are visible.</done>
</task>

</tasks>

<verification>
- `python -m py_compile frontend/ui.py`
- Manual: add two reviewers, save different verdicts for the same claim, switch back/forth and confirm values persist.
</verification>

<success_criteria>
- Active reviewer identity is visible and durable.
- Judgments are reviewer-scoped and disagreement is visible without overwrites.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-04-SUMMARY.md`
</output>
