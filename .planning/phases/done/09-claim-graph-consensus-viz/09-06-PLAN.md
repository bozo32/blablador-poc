---
phase: 09-claim-graph-consensus-viz
plan: 06
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - frontend/ui.py
autonomous: false

must_haves:
  truths:
    - "Compare mode shows side-by-side differences between two reviewers (judgments + edge votes)"
    - "Disagreements list can navigate/focus the relevant claim or edge"
    - "MVP remains usable for ~20 papers and multiple reviewer names"
  artifacts:
    - path: frontend/ui.py
      provides: "Compare mode drawer/panel + disagreements list navigation"
  key_links:
    - from: frontend/ui.py
      to: backend/main.py
      via: "GET /claims/{claim_id}/judgments and graph vote endpoints"
      pattern: "judgments|edge.*votes"
---

<objective>
Add compare mode and a single end-to-end verification checkpoint for multi-user consensus + claim graph exploration.

Purpose: Make disagreement easy to spot and confirm the phase goal is met in a realistic walkthrough.
Output: Compare UI + manual verification steps.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-claim-graph-consensus-viz/09-CONTEXT.md

@frontend/ui.py
</context>

<tasks>

<task type="auto">
  <name>Implement compare mode drawer and disagreements list</name>
  <files>frontend/ui.py</files>
  <action>
1) Add a Compare panel in the Graph tab:
   - two dropdowns for reviewer A and reviewer B
   - persist selection per project via `PUT /project` (compare_reviewer_a/b)

2) Compare surfaces:
   - Claim judgments: show verdict/notes differences for the currently selected claim and for any claim in the visible neighborhood.
   - Edge votes: show per-edge votes side-by-side, highlight conflicts (support vs contradict first).

3) Disagreements list:
   - includes missing-vs-present as a difference
   - sorts support-vs-contradict conflicts first
   - clicking an item focuses/selects the relevant node/edge in the graph and opens its inspection view.
  </action>
  <verify>python -m py_compile frontend/ui.py</verify>
  <done>Compare mode can pick two reviewers, show differences, and navigate to disagreements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Multi-user reviewer identity + per-user judgments + claim graph consensus UI (including compare mode)</what-built>
  <how-to-verify>
1) Start backend + Streamlit.
2) In Project panel, add 3 reviewer names and switch between them.
3) For the same claim, save different judgments under two reviewers; confirm switching reviewers shows the right value.
4) In Graph tab, select an edge and record opposing votes under two reviewers; confirm the edge consensus label/badge reflects the ratio.
5) Open Compare mode, select the two reviewers, and confirm the disagreements list includes your conflicting judgment/vote and can navigate/focus it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Manual checkpoint above
</verification>

<success_criteria>
- Two reviewers can disagree on both judgments and edge votes without overwrites.
- Graph supports interactive inspection and clearly shows consensus/disagreement.
- Compare mode highlights differences and navigates to the relevant items.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claim-graph-consensus-viz/09-06-SUMMARY.md`
</output>
