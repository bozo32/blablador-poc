---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - docker-compose.yml
  - backend/settings.py
autonomous: true

must_haves:
  truths:
    - "When running in Compose, the API can reach GROBID via an internal service URL."
    - "GROBID URL remains configurable for host-run (gradle) development."
  artifacts:
    - path: docker-compose.yml
      provides: "grobid service wired into stack"
    - path: backend/settings.py
      provides: "GROBID_URL default + env override"
  key_links:
    - from: docker-compose.yml
      to: backend/settings.py
      via: "GROBID_URL env in app-api"
      pattern: "GROBID_URL"
---

<objective>
Add GROBID as a Compose service and wire the API container to use it.

Purpose: Make extraction portable and remove host-specific GROBID setup.
Output: Compose includes `grobid`; API defaults to `http://grobid:8070` in containers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@docker-compose.yml
@backend/grobid_client.py
@backend/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grobid service to docker-compose</name>
  <files>docker-compose.yml</files>
  <action>
Extend `docker-compose.yml`:
- Add a `grobid` service using an upstream GROBID image (port 8070).
- Wire `app-api` environment `GROBID_URL=http://grobid:8070`.

Constraints:
- Keep `backend/settings.py` default `GROBID_URL` intact so host-run remains unchanged.
- Keep `GROBID_URL` configurable via env var.
  </action>
  <verify>
  docker compose up --build -d && curl -sf http://localhost:8070/api/isalive >/dev/null
  </verify>
  <done>GROBID container is reachable on localhost:8070 and `app-api` uses it by default in Compose.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit compose-friendly defaults (no behavior changes)</name>
  <files>backend/settings.py</files>
  <action>
If needed for clarity, update descriptions/comments in `backend/settings.py` so it is explicit that:
- `GROBID_URL` defaults to localhost for host-run dev.
- Compose overrides should point at `http://grobid:8070`.

Do not change extraction logic in `backend/grobid_client.py`.
  </action>
  <verify>python -c "from backend.settings import settings; print(settings.GROBID_URL)"</verify>
  <done>Settings remain backward compatible and Compose override is documented.</done>
</task>

</tasks>

<verification>
- `curl http://localhost:8070/api/isalive` returns 200.
- `docker compose logs app-api` shows successful GROBID calls during ingestion/extraction.
</verification>

<success_criteria>
- Compose stack includes GROBID; API can extract TEI without host-installed GROBID.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-02-SUMMARY.md`
</output>
