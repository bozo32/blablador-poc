---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 02
subsystem: infra
tags: [docker-compose, grobid, fastapi, pydantic-settings]

# Dependency graph
requires:
  - phase: 09.1-ingestion-replumbing-solid-spine
    provides: Compose baseline with app-api + app-ui (09.1-01)
provides:
  - GROBID runs as a Compose service and is reachable via internal DNS
  - app-api is configured in Compose to use `http://grobid:8070`
affects: [09.1-ingestion-replumbing-solid-spine, extraction, ingestion]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Service URLs are wired via env vars in Compose (e.g. GROBID_URL)"]

key-files:
  created: []
  modified: [docker-compose.yml, backend/settings.py]

key-decisions:
  - "None - followed plan as specified"

patterns-established:
  - "Compose uses internal service DNS names for inter-service calls (grobid:8070)"

# Metrics
duration: 7 min
completed: 2026-02-14
---

# Phase 9.1 Plan 02: Add GROBID Service Summary

**Compose now runs GROBID and the API container defaults to the internal URL `http://grobid:8070` via `GROBID_URL`.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-02-14T12:13:02Z
- **Completed:** 2026-02-14T12:20:21Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added a `grobid` service to `docker-compose.yml` exposing `8070:8070`
- Wired `app-api` to use `GROBID_URL=http://grobid:8070` in Compose
- Documented host-run default vs Compose override for `GROBID_URL`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add grobid service to docker-compose** - `2454c32` (feat)
2. **Task 2: Add explicit compose-friendly defaults (no behavior changes)** - `0ef7905` (docs)

**Plan metadata:** (added after execution)

## Files Created/Modified
- `docker-compose.yml` - Adds `grobid` service and sets `app-api` `GROBID_URL` env
- `backend/settings.py` - Clarifies `GROBID_URL` default (host-run) vs Compose override

## Decisions Made
None - followed plan as specified.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Freed port 8070 by stopping existing host GROBID**

- **Found during:** Task 1 (Add grobid service to docker-compose)
- **Issue:** `docker compose up` failed because port `8070` was already in use by a local Java process (host-run GROBID)
- **Fix:** Stopped the conflicting process so Compose could bind `8070:8070`
- **Files modified:** None
- **Verification:** `curl -sf http://localhost:8070/api/isalive` succeeded after Compose started
- **Committed in:** `2454c32` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Required to satisfy the plan's localhost:8070 verification; no scope creep.

## Issues Encountered
- Port `8070` was already in use locally; resolved by stopping the conflicting process.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Compose extraction is portable; ready for `09.1-03-PLAN.md`.

---
*Phase: 09.1-ingestion-replumbing-solid-spine*
*Completed: 2026-02-14*
