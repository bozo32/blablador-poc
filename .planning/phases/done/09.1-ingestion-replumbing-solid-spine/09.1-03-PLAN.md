---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 03
type: execute
wave: 3
depends_on: ["09.1-02"]
files_modified:
  - docker-compose.yml
  - backend/settings.py
  - backend/db/__init__.py
  - backend/db/migrate.py
  - backend/db/pg.py
  - backend/object_store/__init__.py
  - backend/object_store/s3.py
  - requirements/app.in
  - requirements/app.lock.txt
autonomous: true

must_haves:
  truths:
    - "Compose starts Postgres and MinIO alongside the app."
    - "App code can connect to Postgres and S3 (MinIO) using env-configured settings."
    - "Schema creation is idempotent on fresh startup."
  artifacts:
    - path: docker-compose.yml
      provides: "postgres + minio services"
    - path: backend/db/migrate.py
      provides: "idempotent schema creation for spine tables"
    - path: backend/object_store/s3.py
      provides: "S3 client wrapper for works/artifacts bucket"
  key_links:
    - from: docker-compose.yml
      to: backend/settings.py
      via: "POSTGRES_DSN + S3 env vars"
      pattern: "POSTGRES"
---

<objective>
Introduce Postgres + MinIO into the Compose stack, plus minimal internal libraries for DB + object store access.

Purpose: Establish the durable ingestion spine (metadata + artifacts) so workers can run remotely.
Output: Compose services + `backend/db/*` + `backend/object_store/*` + settings/env wiring.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@docker-compose.yml
@backend/settings.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postgres + minio to docker-compose (idempotent bucket init)</name>
  <files>docker-compose.yml</files>
  <action>
Extend `docker-compose.yml`:
- Add `postgres` service with a persisted named volume.
- Add `minio` service (S3 compatible) + `minio-console` port mapping.
- Add a one-shot `minio-init` container (using `minio/mc`) to create the `works` bucket idempotently.

Also wire app services:
- `app-api` environment points at Postgres (`POSTGRES_DSN` using host `postgres`) and MinIO (`S3_ENDPOINT_URL` using host `minio`).
- Keep `app-ui` environment minimal; only set what it needs (typically `BACKEND_URL`).

Constraints:
- Keep everything local-only by default; no TLS required.
- Use simple default credentials for local dev via env vars.
  </action>
  <verify>
  docker compose up --build -d && docker compose ps
  </verify>
  <done>Compose shows `postgres` and `minio` running and the `works` bucket exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add settings + minimal DB + S3 client wrappers</name>
  <files>backend/settings.py, backend/db/pg.py, backend/db/migrate.py, backend/object_store/s3.py, backend/db/__init__.py, backend/object_store/__init__.py, requirements/app.in, requirements/app.lock.txt</files>
  <action>
Implement minimal infrastructure code (no behavior changes to ingestion yet):

1) Settings (`backend/settings.py`): add env-backed fields for
- `POSTGRES_DSN` (or components: host/port/user/password/db; pick one approach and keep it simple)
- S3/MinIO: `S3_ENDPOINT_URL`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_BUCKET_WORKS`, `S3_REGION` (optional), `S3_USE_SSL` (bool)
- Internal auth: `INTERNAL_SERVICE_TOKEN` (shared bearer token for worker services)

2) Postgres wrapper (`backend/db/pg.py`):
- Function(s) to open a connection using the DSN (use a lightweight driver like `psycopg`).

3) Migrations (`backend/db/migrate.py`):
- Create an idempotent `apply_migrations()` that creates the minimal tables from `.planning/V2-REPLUMBING-PLAN.md` contract: `works`, `attempts`, `jobs`, `artifacts`.
- Use `CREATE TABLE IF NOT EXISTS` and basic indexes (work_id on attempts, attempt_id on jobs/artifacts).

4) S3 wrapper (`backend/object_store/s3.py`):
- Provide `put_bytes(object_key, bytes, content_type)` and `get_bytes(object_key)`.
- Keep path/layout conventions aligned with V2 spec (bucket `works`).

Update requirements to include the DB + S3 deps used (and recompile lockfile).

Do not yet modify `backend/ingestion_store.py` to use these.
  </action>
  <verify>docker compose exec -T app-api python -c "from backend.db.migrate import apply_migrations; apply_migrations(); apply_migrations(); print('migrated')"</verify>
  <done>
  DB + S3 wrappers exist; migrations run successfully against Compose Postgres.
  </done>
</task>

</tasks>

<verification>
- `apply_migrations()` runs twice without errors (idempotent).
- A trivial object upload/download to MinIO works via the S3 wrapper.
</verification>

<success_criteria>
- Postgres + MinIO are in place, with code-level access for the next plans.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-03-SUMMARY.md`
</output>
