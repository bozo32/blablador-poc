---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 04
subsystem: infra
tags: [fastapi, postgres, psycopg, minio, s3, boto3]

requires:
  - phase: 09.1-ingestion-replumbing-solid-spine
    provides: "Postgres+MinIO runtime, migrations, and baseline S3/PG wrappers (09.1-03)"
provides:
  - "Spine helpers for work_id mapping (doc_id == work_id)"
  - "Postgres works upsert/get helpers aligned to V2 works schema"
  - "Dual-write ingest: local filesystem + S3 PDF object + works row"
affects: [09.1-ingestion-replumbing-solid-spine, extraction-workers, remote-jobs]

tech-stack:
  added: []
  patterns:
    - "Migration invariant: doc_id == work_id"
    - "S3 object layout: works/pdf/{work_id}/{sha256}.pdf"
    - "Idempotent works upsert guarded by sha256"

key-files:
  created:
    - backend/spine/__init__.py
    - backend/spine/ids.py
    - backend/spine/works.py
  modified:
    - backend/main.py
    - backend/ingestion_store.py
    - backend/object_store/s3.py
    - backend/db/pg.py

key-decisions:
  - "Fail fast (with retries) if Postgres migrations / S3 bucket readiness cannot be established on startup"

patterns-established:
  - "Ingest metadata compatibility: append `spine.{work_id,pdf_object_key}` without renaming legacy keys"

duration: 8 min
completed: 2026-02-14
---

# Phase 9.1 Plan 04: Dual-write Ingest to S3 + Postgres Works Summary

**Uploads now dual-write PDFs to MinIO/S3 and upsert Postgres `works` rows while keeping the legacy filesystem ingestion store and metadata format working.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-02-14T12:40:43Z
- **Completed:** 2026-02-14T12:48:41Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Added V2 spine helpers for `work_id` mapping and Postgres `works` access
- Wired `/ingest` to upload PDF bytes to S3 (`pdf/{work_id}/{sha256}.pdf`) and upsert a `works` row
- Extended ingestion `metadata.json` with `spine.work_id` and `spine.pdf_object_key` without breaking existing keys

## Task Commits

Each task was committed atomically:

1. **Task 1: Add spine helpers for work_id and work rows** - `618ead0` (feat)
2. **Task 2: Dual-write ingest upload to S3 and Postgres (keep filesystem)** - `89a98e0` (feat)

**Plan metadata:** (see docs commit for 09.1-04)

## Files Created/Modified

- `backend/spine/ids.py` - doc_id/work_id mapping and PDF object key helper
- `backend/spine/works.py` - `works` table get/upsert helpers
- `backend/main.py` - `/ingest` dual-write to S3 + Postgres, startup readiness checks
- `backend/ingestion_store.py` - nested merge support for `spine` metadata
- `backend/object_store/s3.py` - bucket ensure helper + S3 object existence check

## Decisions Made

- Treat `doc_id` as the canonical `work_id` during migration (as specified), and persist `spine` pointers into legacy metadata for compatibility.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Made Postgres/S3 wrappers import-safe outside the container env**

- **Found during:** Task 1 / Task 2
- **Issue:** Local Python env did not have `psycopg`/`boto3`, causing import-time failures while running plan verification and wiring new imports.
- **Fix:** Lazily import `psycopg`/`boto3` inside connection/client helpers with clear runtime errors when missing.
- **Files modified:** `backend/db/pg.py`, `backend/object_store/s3.py`
- **Verification:** Task verifications passed; Compose runtime continues using full deps.
- **Committed in:** `618ead0`, `89a98e0`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to keep the repo importable and keep verification unblocked; no scope creep.

## Issues Encountered

- Pre-commit formatting/lint hooks (black/flake8) required minor fixes before commits.

## User Setup Required

None - Compose provides Postgres/MinIO defaults.

## Next Phase Readiness

- Ready for follow-on ingestion spine work that consumes `work_id` + `pdf_object_key` (jobs/attempts/artifacts).

---
*Phase: 09.1-ingestion-replumbing-solid-spine*
*Completed: 2026-02-14*
