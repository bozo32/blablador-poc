---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 05
type: execute
wave: 5
depends_on: ["09.1-04"]
files_modified:
  - backend/ingest_pipeline.py
  - backend/ingestion_store.py
  - backend/main.py
  - backend/spine/attempts.py
  - backend/spine/jobs.py
  - backend/spine/artifacts.py
  - backend/object_store/s3.py
autonomous: true

must_haves:
  truths:
    - "Extraction runs are tracked as durable attempts/jobs in Postgres with explicit states."
    - "Extraction artifacts (TEI + extraction.json) are written to S3 and registered in Postgres artifacts."
    - "Retrying the same extraction with the same settings is idempotent (reuses attempt via settings_hash)."
  artifacts:
    - path: backend/spine/attempts.py
      provides: "attempt state machine + idempotency hash"
    - path: backend/spine/jobs.py
      provides: "job rows + progress/heartbeat updates"
    - path: backend/spine/artifacts.py
      provides: "artifact row creation and lookup"
  key_links:
    - from: backend/ingest_pipeline.py
      to: backend/spine/attempts.py
      via: "run_full_ingest_pipeline attempt lifecycle"
      pattern: "attempt"
---

<objective>
Add the durable job/attempt spine for primary extraction while keeping the current ingestion pipeline behavior.

Purpose: Make ingestion repeatable, debuggable, and portable across machines.
Output: Attempt/job rows + S3-backed extraction artifacts + status surfaced through existing ingest endpoints.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@backend/ingest_pipeline.py
@backend/ingestion_store.py
@backend/main.py
@backend/grobid_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement attempts/jobs/artifacts spine helpers</name>
  <files>backend/spine/attempts.py, backend/spine/jobs.py, backend/spine/artifacts.py</files>
  <action>
Implement minimal Postgres-backed spine helpers aligned with V2 contract:

- Attempts (`backend/spine/attempts.py`):
  - `create_or_get_attempt(work_id, kind, settings_json)` returns an `attempt_id` and ensures idempotency via `settings_hash`.
  - State transitions: `queued` -> `running` -> `succeeded|partial|failed|cancelled`.

- Jobs (`backend/spine/jobs.py`):
  - `create_job(attempt_id, worker)` creates a job row.
  - `update_job_progress(job_id, progress_json, heartbeat_at)`.

- Artifacts (`backend/spine/artifacts.py`):
  - `create_artifact(attempt_id, artifact_type, object_key, bytes, content_type)`.

Constraints:
- Keep the schema compatible with migrations from 09.1-03.
- Use `settings_hash = sha256(json.dumps({work_id, kind, settings_json}, sort_keys=True))`.
- Store timestamps as UTC timestamptz.
  </action>
  <verify>python -c "from backend.spine.attempts import create_or_get_attempt; print('ok')"</verify>
  <done>Spine helper modules exist and can create/read rows in Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Wire primary extraction pipeline to spine + S3 artifacts</name>
  <files>backend/ingest_pipeline.py, backend/ingestion_store.py, backend/main.py, backend/object_store/s3.py</files>
  <action>
Update `backend/ingest_pipeline.py` primary extraction flow (`run_full_ingest_pipeline`) to:

1) Resolve `work_id` and `pdf_object_key` from ingestion metadata (`metadata.json` `spine` object).

2) Create/get an attempt row for kind `primary` before starting extraction:
- State: `queued` then `running`.
- Create a job row with worker `grobid`.

3) Run the existing GROBID extraction logic unchanged (fulltext, fallback to header+references).

4) Persist artifacts to S3 (MinIO) under V2 layout:
- `extract/{work_id}/attempts/{attempt_id}/primary/tei.xml`
- `extract/{work_id}/attempts/{attempt_id}/primary/extraction.json`

5) Create artifact rows in Postgres for each stored object.

6) Finalize the attempt state:
- `succeeded` on normal completion.
- `failed` with `failure_reason` on exceptions.

7) Keep the legacy filesystem outputs + metadata stages working:
- Continue writing `tei.xml` under `data/ingestion/...` for now.
- Continue populating `metadata.json` `extraction` stage so the UI remains unchanged.
- Extend metadata with `spine.active_attempt_id`.

Do not add fallback worker or OCR here; that is a later plan.
  </action>
  <verify>
  docker compose up --build -d && python - <<'PY'
import json, subprocess, sys

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

resp = sh('curl -sf -F "file=@fixtures/sample.pdf" http://localhost:8000/ingest')
doc = (json.loads(resp).get('document') or {})
doc_id = doc.get('id')
assert doc_id
sh(f'curl -sf -X POST http://localhost:8000/ingest/{doc_id}/extract >/dev/null')
print(doc_id)
PY
  </verify>
  <done>
  A successful extraction produces: (1) legacy metadata populated, (2) attempt/job/artifact rows in Postgres, (3) TEI/extraction JSON objects in MinIO.
  </done>
</task>

</tasks>

<verification>
- Trigger extraction (`POST /ingest/{doc_id}/extract`) and confirm attempt/job rows exist for the same `work_id`.
- Re-run extraction and confirm idempotency (reuse attempt if settings unchanged).
</verification>

<success_criteria>
- Ingestion runs are durable and portable (state + artifacts persist outside the app container).
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-05-SUMMARY.md`
</output>
