---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 06
type: execute
wave: 6
depends_on: ["09.1-05"]
files_modified:
  - scripts/dev/up.sh
  - scripts/dev/down.sh
  - scripts/dev/smoke_ingest.sh
  - README.md
autonomous: false

must_haves:
  truths:
    - "A fresh machine can run the full stack with `docker compose up --build`."
    - "Upload triggers an end-to-end ingestion run and the spine persists state/artifacts (Postgres + MinIO)."
    - "Services can be moved to remote Linux hosts by changing config (URLs/DSN), without rewriting app logic."
  artifacts:
    - path: scripts/dev/up.sh
      provides: "one-command local stack boot"
    - path: scripts/dev/smoke_ingest.sh
      provides: "CLI smoke for upload/extract"
  key_links:
    - from: scripts/dev/smoke_ingest.sh
      to: http://localhost:8000/ingest
      via: "curl upload + extract"
      pattern: "/ingest"
---

<objective>
Add dev scripts + documentation and perform a full-stack verification checkpoint.

Purpose: Make the replumbed ingestion spine easy to run and easy to prove works.
Output: `scripts/dev/*` helpers, updated README run instructions, and a single end-to-end verification checkpoint.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-CONTEXT.md
@.planning/V2-REPLUMBING-PLAN.md
@docker-compose.yml
@backend/main.py
@backend/ingest_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev scripts for compose up/down and ingestion smoke</name>
  <files>scripts/dev/up.sh, scripts/dev/down.sh, scripts/dev/smoke_ingest.sh</files>
  <action>
Create thin scripts:
- `scripts/dev/up.sh`: runs `docker compose up --build` (and optionally `-d`), then prints the key URLs.
- `scripts/dev/down.sh`: runs `docker compose down -v` (be explicit in output that volumes will be removed).
- `scripts/dev/smoke_ingest.sh`: takes a PDF path, uploads it to `/ingest`, triggers `/extract`, then prints the returned doc id and fetches `/ingest/{id}`.

Constraints:
- Scripts must be POSIX-sh compatible.
- Do not require jq; use python for JSON parsing if needed.
  </action>
  <verify>bash scripts/dev/up.sh && bash scripts/dev/smoke_ingest.sh fixtures/sample.pdf</verify>
  <done>Scripts exist, are executable, and perform the intended actions.</done>
</task>

<task type="auto">
  <name>Task 2: Update README with Compose run instructions</name>
  <files>README.md</files>
  <action>
Add a short section describing:
- `docker compose up --build`
- Service URLs (UI, API docs, GROBID, MinIO console)
- Required env vars (minimal) and the "local defaults" expectation.

Keep existing README content; append/update minimally.
  </action>
  <verify>python -c "print('readme updated')"</verify>
  <done>README includes Docker/Compose quickstart and smoke flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Compose stack + ingestion spine end-to-end (S3 + Postgres)</what-built>
  <how-to-verify>
  1) Run: `bash scripts/dev/up.sh`
  2) Visit: `http://localhost:8501` (UI loads)
  3) Upload a small PDF via UI and wait for extraction to complete
  4) Visit: `http://localhost:8000/docs` and confirm `/ingest/{doc_id}` shows populated extraction stage
  5) Optional: Open MinIO console at `http://localhost:9001` and confirm objects exist under `pdf/` and `extract/`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `scripts/dev/smoke_ingest.sh` completes without errors for a representative PDF.
</verification>

<success_criteria>
- The exit criteria from `09.1-CONTEXT.md` are demonstrably true on a fresh machine.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-06-SUMMARY.md`
</output>
