---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 08
type: execute
wave: 8
depends_on: ["09.1-07"]
files_modified:
  - backend/db/migrate.py
  - backend/spine/documents.py
  - backend/main.py
  - backend/ingestion_store.py
autonomous: true

must_haves:
  truths:
    - "Ingest creates/updates spine identity records for document + document_version (sha256 is a version) in Postgres."
    - "Project membership is tracked so works can be globally de-duplicated while project activity remains scoped."
    - "Existing UI flows remain functional (no breaking response changes required)."
  artifacts:
    - path: backend/db/migrate.py
      provides: "new identity tables and indexes"
    - path: backend/spine/documents.py
      provides: "document/document_version/project membership helpers"
---

<objective>
Introduce the identity split scaffolding required for the full spine flip:

- Bibliographic Work (later)
- Document (global)
- DocumentVersion (sha256 bytes/version)
- Project membership (project_documents)

Purpose: stop treating sha256 as a bibliographic Work identity and enable future version reconciliation (DOI/fingerprint) and project-scoped exports/wipes.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/V2-REPLUMBING-PLAN.md
@.planning/V2-PLANNING.md
@backend/db/migrate.py
@backend/main.py
@backend/spine/works.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add identity tables (documents/document_versions/project_documents)</name>
  <files>backend/db/migrate.py</files>
  <action>
  Add idempotent schema for:

  - `documents` (global, optionally linked to bibliographic work later)
  - `document_versions` (global PDF bytes/version; sha256 unique)
  - `project_documents` (project membership for global documents)

  Keep existing spine tables unchanged for now (attempts still reference work_id) and do not remove legacy ingestion store.
  </action>
  <verify>
  docker compose up --build -d && docker compose exec -T postgres psql -U app -d app -c "\
    \dt documents\
    \dt document_versions\
    \dt project_documents\
  "
  </verify>
  <done>Identity tables exist in Postgres and can be queried.</done>
</task>

<task type="auto">
  <name>Task 2: Implement identity helpers</name>
  <files>backend/spine/documents.py</files>
  <action>
  Implement minimal helpers:
  - `get_or_create_document(document_id, created_by_user_id, biblio_work_id=None)`
  - `upsert_document_version(document_version_id, document_id, sha256, size_bytes, pdf_object_key, created_by_user_id)`
  - `ensure_project_document(project_id, document_id, added_by_user_id)`
  </action>
  <verify>python -c "from backend.spine.documents import get_or_create_document; print('ok')"</verify>
  <done>Helpers import cleanly and perform inserts/reads.</done>
</task>

<task type="auto">
  <name>Task 3: Dual-write identity records during ingest</name>
  <files>backend/main.py, backend/ingestion_store.py</files>
  <action>
  Update `POST /ingest` to, in addition to existing spine writes:
  - create/get a `documents` row
  - upsert a `document_versions` row using sha256 + pdf_object_key
  - ensure a `project_documents` membership row

  Transitional mapping (until reconciliation exists): treat `document_id == doc_id` and `document_version_id == doc_id`.
  </action>
  <verify>
  python - <<'PY'
import json, subprocess

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

resp = sh('curl -4 -sf -F "file=@fixtures/sample.pdf" http://127.0.0.1:8000/ingest?auto_process=false')
assert doc_id
print(doc_id)
PY
  </verify>
  <done>New ingests create identity rows and project membership.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-08-SUMMARY.md`
</output>
