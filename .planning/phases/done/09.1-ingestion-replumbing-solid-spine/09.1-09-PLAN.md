---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 09
type: execute
wave: 9
depends_on: ["09.1-08"]
autonomous: true
files_modified:
  - backend/main.py
  - backend/spine/works.py
  - backend/spine/attempts.py
  - backend/spine/artifacts.py
  - backend/spine/ingest_view.py

must_haves:
  truths:
    - "GET /ingest and GET /ingest/{doc_id} use the spine read path by default when a work exists in Postgres."
    - "Legacy filesystem ingestion store is used only as fallback for missing spine records."
    - "GET /ingest/{doc_id}/body can serve TEI from S3 artifacts when present."
  rollback:
    - "A single env toggle can force legacy reads if needed (SPINE_READS=0)."
---

<objective>
Flip ingestion reads to prefer the new spine (Postgres + S3) while preserving backward compatibility.

This is the first cutover step toward eliminating runtime dependence on `data/ingestion/**`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/V2-REPLUMBING-PLAN.md
@backend/main.py
@backend/object_store/s3.py
@backend/spine/attempts.py
@backend/spine/artifacts.py
@backend/spine/works.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spine ingest view helper</name>
  <files>backend/spine/ingest_view.py</files>
  <action>
  - Implement `build_ingested_document_from_spine(work_id, project_id, include_extraction_data)`.
  - Derive stage statuses from attempts and load `extraction.json` + `tei.xml` from S3 artifacts when requested.
  - Keep the response shape compatible with `schemas.IngestedDocument`.
  </action>
  <verify>python -c "from backend.spine.ingest_view import build_ingested_document_from_spine; print('ok')"</verify>
  <done>Helper can assemble a document record from Postgres+S3.</done>
</task>

<task type="auto">
  <name>Task 2: Flip API reads to spine-by-default with legacy fallback</name>
  <files>backend/main.py, backend/spine/works.py, backend/spine/attempts.py, backend/spine/artifacts.py</files>
  <action>
  - Update `GET /ingest` and `GET /ingest/{doc_id}` to prefer spine reads when available.
  - Add an env toggle `SPINE_READS` (default enabled) to force legacy reads on rollback.
  - Update `GET /ingest/{doc_id}/extraction` and `GET /ingest/{doc_id}/body` similarly.
  - Keep old behavior intact for documents that only exist in the filesystem store.
  </action>
  <verify>
  bash scripts/dev/up.sh && \
    bash scripts/dev/smoke_ingest.sh fixtures/sample.pdf && \
    curl -4 -sf http://127.0.0.1:8000/ingest | python -c "import json,sys; print(len(json.load(sys.stdin).get('documents') or []))" && \
    python - <<'PY'
import json, subprocess

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

resp = sh('curl -4 -sf -F "file=@fixtures/sample.pdf" "http://127.0.0.1:8000/ingest?auto_process=false"')
assert doc_id
sh(f'curl -4 -sf http://127.0.0.1:8000/ingest/{doc_id} >/dev/null')
print('ok', doc_id)
PY
  </verify>
  <done>API reads return documents even if filesystem state is missing, when spine exists.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-09-SUMMARY.md`
</output>
