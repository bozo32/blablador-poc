---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 10
type: execute
wave: 10
depends_on: ["09.1-09"]
autonomous: true
files_modified:
  - backend/db/migrate.py
  - backend/spine/settings_store.py
  - backend/spine/workflow_store.py
  - backend/main.py

must_haves:
  truths:
    - "Settings and workflow definitions are versioned and stored in Postgres under project scope."
    - "A safe-edit policy surface exists (even if minimal) to support future power-user vs noob UI."
    - "No existing endpoints or UI flows break."
  rollback:
    - "New endpoints are additive; disabling them does not affect ingestion/extraction."
---

<objective>
Add inspectable, versioned settings and workflow storage as spine primitives.

Motivation:
- Power-user settings panel later needs a durable store, auditability, and rollbacks.
- Future Orange/Langflow-like workflow builder needs versioned workflow graphs.
- Multi-user later needs project/user scoping from day 1.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/V2-PLANNING.md
@.planning/V2-REPLUMBING-PLAN.md
@backend/db/migrate.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned settings + workflow tables</name>
  <files>backend/db/migrate.py</files>
  <action>
  Add idempotent schema for:
  - settings bundles + versions
  - settings field policy rows (safe edit surface)
  - workflow definitions + versions
  </action>
  <verify>
  docker compose up --build -d && docker compose exec -T app-api python -c "from backend.db import apply_migrations; apply_migrations(); print('migrated')" && \
  docker compose exec -T postgres psql -U app -d app -c "\
    \dt settings_bundles\
    \dt settings_versions\
    \dt settings_field_policies\
    \dt workflow_definitions\
    \dt workflow_versions"
  </verify>
  <done>Tables exist in Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Implement spine stores</name>
  <files>backend/spine/settings_store.py, backend/spine/workflow_store.py</files>
  <action>
  Implement minimal helpers:
  - create/get default settings bundle for a project
  - append settings version (monotonic)
  - fetch latest settings version
  - create workflow definition and append versions
  - list workflow definitions
  </action>
  <verify>
  python -c "from backend.spine.settings_store import get_or_create_settings_bundle; from backend.spine.workflow_store import create_workflow_definition; print('ok')"
  </verify>
  <done>Helpers import cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add additive API endpoints</name>
  <files>backend/main.py</files>
  <action>
  Add small, additive endpoints (project scoped via X-Project-Id header):
  - GET /spine/settings (latest)
  - POST /spine/settings (append version)
  - GET /spine/workflows (list)
  - POST /spine/workflows (create def + first version)
  - POST /spine/workflows/{workflow_id}/versions (append version)
  </action>
  <verify>
  python - <<'PY'
import json, subprocess

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

sh('curl -4 -sf http://127.0.0.1:8000/spine/settings >/dev/null')
resp = sh('curl -4 -sf -X POST http://127.0.0.1:8000/spine/settings -H "Content-Type: application/json" -d "{}"')
assert json.loads(resp).get('bundle_id')
wf = sh('curl -4 -sf -X POST http://127.0.0.1:8000/spine/workflows -H "Content-Type: application/json" -d "{\"name\":\"default\",\"graph\":{}}"')
assert json.loads(wf).get('workflow_id')
print('ok')
PY
  </verify>
  <done>Endpoints can create and read settings/workflows without breaking the rest of the app.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-10-SUMMARY.md`
</output>
