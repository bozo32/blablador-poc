---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 13
type: execute
wave: 13
depends_on: ["09.1-12"]
autonomous: true
files_modified:
  - backend/main.py
  - backend/ingestion_store.py
  - backend/spine/artifacts.py

must_haves:
  truths:
    - "Resolution outputs are stored as S3 artifacts and registered in Postgres artifacts."
    - "GET /ingest/{doc_id}/resolution prefers spine reads with legacy fallback."
  rollback:
    - "SPINE_READS=0 forces legacy reads"
---

<objective>
Move reference resolution outputs to the spine (S3 + Postgres artifacts) so the ingest record is reconstructable without relying on `data/ingestion/**/metadata.json`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/main.py
@backend/spine/ingest_view.py
@backend/spine/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store resolution.json as an artifact</name>
  <files>backend/main.py</files>
  <action>
  On successful resolve:
  - write `resolution.json` to S3 under `resolve/{work_id}/attempts/{attempt_id}/primary/resolution.json`
  - create an artifacts row `artifact_type=resolution.json`
  </action>
  <verify>
  python - <<'PY'
import json, subprocess

def sh(cmd):
    return subprocess.check_output(cmd, shell=True, text=True)

resp = sh('curl -4 -sf -F "file=@fixtures/sample.pdf" "http://127.0.0.1:8000/ingest?auto_process=false"')
doc_id = (json.loads(resp).get('document') or {}).get('id')
assert doc_id
sh(f'curl -4 -sf -X POST http://127.0.0.1:8000/ingest/{doc_id}/extract >/dev/null')
sh(f'curl -4 -sf -X POST http://127.0.0.1:8000/ingest/{doc_id}/resolve >/dev/null')
print('ok', doc_id)
PY
  </verify>
  <done>Resolution run produces an S3 artifact + artifacts row.</done>
</task>

<task type="auto">
  <name>Task 2: Spine-first resolution reads</name>
  <files>backend/main.py, backend/spine/ingest_view.py</files>
  <action>
  Update `GET /ingest/{doc_id}/resolution` and `GET /ingest/{doc_id}` (resolution field) to prefer spine artifacts.
  Keep legacy fallback.
  </action>
  <verify>curl -4 -sf http://127.0.0.1:8000/ingest | python -c "import sys,json; json.load(sys.stdin); print('ok')"</verify>
  <done>Resolution endpoint returns data even if legacy metadata.json is missing.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-13-SUMMARY.md`
</output>
