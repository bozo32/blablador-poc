---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 14
type: execute
wave: 14
depends_on: ["09.1-13"]
autonomous: true
files_modified:
  - backend/db/migrate.py
  - backend/spine/works.py
  - backend/spine/documents.py
  - backend/spine/ingest_view.py
  - backend/main.py

must_haves:
  truths:
    - "Ingest listing/get are driven by project membership tables (project_documents) instead of scanning data/ingestion/**."
    - "PDF bytes identity is global by sha256; project membership is separate."
  rollback:
    - "SPINE_READS=0 forces legacy filesystem listing"
---

<objective>
Complete the 'works are global, projects are membership' split for ingestion listing.

This removes a major legacy tail: `list_ingested_documents()` scanning directories.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/ingestion_store.py
@backend/spine/documents.py
@backend/spine/ingest_view.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spine list helpers for project_documents + document_versions</name>
  <files>backend/spine/documents.py, backend/spine/ingest_view.py</files>
  <action>
  Implement a spine list that returns the same payload as `GET /ingest` but sourced from:
  - `project_documents` (membership)
  - `document_versions` (sha256, size, pdf_object_key)
  - latest attempts + artifacts for stage state
  </action>
  <verify>python -c "from backend.spine.ingest_view import list_ingests_from_spine; print('ok')"</verify>
  <done>Helper returns a list without touching filesystem.</done>
</task>

<task type="auto">
  <name>Task 2: Switch GET /ingest to prefer project_documents-based list</name>
  <files>backend/main.py</files>
  <action>
  Update `GET /ingest` spine path to use the new project_documents-based list.
  Keep legacy merge fallback.
  </action>
  <verify>curl -sf http://localhost:8000/ingest >/dev/null && echo ok</verify>
  <done>List endpoint no longer depends on works table ordering or filesystem scan.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-14-SUMMARY.md`
</output>
