---
phase: 09.1-ingestion-replumbing-solid-spine
plan: 15
type: execute
wave: 15
depends_on: ["09.1-14"]
autonomous: true
files_modified:
  - backend/main.py
  - backend/ingestion_store.py
  - backend/graph_store.py
  - frontend/ui.py
  - scripts/dev/down.sh

must_haves:
  truths:
    - "The app can run with legacy ingestion store disabled (no runtime dependency on data/ingestion/**)."
    - "UI still works and can ingest/extract using spine-only paths."
  rollback:
    - "SPINE_MODE=legacy restores current behavior"
---

<objective>
Introduce a spine-only mode and deprecate legacy ingestion store as a runtime dependency.

Legacy `data/ingestion/**` remains available as an optional export/debug artifact, not as the live system of record.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add SPINE_MODE toggle (legacy/dual/spine)</name>
  <files>backend/main.py, backend/ingestion_store.py</files>
  <action>
  Implement `SPINE_MODE`:
  - `legacy`: current filesystem-first behavior
  - `dual`: write both, read spine-first
  - `spine`: read/write spine only; legacy used only for export
  </action>
  <verify>SPINE_MODE=spine curl -sf http://localhost:8000/openapi.json >/dev/null && echo ok</verify>
  <done>Service starts in spine-only mode.</done>
</task>

<task type="auto">
  <name>Task 2: Remove remaining filesystem-only read tails in UI/graph enrichment</name>
  <files>backend/main.py, backend/graph_store.py, frontend/ui.py</files>
  <action>
  Replace any remaining calls that assume filesystem listing (e.g. ledger enrichment loops over `list_ingested_documents`) with spine-backed list.
  </action>
  <verify>curl -sf http://localhost:8000/ledger >/dev/null && echo ok</verify>
  <done>Ledger/graph views work without filesystem ingestion present.</done>
</task>

<task type="auto">
  <name>Task 3: Add an easy 'wipe everything' UX for POC</name>
  <files>frontend/ui.py, backend/main.py, scripts/dev/down.sh</files>
  <action>
  Add a guarded wipe button for local POC:
  - clears Postgres spine tables
  - clears MinIO objects for the bucket
  - clears legacy data directories if present
  Use a loud confirmation flow.
  </action>
  <verify>
  python -c "print('manual UI verify: wipe flow present and gated')"
  </verify>
  <done>Single-user POC can reset completely from the UI.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/09.1-ingestion-replumbing-solid-spine/09.1-15-SUMMARY.md`
</output>
