---
phase: 09.2-ingestion-automation-robustness-fallback-extraction
plan: 03
objective: "If GROBID header parsing fails, still extract references and record header failure as a quality flag."
depends_on:
  - plan: 02
provides:
  - "Refs-only extraction path persisted in spine"
  - "Quality flagging for header failure"
---

# 09.2-03: Header-500 Resilience (Refs-Only)

## Work

- When `processHeaderDocument` fails, continue by calling references extraction.
- Persist refs artifact even if header/fulltext fails.
- Ensure attempt is `partial` (not `failed`) when refs extraction succeeds.

## Verification

- Use the choker fixture.
- Confirm:
  - attempt state becomes `partial`
  - quality flags indicate `primary.header_failed=true` and `primary.refs_only=true`
  - refs artifact exists in object store and is readable via artifacts pointers
