---
phase: 09.2-ingestion-automation-robustness-fallback-extraction
plan: 07
objective: "Gate reference resolution based on reference quality; prompt users when refs are low-quality instead of silently running resolution."
depends_on:
  - plan: 06
provides:
  - "Resolution policy based on quality flags"
  - "User-visible prompts and explicit overrides"
---

# 09.2-07: Resolution Gating by Refs Quality

## Work

- Define a single policy function: given attempt quality flags, decide:
  - allow resolution automatically
  - require user confirmation
  - block resolution (hard failure)

## Verification

- Normal PDF: resolution auto-runs and persists `resolution.json` artifact.
- Choker refs-only: resolution is gated/prompted.
- OCR fallback: resolution is gated/prompted.
