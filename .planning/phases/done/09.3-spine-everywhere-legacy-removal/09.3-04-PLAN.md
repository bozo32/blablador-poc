---
phase: 09.3-spine-everywhere-legacy-removal
plan: 04
objective: "Persist project metadata (reviewers, preferences, settings) in the spine and remove dependence on data/project.json." 
---

# 09.3-04: Project Metadata to Spine

## Work

- Add Postgres tables (or reuse existing settings/workflow versioning) to store:
  - reviewer identities + compare selections
  - UX preferences (dense mode, etc.)
- Provide a small API surface for UI read/write.

## Verification

- Create/rename reviewer, reload, verify persistence.
- Delete local `data/project.json` and confirm no workflow breaks.
