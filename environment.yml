name: blablador310
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy>=1.23
  - pandas>=2.0
  - scikit-learn>=1.3
  - tqdm>=4.65
  - requests>=2.28
  - pydantic=1.10.14
  - python-dotenv
  - faiss-cpu>=1.7
  - streamlit>=1.26
  - uvicorn>=0.23
  - watchdog>=2.1
  - fastapi>=0.85
  - pypdf
  - pip
  - pip:
      - transformers>=4.35
      - sentence-transformers>=2.2
      - rank_bm25>=0.2
      - huggingface-hub>=0.20
      - pymupdf           # <-- move from conda to pip
      - pydantic-settings<2.0  # <-- move from conda to pip
      - torch>=2.1             # <-- move from conda to pip (PyTorch for ARM is best on pip)
      - black
      - flake8
      - fastcoref