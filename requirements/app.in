# Runtime dependencies for container images (app-api + app-ui).
# Keep this intentionally small and let pip-compile resolve transitive pins.

numpy<2

fastapi
uvicorn
python-multipart

pydantic>=2
pydantic-settings

python-dotenv
requests

# Fallback text extraction (Phase 09.2)
pypdf

# OCR-capable fallback worker (Phase 09.2)
pymupdf
pillow
pytesseract

# V2 ingestion spine: Postgres + S3
psycopg[binary]
boto3

lxml
tqdm

faiss-cpu
sentence-transformers
torch
transformers

rank-bm25

streamlit
streamlit-agraph
streamlit-autorefresh

pandas
graphviz
watchdog
